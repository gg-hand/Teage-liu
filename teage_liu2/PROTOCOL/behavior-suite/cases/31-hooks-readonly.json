{
  "id": "hooks-readonly-31",
  "protocol_version": "v1.0.0",
  "description": "hooks H-17(只读视图强制)+ types T-3: inputs.mutate_snapshot 让扩展在 before 内**原地篡改** snapshot.messages(追加一条 assistant)与 extra —— core 交给扩展的是**只读视图**(messages/tools/history → tuple,extra → MappingProxyType),两处篡改均抛异常被拒,进入 LLM 的消息序列不含篡改(断言 messages_roles 恒为 [user])。2026-09-11 由 S2「边界只读视图」落地,取代此前如实锚定『篡改生效』的旧断言(旧断言为 [user, assistant])。",
  "setup": {
    "extensions": [
      {
        "name": "ext_a",
        "lang": "python",
        "transport": "in-process",
        "protocol_version": "v1.0.0",
        "hooks_implemented": ["before"],
        "capabilities": []
      }
    ]
  },
  "inputs": {
    "session_id": "s-31",
    "user_input": "hi",
    "mutate_snapshot": true,
    "ext_a_actions": []
  },
  "expected": {
    "event_stream": [
      { "type": "step_start" },
      { "type": "done", "payload": { "termination_reason": "normal" } }
    ],
    "final": {
      "termination_reason": "normal",
      "llm_assert": { "messages_roles": ["user"] }
    }
  }
}
