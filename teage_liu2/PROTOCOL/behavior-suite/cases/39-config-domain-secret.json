{
  "id": "config-domain-secret-39",
  "protocol_version": "v1.0.0",
  "description": "config C-2(敏感字段): ${VAR} 占位注入 + .env 分离 + 脱敏 —— 占位符必须被环境变量替换为真实值(不得残留 ${...} 字面量),密钥明文不得出现在任何错误消息中,脱敏输出必须含 **** 标记(隐藏明文)。",
  "setup": { "extensions": [] },
  "inputs": {
    "env": { "BS_TEST_KEY": "sk-secret-9f3a" },
    "yaml": "llm:\n  main_provider: anthropic\n  main_model: m\n  main_api_key: ${BS_TEST_KEY}\ncore:\n  max_loops: 5\n",
    "probe": { "field": "llm.main_api_key" }
  },
  "expected": {
    "event_stream": [
      {
        "type": "custom:config_secret_result",
        "payload": {
          "placeholder_resolved": true,
          "secret_not_in_errors": true,
          "masked_hides_secret": true
        }
      }
    ]
  }
}
