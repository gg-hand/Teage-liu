# teage_liu2 开发日志

> **定位**:`teage_liu2/` 新系统的开发变更摘要(追加式,顶部最新)。与项目根 `CHANGELOG-开发日志.md` 的分工:**本文件只记 teage_liu2 新系统开发变更**;跨系统/全局/老系统的变更仍记在根 CHANGELOG。

## 2026-09-18 两系统配置解耦（WP-H-4 立项落地）

> 来源:`docs/plans/2026-09-17-两系统配置解耦-立项评估.md`(方案 B)。**协议无新增元素,VERSION 仍 v1.0.0;套件 46/46、pytest 260 passed、audit exit=0。**

- **liu2 独立配置**:新增 `config-liu2.yaml`(仓库根,gitignored),只含 liu2 实际消费的 4 段 `core` / `llm` / `storage` / `host_components`;`${VAR}` 占位原样保留、共用根 `.env`。**`storage.sqlite_path` 由 `data/sessions.db` 改为 `data2/sessions.db`** —— 原值指向老系统会话库,是"Rust 存储代理缺位"时的兜底,属实质隐患修复。
- **加载接线**:`teage_liu2/server/app.py` 的 `create_app` 改配置来源优先级 —— `config` 参数 > `config_path` 参数 > 环境变量 `TEAGE2_CONFIG` > 默认 `config.yaml`(向后兼容,老行为不变)。
- **启动脚本**:新增 `start_liu2.ps1`(固定 7878、`TEAGE2_CONFIG=config-liu2.yaml`、载入共用 `.env`、端口与 API Key 前置校验、`uvicorn ... --factory`),解决 liu2 长期无启动脚本的问题;老系统 `start.ps1`/`restart.ps1`/`stop.ps1` 零改动。
- **.gitignore**:增 `/config-liu2.yaml`、`/config-liu2.yaml.bak`(与 `config2.yaml` / `config-a2a-*.yaml` 同策略)。**CODEBUDDY.md** 常用命令与配置约定同步为双系统口径。
- **验证(全部实测)**:①`load_config("config-liu2.yaml")` 4 段 + 占位解析 + `core_config_from` 通过 ②拦截 `load_config` 观测入参 —— 设 `TEAGE2_CONFIG` 入参为 `config-liu2.yaml`、**不设**为 `config.yaml`(回归对照) ③**脚本级实跑** `.\start_liu2.ps1` → banner 正确(Config=config-liu2.yaml) + `/health` 200 + `/ui` 200 ④**守卫分支实跑** —— 端口占用报真实 PID 且退出码 1、配置缺失报 ERROR 且退出码 1 ⑤`POST /reload` 200 `{"reloaded":true,"branches":["guardrails","audit"]}`(分支取自新文件;`/reload` 读 `app.state.config_path`,无"读 A 写 B";liu2 无 `/config` 写回路由) ⑥门禁 46/46 + 260 passed + exit=0。
- **缺陷自修(本次引入)**:`start_liu2.ps1` 首版为**无 BOM + LF**,而仓库既有 4 个 `.ps1` 均为 **UTF-8 BOM + CRLF** —— PS 5.1 `-File` 对无 BOM 文件按 GBK 解码,中文注释字节被误解,**行号错位**并抛 `Missing closing '}'` / `Missing expression after unary operator '--'`(后者只是下游症状)。修正为 BOM + CRLF 后语法检查 0 错误,实跑通过。**教训:新建 `.ps1` 必须同仓库编码(BOM);且"语法检查通过"≠"可运行",必须实跑。**
- **勘误**:初版评估称"`storage` 段疑似 liu2 专用"**有误** —— 老系统明确消费(`teage_liu/app.py:329/352/429`、`background_loops.py:31/118` 的 `.get("storage", {})`),当时 grep 只覆盖 `cfg["storage"]` 形式。检索共享面须同时覆盖 `[...]` 与 `.get(...)`。
- **二次核查与补做(同日,完美主义口径)**:
  - **键级复核纠正两处说反的口径**:①`llm` 段并非"两侧读不同键子集" —— 实测 **12 键中 11 键两系统同读**(含 `main_api_key` / `consolidation_api_key`),即"改一处同时改两个系统的 provider/model/密钥";②`storage` 段并非"三键全读" —— liu2 **只读 `sqlite_path`**,`session_ttl_days` / `cleanup_interval_hours` 属老系统专属(liu2 未实现 TTL/清理)。另:顶层段数 **22**(非 23)。
  - **文档一致性补做(初版遗漏,属项目纪律"文档一致性是交付项")**:`teage_liu2/docs/CORE.md` §8、`INTERFACES.md`(启用配置 + 示例代码)、`docs/SUBSYSTEM-SPI.md` §13.1/§13.2、`docs/ARCHITECTURE.md` 目录清单 —— 四处仍把 `config.yaml` 当作 liu2 的配置文件,已改为双配置口径;`core/config.py` / `extension_loader.py` / `llm.py` 的误导性 docstring 与错误文案同步(`load_config` 默认值 `"config.yaml"` **保留不动**,向后兼容)。
  - **配置键级清理**:`config-liu2.yaml` 移除 3 个 liu2 零消费的死键(`storage.session_ttl_days`、`storage.cleanup_interval_hours`、`llm.context_threshold`)—— 因"非 core 段不校验未知键"而本不报错,但保留即误导;剔除后 `llm` 段 11 键、`storage` 段仅 `sqlite_path`,实测加载与 `core_config_from` 均正常。
  - **新增两条风险登记**(见评估文档 §2 第 6/7 条):①未知键拒绝**仅覆盖 `core` 段**,`llm`/`storage`/`host_components` 段 typo **静默回落默认**(实证 `llm.actvity_timeout=99` → 回落 60),解耦后两份 config 并存使该风险放大,属 core 行为变更**须单独立项**;②`.env` 以 `load_dotenv(override=True)` 加载,若把 `TEAGE2_CONFIG` 写进 `.env` 会覆盖启动脚本设置 → 已定为防御约定(`CODEBUDDY.md` 已登记)。
  - **行号引用纪律定调**:本次共两次行号漂移(`app.py` 内改动、`core/config.py` docstring 补注),故评估文档已把易漂移引用改为**符号名**(如 `_CORE_ALLOWED_KEYS` / `core_config_from`),仅结构稳定处保留行号 —— 后续引用优先"文件 + 符号名"。
- **配置校验严格性立项评估(同日,评估性质)**:产出 `docs/plans/2026-09-18-配置校验严格性-立项评估.md` —— **6 个 typo 场景逐一实测**。判定:①"严格校验限 `core` 段"**原则合理**且契约锚定(`config.spec` §1 明文 + 行为条款 C-1 + 套件 case 21 固化),其合理性来自依赖铁律(core 不知道枝干名,无法枚举扩展段键名);②但落地有 3 缺口 —— **G1 契约违规**:`host_components[].backend` 键名拼错 → **静默回落内置 SQLite**(实测装配为 `SQLiteStorageProvider`,存储路径由 `data2/storage_rust/sessions.db` 静默变为 `data2/sessions.db`),直接违反 spec §3"必填∈注册表""不做静默降级、不回落内置实现";**G2 覆盖空洞**:`llm`/`storage` 段既非 core 段也非扩展段,C-1 二分法未指派归属 → 零校验(实测 `llm.main_base_urll` 拼错 → 静默回落 provider 默认端点);**G3 委派未达目标**:C-1 把扩展段校验委派给扩展 setup,但现网扩展只做**值校验**、无未知键拒绝(实测 `guardrails.denylist` → `denylst` **未报错**、`denylist=[]`、setup 放行 → **拦截静默关停**)。另发现套件**覆盖盲区**:case 24 只测"值非法"(`backend: "nope"`),未测"键缺失/拼错",正是 G1 的向量(假绿式覆盖)。**本次未改任何代码/配置,不改 VERSION。**
- **配置校验严格性:Phase 1 落地(同日,已执行)**:按"更适合 liu2"的方式实施 —— **协议 schema 成为配置结构校验的运行时唯一源**,而非再手写一遍白名单。根因:此前"`config.schema.json` 只作文档 + 实现另手写校验"是**双源必然漂移**(历史上已双向漂移:`extensions_root` 为"实现先/schema 后补";`HostComponent` 的 `required`+`additionalProperties:false` 为"schema 先/实现从未落实")。改动:①`core/config.py` 新增 `load_config_schema()`(协议文件缺失/损坏 = 启动失败,绝不静默跳过校验)、`check_host_components_segment()`(消费 schema 的 `definitions.HostComponent`,经零依赖 `core/schema_check.py`)、`_config_code_for()`(描述→`CONFIG_*` 单一映射点);②`server/host_components.py` 接入该段校验,**删除 `entry.get("backend","sqlite")` 默认回落**与 `or []` 写法(后者使 `null`/`{}`/`0`/`""` 等 falsy 非数组静默当缺省),全部**配置输入型**失败补 `CODE: ` 前缀(对齐 errors 域 §5 面④;backend 契约型失败仍为 `TypeError`);③测试对齐(`test_server_host_components.py`:拆分"缺省=sqlite"错误锚定、新增缺必填/未知键/falsy 非数组三组断言);④套件 `host-component-reject-24` 向量 **5→7**(补「缺必填 backend」「条目内未知键」+)并改由 schema 结构校验承载;⑤契约/文档同步:`config.spec.md` §1(新增"schema 运行时地位"条款)+§3(条目级校验归属与结构/语义职责切分)、`errors.spec.md` §5 面④覆盖面、`PENDING.md`(P-1 计数 5→7、P-6 勘误"backend 缺省 sqlite"**不成立**)、`config.yaml.example`、`CORE.md`、`core/config.py` 模块 docstring。**验证**:套件 46/46、pytest 260→**266 passed**、`audit_liu2.ps1` exit=0;两条行为实测(backend 拼错→`CONFIG_MISSING_KEY`、条目内未知键→`CONFIG_UNKNOWN_KEY`、falsy 非数组→`CONFIG_INVALID_VALUE`、合法路径不变);**变异验证 2 组**(停用 schema 校验 → pytest 7 红 + 套件 case24 红 `rejected 6/7`;还原 `or []` → G4 三例红;均逐字节还原);启动实跑 `/health` 200。**不改 VERSION**(未增删/改名配置键,依 config.spec §4)。
- **配置校验严格性:Phase 1.5 收口(同日,未完成事项核查后立即处理)**:核查发现 4 项遗留并处理 —— ①**`core` 段仍是双源**(`_CORE_ALLOWED_KEYS` 11 键 vs schema `CoreConfig.properties` 11 键,当前一致但无机制保障,且 schema 的 `CoreConfig` 至今零消费者);**决定不迁 schema 驱动**(该段承载 schema 未表达的额外语义:`injection_budget_chars` 每层 100–100000 范围、「可用:」提示文案,迁移会**弱化**校验),改以**机制锁定**:新增契约同步测试 `test_config.py::test_core_allowed_keys_matches_protocol_schema`(白名单必须等于 schema properties 键集 + 断言 `additionalProperties:false`),漂移即红。②**loader 的 `options 非映射`分支已成死代码**(实测被 schema 抢先:`host_components[0].options 类型应为 object`)→ 删除,并修正对应测试项归属说明(此前"绿了但测的是 schema 层",属我方案里自己警告过的"静默变绿但测错分支")。③`HookChain.setup_all` 补 docstring **⚠非生产装配路径**标注(与 `BranchRegistry.setup_all` 同名不同义,是我执行前审查误判的源头)。④记录已知前置:若将来迁移 `core` 段,须先给 `core/schema_check.py` 补 `propertyNames` 支持。**验证**:套件 46/46、pytest 267 passed、audit exit=0;**变异验证**(向 `_CORE_ALLOWED_KEYS` 注入伪键 → 契约同步锁必红并精确报出漂移键;逐字节还原)。
- **配置校验严格性:Phase 4 落地(同日,修 G3 安全缺口)**:G3 = config 域 C-1 把扩展段校验委派给扩展 setup,但**只做值校验防不住 typo** —— 实测 `core.branches.guardrails.denylist` 写成 `denylst` → 扩展取到 `denylist=[]`、setup 放行 → **拦截静默关停**(安全相关)。修法(纪律 + 机制并施,**零协议改动**):①`core/config.py` 新增零依赖助手 `reject_unknown_keys(segment, allowed, segment_name)`(缺席=合法缺省;未知键→`CONFIG_UNKNOWN_KEY` + 「可用:」提示;非映射→`CONFIG_INVALID_VALUE`)——**归属不变量**:`allowed` 由扩展自己声明,core 不参与判定枝干键集,**不违"core 不知道枝干名"铁律**;②`data2/extensions/{guardrails,audit}` 的 setup 接入(注:data2 为 gitignored 安装态,仓库内耐久锚点见下);③仓库内锚点:新增 `tests_core::test_reject_unknown_keys_helper`(助手单测)与 `tests_branches::test_unknown_config_key_rejected`(经 extension_loader 装载**真实**扩展,扩展未装则 skip);④纪律落文:`config.spec.md` C-1 增「C-1 完整性要求」条款、`SUBSYSTEM-SPI.md` §12 与 `CORE.md` §8 接入三步法各加"配置自校验须含未知键拒绝"、`INTERFACES.md` 扩展模板示例补该调用。**验证**:套件 46/46、pytest 267→**269 passed**、audit exit=0;行为实测(guardrails/audit typo → `CONFIG_UNKNOWN_KEY` 且列可用键;合法配置不变);**变异验证**(注释掉扩展侧调用 → `test_unknown_config_key_rejected` 报 `DID NOT RAISE` 必红;逐字节还原)。
- **配置校验严格性:实施方案 + 执行前审查(同日,评估性质,未改代码)**:产出 `docs/plans/2026-09-18-配置校验严格性-实施方案.md`(v2) 与 `…-执行前审查.md`。**Phase 1 定性 = 实现对齐既有契约**(`config.schema.json:38-39` 早已 `required:["slot","backend"]` + `additionalProperties:false`;`config.spec.md:41/49/53` 早已声明必填/未注册即失败/不回落内置实现)→ 非"加严取舍"。**审查(两轨独立 + 主 agent 裁决)抓出方案 v1 的 4 项必改**:①示意代码误用 `SLOTS`(值 ABC 类元组,`sorted` 抛 TypeError)应为 `BACKENDS`;②文档同步清单失实(误列 CORE/INTERFACES/SPI,漏 `host_components.py:11`/`config.yaml.example:344`/`PENDING.md:77`);③**C-1 修正方向错误**(生产装配走 `registry.setup_all` 传 `branch_cfg`,C-1 括注本正确;误判源于把仅测试用的 `HookChain.setup_all` 当生产路径);④错误码"先不补"建议不成立(`errors.spec:69` ④面要求启动失败带 `CODE:` 前缀,现为裸 `ValueError`=既存漂移)。并**补录既存漏洞 G4**:`cfg.get("host_components") or []` 使 falsy 非数组静默当缺省,漂移于 schema `type: array`。Phase 2–4(取消 config.yaml 回落 / `llm`·`storage` 段校验 / 扩展段 typo 防护)仅设计描述,**未做执行前审查,须各自单独立项**。

- **配置校验严格性:Phase 2 落地(同日,取消 config.yaml 回落)**:`server/app.py` 新增 `DEFAULT_CONFIG_PATH = "config-liu2.yaml"`;`create_app` 优先级 = `config` 参数 > `config_path` 参数 > `TEAGE2_CONFIG` > `config-liu2.yaml`,且**该文件缺失即抛 `CONFIG_MISSING_KEY`(启动失败),不再回落老系统 `config.yaml`**(根治"liu2 静默读到老系统配置"这一隐患类别 —— 历史上 `storage.sqlite_path` 正因此指向老系统会话库)。**验证**:无 env 时实测加载 `config-liu2.yaml`;新增锚点测试 `test_server_routes.py::test_create_app_requires_liu2_config_and_never_falls_back`(**即便目录内放一份老系统 `config.yaml` 也必须不被采用**);门禁 套件 46/46、pytest 270 passed、exit=0;`start_liu2.ps1` 实跑 `/health` 200。文档同步:`CORE.md` §8、`INTERFACES.md`(启用配置 + 示例)、`core/config.py` docstring、`CODEBUDDY.md`。**注:本次未触碰 `start_liu2.ps1`（避免 Edit 破坏其 BOM 编码）。**
- **配置校验严格性:Phase 3 立项评估 + 执行前审查(同日,评估性质,未改代码)**:产出 `docs/plans/2026-09-18-宿主段校验(Phase3)-立项评估.md`。评估:给 schema 补 `LlmConfig`(11 键)/`StorageConfig`(1 键) 定义 + `additionalProperties:false`,实现侧新增 `check_declared_segments(cfg)`(零白名单,键集改动只改 schema);契约须改**三类归属**(core 段 / 宿主段 / 扩展段)+ 补 §4 演进规则。**执行前审查发现原评估 3 处须修正**:①套件成本偏乐观(`runner._run_config_case` 不调宿主段校验,需改 2 处:分派 + `protocol_pipeline_ids`);②类型收紧不止 `activity_timeout` —— `max_context_tokens` 同样有 `int()` 宽容(`llm.py:1028`);③**`config.spec.md` §4 存在真空**(未规定宿主段归属与演进级别、无"语义收紧"分类,须补 4 项,其中"宿主段键增删级别"属**新定规则**)。审查同时**证实** 11 键清单准确(逐键行号)、`storage` 闭合安全、越界键仅存在于老系统资产(8 处 + 2 老系统测试文件,Phase 2 后不影响 liu2)。**未发现阻断项,方案可执行。**

- **配置校验严格性:Phase 3 落地(同日,修 G2 宿主段 —— 承接上面 Phase 3 立项评估)**:`llm` / `storage` 段既非 core 段也非扩展段,C-1 二分法未指派归属 → **零校验、typo 静默回落**;本轮按已建立架构收口,**零白名单**。①**契约先行**:`config.schema.json` 新增 `definitions.LlmConfig`(11 键 = liu2 实际消费面,`additionalProperties:false`)/ `definitions.StorageConfig`(仅 `sqlite_path`),`llm`/`storage` 由 `{"type":"object"}` 改 `$ref`(`server` 段**保持不闭合**);两段类型含 `null`(YAML 空段 `llm:` 的缺省写法与缺席等价,schema 明文声明而非实现特例)。②**实现**:`core/config.py` 新增 `_closed_segment_definitions()` + `check_declared_segments(cfg)` —— **数据驱动**:段 = `definitions.ConfigFile.properties` 中 `$ref` 指向"带 `additionalProperties:false` 的定义"者(`core` 因承载 schema 未表达的分层语义、`host_components` 因需逐条 `[i]` 定位而显式排除),**段清单与键集都不写死在实现里**;失败经既有 `_config_code_for()` 映射为 `CONFIG_*` 并抛 `ValueError`(未知键附「可用:」提示,与 core 段文案一致)。③**调用点两处且同口径**:`create_app`(**置于一切构造之前** —— 否则 `max_context_tokens="abc"` 会先在 `LLMClient` 内以裸 `int()` 异常暴露、`main_base_urll` 更会静默回落 provider 默认端点)+ `routes.reload`(消除"启动拒绝、reload 接受"的后门)。④**契约文本**:`config.spec.md` §1「严格校验限 core 段」→ **三类归属**(core 段 / 宿主段 / 扩展段)+ schema 运行时地位补 llm/storage + **C-1 补第②类**;§4 补四项(三类归属、"宿主段键集由本域 schema 声明非扩展可决定"、**"未增删/改名键的校验语义收紧 = 不升 VERSION + 登记 PENDING"**、**"宿主段键增删级别 = 与 core 键同规则"属新定规则**);`PENDING.md` 新增 **P-11**(errata,含 G2/G3 与"G1/G4 属实现违反已写明契约、不涉协议决策故不登记"的界定)。⑤**测试**:`tests_core/test_config.py` 新增 6 组(未知键+可用提示、老系统专属键 3 例、类型/范围收紧、缺席与 null 缺省、非映射根、**零白名单机制锁** `test_declared_segments_are_schema_driven`);`test_server_routes.py` 新增 2 锚点(`create_app` 拒绝 llm typo;`/reload` 与启动同口径拒绝);套件新增 `cases/47-config-domain-host-segment.json` + `runner.py` 改 2 处(宿主段驱动块 + `protocol_pipeline_ids`)—— 用例只断言错误码、**不复制键清单**(复制即第二份事实源)。⑥**验证**:套件 **46→47/47**、pytest **270→278 passed**、`audit_liu2.ps1` exit=0;**变异验证 4 组必红且还原逐字节 sha256 一致**(①`check_declared_segments` 变空操作 → 套件 case47 红 + 6 测试红 ②schema 去 `additionalProperties:false` → 套件 case47 红 + 6 测试红 ③移除 `create_app` 调用 → 仅 create_app 锚点红 ④移除 `reload` 调用 → 仅 reload 锚点红);**实跑** `.\start_liu2.ps1` → `/health` 200(`protocol_version: v1.0.0`)+ `/ui` 200(证明现网 `config-liu2.yaml` 在收紧后仍合规、零迁移);**fail-fast 实测** `create_app(config_path="config.yaml")`(老系统真实配置)→ `CONFIG_UNKNOWN_KEY: storage 段 不允许字段 'cleanup_interval_hours'(可用: sqlite_path)`。⑦**行为收紧须明示**:`llm` 三个数值键此前容忍"数字字符串"(`int()`/`float()`),现 = 启动失败;`llm.context_threshold` 与 `storage.session_ttl_days`/`cleanup_interval_hours` 现为未知键(Phase 2 后默认路径读不到,仅 `TEAGE2_CONFIG` 指向老系统文件时触发,属期望 fail-fast)。⑧**文档同步**:`CORE.md` §7、`SUBSYSTEM-SPI.md` §9、`config-liu2.yaml` 头注释(原"非 core 段未知键不报错(静默忽略)"已失效,改为三键现为启动失败)。**不改 VERSION**(未增删/改名任何配置键,依 §4)。
  - **配置模板已补(同日)**：新增根 `config-liu2.yaml.example`(入库;实际 `config-liu2.yaml` 仍 gitignored —— `.gitignore` 用 `/config-liu2.yaml` 精确锚定,不误伤 `.example`),只含 liu2 实际消费的 4 段,header 写明严格校验口径、老系统专属键写上即启动失败、数值写纯数字、`TEAGE2_CONFIG` 不得进 `.env`;并标注两处易踩坑(`branches:` 留空为 null 会被拒 → 写 `{}`;声明未装的扩展 = 启动失败 → 扩展条目默认注释掉、`host_components` 整段注释保留可用形态)。**实测**:`load_config` + `check_declared_segments` + `core_config_from` 通过 → `create_app(config_path="config-liu2.yaml.example")` **OK**(复制即能启动)。另 `core` 段迁 schema 驱动的前置仍是给 `core/schema_check.py` 补 `propertyNames` 支持。
- **配置校验严格性:Phase 3 执行后独立审查(同日;协议一致性 + 可行性回归两轨,主 agent 取证裁决)**:产出的 13 项发现全部取证复核,**7 项落地修复**(其余为"不改"判定)。**真回归 1 项**:`#1` 审查实测 `create_app(config=["x"])` 抛 `AttributeError: 'list' object has no attribute 'get'` —— 我把校验点插在 `cfg.get("_config_path")` **之后**,使校验内"配置根必须是映射"分支不可达 → 前移到 `cfg` 任何字段读取之前(锚点 `test_create_app_rejects_non_mapping_config_root`)。**口径分裂 1 项**:`routes.reload` 当时只补 llm/storage,而 `host_components` 在热重载路径**零校验**(该路径不重装宿主组件,`load_host_components` 未被调用)→ 补段结构校验;语义类(未知插槽/backend、重复接管、ABC)仍只在启动期校验(装配含 spawn,不可在热重载做),该**刻意非对称**已写进代码注释。**漂移 2 项(A 轨)**:①`host_components: null`(YAML 空段)被当缺省,而 schema 只写 `type: array` → 判为"**显式缺省**"并**改由 schema 声明**(`["array","null"]`;条目内 `options` 同办 `["object","null"]`),消除"实现容忍、契约未声明"的漂移(G4 的实质是 `{}`/`0`/空串等**类型错误**被当缺省,那部分照旧拒绝);②`CODEBUDDY.md` 仍写"未知键校验只覆盖 `core` 段、llm/storage 静默回落"(与本轮直接矛盾,且该文件就在本次 diff 内)→ 改写为四段严格校验 + 模板指引,补记老系统专属键与"数值写纯数字"。**子集校验器的静默洞 1 项**:`core/schema_check.py` 对未支持关键字(`$ref`/`allOf`/`propertyNames`…)宽容跳过 → 契约侧给闭合段加一层 `$ref` 即变"看着校验了其实没校验" → 新增**机制锁测试** `test_declared_segments_have_no_unsupported_keywords`(闭合段含此类关键字即红)+ **段账目测试** `test_config_file_segments_are_all_accounted_for`(`ConfigFile.properties` 每段必须被显式归类 —— `core`/`host_components` 专用校验、闭合段、或"liu2 不消费的段",否则新段会**静默零校验**地进契约,正是 G2 成因)+ **空段同源锁** `test_null_defaults_are_declared_in_schema`(`None` 容忍度必须在 schema 里声明)。**本次引入的安装态缺陷 1 项**:`teage_liu2/pyproject.toml` 无 package-data → `PROTOCOL/config/config.schema.json` 装包后不带,而 Phase 3 起 schema 在启动路径**无条件**加载(经 `check_declared_segments`)→ 装包部署"安装即坏"(Phase 1 前只在 `host_components` 出现时才加载,故未暴露)→ 补 `[tool.setuptools.package-data]`(`PROTOCOL/config/*.json` + `web/**`,后者是 `/ui` 静态前端,缺失时挂载被静默跳过)。**收尾 1 项**:`cases/24-host-component-reject.json` 与 `tests_core/test_server_host_components.py` 末行缺换行,已补。**判"不改"6 项**:`server` 段惰性声明(保留声明、仅补 description 注明"liu2 不消费本段")、`_DECLARED_SEGMENTS_EXCLUDED` 是刻意排除名单(改 spec 措辞而非去常量)、两处历史日期化文档与老系统资产(`config.yaml.example`/`DEPLOY.md`/前端 settings)、`data2/extensions` 依赖的 G3 锚点在 CI 会 skip(已登记的既存限制)、`core/llm.py` "llm.main_model 未设置"仍无 `CONFIG_*` 前缀(既存面④缺口,非本次引入)。**验证**:`audit_liu2.ps1` → 套件 **47/47**、pytest **283 passed**、**exit=0**;**变异验证 7 组必红且还原逐字节 sha256 一致**(①闭合段清单变空 ②schema 去闭合 ③校验点移回 `_config_path` 之后 ④reload 不校验 host_components ⑤去掉 `null` 声明 ⑥闭合段注入 `propertyNames` ⑦`ConfigFile` 新增未归类段);**实跑** `.\start_liu2.ps1` → `/health` 200 + `/ui` 200;`create_app(config_path="config.yaml")`(老系统配置)仍 fail-fast。**计数勘误**:pytest 基线 270 → 本轮 Phase 3 自身新增 8 个测试函数(`test_config.py` 6 + `test_server_routes.py` 2)、审查后增至 283;上一条 Phase 3 落地 bullet 中的 "278 passed / 新增 6 组" 是**审查前**快照(该文件本轮 diff 合计 8 个新函数,另含 Phase 1/1.5/4 的 2 个)。**文档同步**:`CODEBUDDY.md`、`PROTOCOL/README.md`(用例 47 + `inputs.host_segments` + `custom:config_host_segment_result`,套件规模 46→47)、`config.spec.md` §1/§3/§4、`config.schema.json`、`PENDING.md` P-11。**不改 VERSION**。

## 2026-09-17 M4 收口(WP-H) + P-10 条件② 套件锚定

> 来源:《2026-09-10-协议收口与遗留债-解决计划》§9 WP-H + PENDING P-10。**协议无新增元素,VERSION 仍 v1.0.0;套件 45 → 46、pytest 260 passed(收尾门禁全绿);P-10 用例经变异验证(退化必红 + 还原逐字节零残留)。**

- **WP-H 步骤 1 前端 fail-closed 实测**:playwright + Edge headless(脚本落 `d:/tmp`,测毕已删;经路由拦截把 `/ui/js/app.js` 映射本地文件,免起服务)。PASS:①注入 `window.DOMPurify=undefined` → `renderMarkdown` 输出纯转义 `&lt;img src=x onerror=alert(1)&gt;`;②注入 `window.marked=undefined` 同样纯转义;③对照正常路径 `onerror` 被 DOMPurify 剥除为 `<img src="x">`。P-16 修复(2026-09-10)首次有浏览器级实测证据。
- **WP-H 步骤 2 临时件清理**:`d:/tmp/liu2-7878.r2.{out,err}.log` 已删(7878 无监听进程,复核清零)。
- **WP-H 步骤 4 配置解耦立项评估**:产出 `docs/plans/2026-09-17-两系统配置解耦-立项评估.md`(根目录,跨系统性质)。要点:两系统仅 `llm` 段实际交集;liu2 `create_app(config_path=)` 已参数化、老系统 `TEAGE_CONFIG` 已参数化 → 方案 B(liu2 独立配置文件)代价低可回滚;4 项待裁决。**config 未动。**
- **P-10 条件② 套件锚定**:新增用例 `cases/46-events-usage-null.json`(provider 未上报 → `step_end.usage=null` 与 `done.usage=null` 且 normal 收尾);runner `_build_fake_llm` 增 `inputs.llm_no_usage` 键(默认脚本项不含 usage 键,`FakeLLMClient.resp.get("usage")` → None)。**变异验证**:`core/step.py` 强制 `usage or {}` 时用例必红(45/46),还原逐字节核对零残留(git diff step.py 为空)。PENDING P-10 入协议条件② 勾稽为已做。
- **验证**:套件 **45 → 46/46**;pytest **260 passed**;`audit_liu2.ps1` exit=0;lints 0。**M4 除步骤 3(用户裁决"保持现状")外全部收口。**

## 2026-09-11 综合评审修复(P0×2 + P1×6 + P2 群;计划+报告见根 docs/plans)

> 来源:根 `docs/plans/2026-09-11-liu2深度综合评审报告.md` + `2026-09-11-综合评审修复计划.md`(经 plan-verifier 审查后执行)。**协议无新增元素,VERSION 仍 v1.0.0;套件 45/45、pytest 258 passed、lints 0;全部 P0/P1 修复经变异验证(退化必红 + 还原逐字节)。**

- **P0-1 跨 session 并发串扰**:`pipeline` 形态实例改**每对话新建**(`_make_mode_instance()`;原单例 `final_snapshot` 被不同 session 并发互踩 → after/on_error 拿错会话快照 + extra 跨会话串写);`rebind_hooks` 不再同步实例 hooks(新对话用新链、进行中对话不受影响);`CORE.md §6"core 单快照无共享"前提修正`。锚定 `tests_core/test_pipeline_concurrency.py`(bare 形态 LLM 门控制造交错窗口)。
- **P0-2 套件假绿**:用例 14/15/10/13 弱断言(`{type:boolean}`/`{type:integer}`)全部改**精确字面量**;runner negotiation 逐场景 mismatch **硬校验进 errors**;P-1/P-2 转档证据修复(转档评审前置条件)。
- **P1-1**:入口 user 消息校验移到落盘**之前**(此前先 `flush=True` 落盘后校验,被拒超限输入已入历史库且下轮回流 LLM)。
- **P1-2**:app lifespan shutdown 重排 —— `registry.shutdown` 仅 cancel+teardown(存储传 None 跳过 close)→ `storage_writer.close()` **先排空**(连接仍开)→ 再关存储 → supervisor → L3/LLM;清理逻辑抽 `_teardown_all_resources` 闭包,**启动段失败同样兜底**(防孤儿进程/句柄)。
- **P1-3**:热重载回滚后对同名保留旧扩展**重新 `_register_extension`**(恢复 bus 身份 + L3 订阅;此前注销后无人恢复 → 宿主通道全死)。锚定 `test_reload_rollback_restores_bus_identity`。
- **P1-4**:`loop.py` 轮间增量收口失败 error 事件补 `code=HOOK_INVALID_ACTION`(与首轮孪生路径一致,消事件面分叉)。
- **P1-5**:`hooks.py` H-20 schema 校验包 try(TypeError/ValueError/KeyError/RecursionError)→ fail-open 放行原输入 + 记码(畸形 schema 不再击穿对话)。
- **P1-6**:`/reload` 成功路径补 `configure_limits(core.max_* 三键)`(此前 reload 后资源上限静默失效)。
- **P1-7**:runner 守卫上移至 `_run_protocol_case`(22 条协议类用例纳入);storage/host_port ops **合并 op 白名单**(未实现 op 显式失败);`read` op 接线(用例 11 补 `read_roundtrip_ok` 承重往返);`cases/23/40/43` 的 `inputs.extension_name` 真实消费。
- **T-8 套件锚定**:新增用例 `45-resource-limits.json`(runner `core_overrides.resource_limits` 接线补齐)—— T-8 资源上限此前套件 0 用例。
- **P2 群**:`loop.py` 工具 duration 改本工具计时;`storage.py` 补 `Union` import + 增公开 `validate_kind()`(proxy 弃用 `_validate_kind`);`hooks.py` 终态钩子日志改常量插值;`remote_adapter` 非法 decision 值降级 allow;`tasks.cancel_task` 先改状态再出表;`storage_writer` 重试成功补 `resolve`(埋雷);`history.py` 三个读方法统一锁纪律;`stdio.py` spawn 显式 `limit=FRAME_MAX_BYTES+64KiB`(帧长检查恢复可达);`pipeline` L3 旁路改 deepcopy(嵌套引用同源污染);`types.py readonly_view` docstring 措辞收敛;routes `/chat` error 路径显式消费至流结束;删 `test_server_host_components.py` 空断言。
- **契约文本**:lifecycle.spec §2 Extension 字段名勘误(`lang`,manifest 仍 `language`)+ build_injections 隐式参与注;events.spec step 1-based 契约化;PENDING P-2 口径统一(downgrade);errors.spec §2 措辞修正(13/18 码)+ §3 H-5 拦截折衷登记;transport T-2 / config §1 / types §7 措辞;`CORE.md` §2 after 签名(AfterResponse)+ §6 shutdown 编排同步;`INTERFACES.md` 45/45。

## 2026-09-11 交叉评审剩余问题一并解决(P1-4/P1-5/P1-6/P1-8/P1-9 + P2 + 装载边界)

> 来源:根 `docs/plans/2026-09-11-core与协议-交叉评审与修正方案.md` §9/§10。**协议无新增元素,VERSION 仍 v1.0.0;PENDING P-9③ 勾稽补齐(机械校验已加)。**

- **P1-4 资源上限接线**:`core/snapshot.py` 增 `configure_limits()` / `active_limits()`(模块级 `_ACTIVE_LIMITS`,与 `hooks.hook_timeout` 注入同形;键未知/值非法 → 保持协议默认);`core/pipeline.py` 的 `ChatPipeline(resource_limits=...)` 构造即生效;`server/app.py` 把 `core.max_snapshot_bytes / max_message_bytes / max_messages_per_conversation` 接入。此前三键"解析校验但从不消费"。
- **P1-5 H-20 落地**:新增 `core/schema_check.py`(极简 JSON Schema 子集:`type/required/properties/items/enum/const/additionalProperties/min·max`,未知关键字宽容 —— 与"未知协议元素透传不崩溃"一致,**零新依赖**);`hooks.pre_tool_call_all` 在 modify 后重过工具 `input_schema`,非法 → 视同策略拒绝(`TOOL_MODIFY_INVALID` 记码 + `ToolDecision(reject, reason=...)`)。
- **P1-6 H-5 轮中短路**:`hooks.post_tool_call_all` / `after_step_all` 应用 action 后 `if cur.stop: break`;`core/loop.py` 在 `post_tool_call` 置 stop 后不再执行批次剩余工具,并补发 `tool_use` + `tool_result`(is_error, `TOOL_REJECTED_BY_POLICY`,"对话已被拦截,工具未执行")保持 H-19 事件流完整与消息配对(防下轮 LLM 400)。
- **P1-8 套件 inputs 假绿**:runner 新增 `_check_unknown_extension_inputs`(`<ext>_<suffix>` 白名单,后缀未识别即失败);删 case 05 `forged_revision`、case 14 `reload_cycles`;case 09 改用被真正消费的 `round_injector_inject_round`;case 10 `limits` 落实(与 `FRAME_MAX_BYTES`/`JSON_MAX_DEPTH` 比对 + `check_frame_limits` 真实驱动);case 12 `concurrency_max: 4`(原 `range:[1,64]` 不承重);case 18/25 文件名对齐 id;删死代码 `_find_behavior_inputs`。
- **P1-9 文档一致性(14 处)**:`INTERFACES.md` 两处(含 17/17→44/44)、`docs/SUBSYSTEM-SPI.md` 的 `branches/audit.py`、`PENDING.md` P-2 矛盾结论、`config.spec.md` 资源上限键登记、`PROTOCOL/README.md`(v1.15/完成时)、`CORE.md` 三处(装配点例外/铁律 3/构造参数)、`errors.spec.md`(7 常量)、`hooks.spec.md`(不绑定 API)、`lifecycle.spec.md`(`language`)、`types.spec.md` T-4。
- **P2 群**:`actions.validate_action` 校验 SetExtra 值可 JSON 序列化;`pipeline.chat_stream` 入口 user 消息走 `snapshot_with_user_message`(死代码转活,超限 → `error` HOOK_INVALID_ACTION);`storage.SQLiteStorageProvider.query` 改 `json_extract` WHERE(filters 先于 limit,P-4 ⑧);`types.py` 终止原因常量改 re-export `errors.py`(消双源);`ToolDecision.reason` 补齐(actions + remote_adapter + loop 文案 + 套件 case 16/32);observe 扩展 `ToolDecision` 忽略;`tasks.cancel_all` 改 async + `gather` 等待 + `_on_task_done` 异常回收;`step.py` 总超时只包裹 `await __anext__()`(yield 移出 timeout 上下文)。
- **装载边界**:`extension_loader.check_import_boundary`(AST 扫描,禁 `teage_liu2.server` / `teage_liu2.branches` / `teage_liu`)在装载前执行 —— lifecycle §3.3 由"文档纪律"变为"机制保障"。
- **新增测试**:`tests_core/test_cross_review_fixes.py`(16 条);`test_tasks.py` 适配 async `cancel_all`(并断言"返回即取消完成");`test_events_contract.py` 加 P-9③ 机械校验。
- **验证**:套件 **44/44**;pytest **232 → 250 passed**;`audit_liu2.ps1` **exit=0**;lints 0;**变异验证 9/9 全承重**(退化必红 + 还原逐字节)。**未做 git 写操作,无临时文件残留。**

## 2026-09-11 core 与协议 交叉评审修正(P0-1 + P1-1/P1-2/P1-3 + P1-7)

> 来源:根 `docs/plans/2026-09-11-core与协议-交叉评审与修正方案.md`(4 路只读子智能体并行评审 + 主 agent 行号级复核)。裁定口径:**以"core 与协议的正确"为唯一标准,哪边错就改哪边**。**协议新增 PENDING P-9/P-10,VERSION 仍 v1.0.0。**

- **P0-1(正确性)轮中 SetStop 拦截丢失**:`core/loop.py` 的 `after_step` 应用 action 后,原代码直接进入"自然结束"分支 —— `cur.stop` 检查只在工具批次之后,故 `after_step` 返回 SetStop 且本轮 end_turn 时拦截被**静默丢弃**为 `done(normal,is_complete=true)`(违反 hooks H-5)。已补 guard → `done(intercepted)`;用例 44 承重锚定。
- **P1-1 `tool_result` 越界键 → 协议定义的 `code`**:原发 done 专用枚举名 `termination_reason`(ToolResultEvent `additionalProperties:false`,越界)。改 errors 域可选 `code`:`core/loop.py` reject → `TOOL_REJECTED_BY_POLICY`、执行异常 → `TOOL_EXEC_FAILED`;两码由"仅日志面"升级为"事件面 + 日志面"双面(`errors.spec.md` §3/§5① 同步);用例 32 断言改 `code`。登记 PENDING **P-9**。
- **P1-2 `step_start.step` 恒 0 → 真实序号**:`StepExecutor.execute` 增 `step` 形参(默认 1),`core/loop.py` 传 `cur.round`、`core/modes.py`(BareMode)传 1;schema `minimum:1` 不动(协议本正确、实现违约)。锚定 = 用例 09 断言 `step=1` + `tests_core/test_events_contract.py` 的两轮序列 `[1,2]`。
- **P1-3 usage 口径分裂 → 协议统一**:`StepEndEvent.usage` / `StepSummary.usage` 放宽为 `["object","null"]`,与 `DoneEvent` / `AfterResponse` 一致(provider 未上报 = null,不以 `{}` 伪装;否则 `DoneEvent.usage=null` 成死分支)。登记 PENDING **P-10**;core 零改动。
- **P1-7 热重载 cancel_all 契约收口**:`TaskRegistry.cancel_all` 为**全局**且对扩展侧登记任务只 `_registered.clear()`(宿主物理上无法终止扩展进程内任务)→ L-4/T-4 原承诺"热重载宿主兜底取消"**既会误杀新链刚注册的任务、又语义不成立**(且 `registry.py` 与 `supervisor.py` 注释互相甩锅)。按 liu2 理念(扩展自持 / 主干最小 / 契约必须可兑现)定案 = **改协议文本**:取消执行责任在扩展 teardown,`cancel_all` 兜底仅限宿主整体 shutdown;`core/tasks.py` 两处 docstring + `registry.rebuild` 注释同步;`INTERFACES.md:302` 原已正确、未动。
- **评估中被否决的方案**(记录以防重复提案):P1-1 只删键不补 `code`(能力缺口保留)、P1-1 新字段名 `rejected: bool`(无法表达原因)、P1-2 改 schema 允许 0(协议迁就缺陷)、P1-3 改代码发 `{}`(语义失真)、P1-7 给 `TaskRegistry` 加 owner 粒度(宿主背任务生命周期,与"主干最小/扩展自持"相悖)。
- **新增测试**:`tests_core/test_events_contract.py`(6 条:step 透传/两轮序列/usage null 合法并过 schema/reject code/exec-fail code/成功不带 code;含 `jsonschema` 校验 events schema)。
- **验证**:套件 **43 → 44/44**;pytest **226 → 232 passed**;`audit_liu2.ps1` **exit=0**;lints 0;**变异验证 6 项全承重**(P0-1 guard / P1-1 code ×2 / P1-2 step ×2 / P1-3 schema 反向,退化必红 + 还原逐字节完好)。**未做 git 写操作,无临时文件残留。**

## 2026-09-11 T-3 边界只读视图 + WP-E·E1/E4 routes 锚定(M3 收尾)

> 来源:根 `docs/plans/2026-09-11-三项待裁决分析.md` §6;用户裁决「T-3 采用边界只读,防止误用」「WP 按推荐」。**PROTOCOL v1.0.0 冻结面未动,不升 VERSION。**

- **T-3 / H-17 边界只读视图(S2)**:`core/types.py` 新增 `readonly_view(snapshot)`(`messages`/`tools`/`history` → `tuple`、`extra` → `MappingProxyType`;元素共享引用、不 deepcopy,T-5 不受影响);`core/hooks.py` 对**全部面向扩展的 9 处钩子调用**统一改传只读视图(`build_injections`/`inject_round`/`before`/`pre_tool_call`/`on_tool_call`/`post_tool_call`/`after_step`/`after`/`on_error`),core 自身仍用原快照。`CORE-缺口记录.md` 的 H-17 由"缺口"转"已修复"。
- **套件与单测**:`cases/31-hooks-readonly.json` 由"如实锚定篡改生效"收紧为 `messages_roles=[user]`(description 同步);`tests_core/test_snapshot_identity.py` 新增 `test_readonly_view_shares_element_references`(容器类型 + 元素同一)与 `test_hook_boundary_snapshot_is_readonly`(四类原地篡改全被拒 + core 状态不变)。
- **WP-E·E1 routes 生产修复锚定**:新增 `tests_core/test_server_routes.py`(`server/` 首次有测试锚定)—— `/chat` 拦截取 `done.response`、`/chat/stream` 不提前关闭生成器(终态 `after` 仍触发)、`before_id` 游标分页、`/reload` 返回分支名字符串。夹具:`app_mod.LLMClient` 换假客户端 + 临时 `storage.sqlite_path` + 临时 `extensions_root`/`config.yaml`。
- **WP-E·E4 L-11 路由层真并发**:`asyncio.gather` 两个 `/chat`(同 session),第一个在假 `chat_stream` 内持锁阻塞,`gate.set()` 后断言严格顺序 —— 删 `async with lock` 即红。测试经 `starlette.requests.Request` 直调路由协程(确定性,无 TestClient 线程)。
- **变异验证(全承重)**:T-3 去 `before` 边界包装 → 用例 31 红(`actual=['user','assistant']`)+ 单测 1 红;E1/E4 批量退化 5 处 → 5 用例全红;恢复后 `git diff --stat teage_liu2/server/routes.py` 为空(逐字节还原)。
- **验证**:套件 **43/43**;pytest **221 → 226 passed**;`audit_liu2.ps1` **exit=0**;lints 0。**未做 git 写操作。**

## 2026-09-11 三项待裁决落地(CI 依赖放行 + L-11 边界修复 + LLM_CANCELED 事件面锚定)

> 来源:根 `docs/plans/2026-09-11-三项待裁决分析.md`。**PROTOCOL v1.0.0 冻结面未动,不升 VERSION。** 跨系统摘要见根 `CHANGELOG-开发日志.md` 同日条目。

- **CI 依赖放行(WP-E 步骤 0)**:`.github/workflows/liu2-audit.yml` 装机行追加 `fastapi httpx`(用户 2026-09-11 批准)。原方案 C"不经 FastAPI"不成立 —— `server/routes.py:9-10`、`server/app.py:16-18` 均**模块级** import fastapi。头注释补"覆盖被测试模块模块级 import 全集"理由(与 `python-dotenv` 同款)。
- **L-11 会话锁边界漏洞修复**:`server/session_locks.py` 的 `get()` 超限淘汰跳过**本次刚返回的锁**(`lid == session_id`);此前会淘汰刚拿到的锁 → 同 session 两次 `get` 拿到两把不同锁、互斥失效,且文档承诺的"临时超限"分支不可达。`tests_core/test_session_locks.py` 更新 `test_held_lock_not_evicted`(改为断言临时超限)并新增 `test_just_returned_lock_not_evicted`。
- **LLM_CANCELED 归位事件面**:`_run_error_code_matrix` 改为接收 `custom_events` 并汇总各场景 `error` 事件 `code`(经 `custom:error_matrix` 断言)—— 修复此前该用例事件面通道为空、LLM_* 仅靠日志面侥幸通过的隐患;`cases/20-error-codes.json` 增 `LLM_CANCELED` 场景 + `must_include`(info 级日志不入 ②,只走事件面①)。套件错误码并集 **12 → 13**;PENDING P-8②、`PROTOCOL/README.md` 同步。
- **条款覆盖登记**:根协议收口计划 §0/§13 回写 T-3/T-5/L-5/L-6/L-10/L-11 的锚定文件与限定语(T-3 仅内核侧、L-11 仅机制、L-10 仅 done 路径)。
- **变异验证**:事件面退化 → 用例 20 红;去"刚返回锁"防线 → 会话锁 2 条红。**验证**:套件 **43/43**、pytest **219 passed**(218 + 1)、`audit_liu2.ps1` **exit=0**、lints 0。

## 2026-09-11 「部分落实」项统一落地(协议债收口 + 测试锚定,套件 39 → 43)

> 来源:根 `docs/plans/2026-09-11-部分落实项统一落地审查.md`(审查结论 + 逐项落地)。**PROTOCOL v1.0.0 冻结面未动,不升 VERSION。**

- **套件新增 4 例 + 扩 1 例**:`host-component-disabled-40`(未声明不启用＝已装未启用统计,`wire_extensions` 返回 disabled)/ `host-component-default-41`(缺省省略 host_components＝全插槽用 core 默认实现)/ `stdio-proxy-dual-channel-42`(P-7 双档:background 合帧经 `log_messages`、flush 直发保 FIFO 序、`metrics_snapshot` 区分两档)/ `host-component-uninstalled-43`(声明未装＝启动失败);`error-codes-20` 扩 `llm_error`(LLM_TIMEOUT/LLM_STREAM_FAILED/LLM_API_ERROR)与 `storage_write_fail` 两类场景。
- **runner 能力**:`_run_stdio_case` 的 `custom:stdio_result` 增 `buffered_frames` / `flush_direct_calls`(取 `metrics_snapshot`);`_run_host_component_case` 增 `mode` = `disabled_probe` / `default_path` / `uninstalled_probe`;错误码矩阵增 `_RaisingLLM`(异常工厂)与 `_FaultHistoryStore`。
- **错误码 18/18 全码锚定**:套件并集 8 → 12 码;新单测 `test_step_error_codes.py` / `test_storage_error_codes.py` / `test_error_codes_schema.py`(机械比对) + `test_config.py` 补 CONFIG_MISSING_KEY。PENDING P-8 条件②③勾稽为已满足。
- **WP-F/WP-E(无 fastapi 部分)测试锚定**:`test_snapshot_identity.py`(T-3/T-5)、`test_assembler.py`、`test_modes.py`、`test_supervisor.py`(L-5/L-6)、`test_remote_adapter.py`、`test_server_host_components.py`(P-5)、`test_server_stdio_proxy.py`(E3 + 双档)、`test_session_locks.py`(L-11)、`test_session_extra_continuity.py`(L-10) → pytest 165 → **218 passed**。
- **协议登记**:PENDING P-1/P-5/P-7 条件③ → 已满足(勘误 E-1..E-3);T-8 移出未覆盖(未覆盖 20 → 6 条)。
- **变异验证**:5 处退化 → 单测 7 红 + 套件 3/3 红;恢复后全绿。**验证**:套件 **43/43**、pytest **218 passed**、`audit_liu2.ps1` **exit=0**。

## 2026-09-11 M2 行为套件场景与用例扩展(WP-A + WP-B,用例 21 → 39)

> 来源:根 `docs/plans/2026-09-10-协议收口与遗留债-解决计划.md` 的 M2;执行编排与事实修正见根 `docs/plans/2026-09-11-M2行为套件场景能力-执行计划.md`;跨系统摘要见根 `CHANGELOG-开发日志.md` 同日条目。**PROTOCOL v1.0.0 冻结面未动,不升 VERSION。**

**WP-A 场景与断言能力(A1–A5)**
- 新增 `PROTOCOL/behavior-suite/tools/reference_storage_backend.py`(P-4 参考后端:**零 `teage_liu2.core` 依赖**,标准库 sqlite3)+ `tests_core/test_reference_backend.py`(6 条,含子进程真实往返)。执行中发现并修复**参考后端自身缺陷**:`_insert_message` 内层 `with self.conn:` 会提交外层事务(sqlite3 嵌套 with 语义)→ `log_messages` 原子性失效。
- `runner.py` 新增协议层分支:`stdio-proxy`/`storage-batch-atomic`/`types-doc-opaque`(真实子进程)、`host-component`(插槽装载 + 快速失败矩阵)、`events-`(L3 旁路驱动);`_assert_final` 增 `llm` 形参(既有 2 处调用点零改动);`_run_pipeline_case` 支持 `storage_writer`/`session_store`/`core_overrides` 并暴露 `custom:pipeline_l3_result`;`_build_behaviors` 接线 `mutate_snapshot`。
- 用例 22(stdio 全链)、23/24(host-component)、25(llm_assert)、26(批量原子性)。

**WP-B 13 条黄金用例(27–39)**
- hooks 域 6(H-3/H-8/H-9/H-16/H-17/H-19)、events 域 4(E-2/E-4/E-8/E-9)、types 域 2(T-1/T-7)、config 域 1(C-2);**逐条变异验证**(退化实现 → 变红 → 恢复)。

**执行中发现的 core 缺口(3 修 + 2 登记)**
- 修复:`core/hooks.py` 的 `_log_observe_action` 补 `HOOK_TERMINAL_ACTION_IGNORED` 码前缀(E-9 契约漂移);`core/injection.py` 的 `assemble_injections` 预算改 `{**DEFAULT_LAYER_BUDGETS, **(budgets or {})}`(部分层覆盖此前 KeyError);参考后端嵌套事务(见上)。
- 登记未修:H-17"只读视图"无内核级强制(容器可变,用例 31 如实锚定);H-9 语义级校验缺可构造负例(用例 29 改正向不变量断言)。详见 `CORE-缺口记录.md`。

**协议条件勾稽**:PENDING 条件③ —— P-2/P-4/P-6 **已满足**,P-1/P-5/P-7 **部分满足**(缺口均属 M3/WP-E 的 server 层观测能力)。

**验证**:行为套件 39/39;pytest 165 passed(新增 6 条);`scripts/audit_liu2.ps1` exit=0。

## 2026-09-11 协议收口 M1(WP-C + WP-D + WP-G + register_task 修复)

> 来源:根 `docs/plans/2026-09-10-协议收口与遗留债-解决计划.md` 的里程碑 M1;跨系统摘要见根 `CHANGELOG-开发日志.md` 同日条目。**PROTOCOL v1.0.0 冻结面未动,不升 VERSION。**

**WP-C 三码发射点(P-8 条件④闭合)**
- `core/hooks.py`:`pre_tool_call` 返回 modify 但 `input=None` 时补 `elif` 分支记 `TOOL_MODIFY_INVALID` 并降级 allow(此前静默忽略、无 else 分支);`dispatch_tool_call` 异常日志补 `TOOL_EXEC_FAILED` 前缀。
- `core/loop.py`:reject 路径 warning 补 `TOOL_REJECTED_BY_POLICY` 前缀。
- 契约回流:`PROTOCOL/errors/errors.spec.md` §5 三者从"⑤ 待落地"移入 ② 日志面;`PENDING.md` P-8 条件④ → 已满足;套件用例 16 增日志面断言。
- 锚定:`tests_core/test_tool_error_codes.py`(2 条,变异验证通过)+ 套件用例 16(变异验证:去前缀变红)。

**WP-D 协议域归档与契约同步**
- `PROTOCOL/lifecycle/lifecycle.spec.md` 新增 §3「扩展 manifest 声明规范」(字段集表 + kind 分支约束 + 装载语义);`lifecycle.schema.json` 新增 `ExtensionManifest`(含 language/kind 条件必填 if-then)。
- `PROTOCOL/config/config.spec.md` 新增 §3「宿主组件插槽」;`config.schema.json` 新增 `HostComponent`,并补录此前 schema 遗漏的 `extensions_root`。
- `PENDING.md` P-1:登记三个资源上限键 + manifest 规范条件④勾稽为已做。
- `core/storage.py`:`MessageStore.get_session_messages` 补 `before_id` 形参(契约一致性,零行为变化)。

**WP-G 文档一致性 + 同源小修**
- `types` / `transport` 两域 `T-*` 编号各加域内限定;`PROTOCOL/README.md` 增「条款编号约定」(跨域须带域名前缀,编号不变)与 runner 依赖边界说明。
- 用例 17 description 与实现对齐(删"防重入/并发上限"表述)。
- `core/transport.py`:`InProcessHostPort.register_task/cancel_task` 改 async + await + 错误上抛(修复"未 await 丢弃协程、task_* 永不执行"缺陷);`docs/INTERFACES.md` 示例改 await、cancel_all 漂移表述修正。
- 锚定:`tests_core/test_host_port_tasks.py`(3 条)。

**验证**:行为套件 21/21;pytest 156 passed(新增 5 条);`scripts/audit_liu2.ps1` exit=0。

## 2026-09-10 core 性能与健壮性完善(7 任务,零契约变更)

> 来源:`docs/plans/2026-09-10-liu2-core性能审查报告.md`(本机实测);执行计划:`docs/plans/2026-09-10-core性能与健壮性完善-执行计划.md`。**PROTOCOL/ v1.0.0 冻结面一字未动**,不改配置键、不新增依赖。

**落盘档位(D-1 裁决 = 保持 FULL)**
- `core/history.py` / `core/storage.py` 连接显式化 `PRAGMA busy_timeout=5000`(与 Rust 后端对齐);`synchronous` 保持 SQLite 默认 **FULL**(每次 commit 逐次 fsync)。用户裁决**不启用** WAL 下的 `NORMAL`(可省 fsync、实测单条写 1.23→0.10 ms,代价是断电/OS 崩溃可能丢最近若干已提交事务)。
- `core/storage_writer.py` docstring 由"NORMAL 下 WAL 已写,进程崩溃不丢"改为如实描述(当前为逐次 fsync 的 FULL 档)。PENDING P-4 增条款⑩、P-7 现状行注明 flush 档语义同上。

**协议边界(transport + stdio)**
- `json_depth` 递归 → **迭代(显式栈)**:深嵌套不再抛 `RecursionError`(旧版数百层即崩),实测快 ≈2×;拆出 `check_frame_depth`(纯深度校验)。
- `TransportFrame.decode(line, raw_bytes=None)`:入站帧体积一律按**线上字节**计(stdio 读循环已持有 `len(line_bytes)`),去掉每帧一次的整帧 `json.dumps`;补 `RecursionError → ValueError` 兜底。
- `stdio._read_loop`:decode + dispatch 全包进逐帧 `try/except`(仅 `CancelledError` 放行)—— 修复 `type`/`encoding` 字段为不可哈希 JSON 类型时 `TypeError` 逃出 `except ValueError` 使**整条读循环终止(通道死亡)**的真实缺陷。

**快照体积预算(types + snapshot)**
- 新增 `_SizeCache`(记账缓存:`messages_ref` 同一性 + `msg_bytes` + `base_bytes`)与 `snapshot_bytes()`/`_measure()`;`apply_action_batch` 增量维护,`_check_snapshot_budget` 稳态**零全量序列化**。
- 精确性锚:逐点对拍 `len(json.dumps(to_dict()))`(含 **revision 9→10 进位差分**、with_round 位数变化、截尾路径);不可序列化时保持"体积恒视为 0,永不拒绝"的历史语义。

**超时原语(llm + hooks)**
- `asyncio.wait_for` → `asyncio.timeout`(逐 chunk / 逐钩子),语义等价性逐条验证(TimeoutError 别名、on_timeout 触发、外层嵌套、外部取消不误转)。

**L3 批处理(event_stream)**
- 恢复"64 条先到**立即**冲刷"(旧实现同 tick 内 500 条 0 次触发);`_timer_task` 创建时登记 + `_start_timer()`;在途冲刷不被取消;缓冲 `list.pop(0)` → `deque.popleft()`;`close` 只取消定时窗口 + 兜底投递。

**实测收益**(仓库外脚本,用完即删)

| 项 | 修前 | 修后 |
|---|---|---|
| `apply_action_batch`(605 KiB 快照) | 3.156 ms | **0.016 ms**(≈194×) |
| `_with_activity_timeout`(8000 chunk) | 77.5 ms | **21.2 ms** |
| 单钩子调用(`HookChain._call`) | 9.5 µs | **2.5 µs** |

**验证**:`pytest teage_liu2/tests_core` **150 passed**(新增 26 条:history 1 / storage 1 / transport 7 / stdio 4 / 快照 2 / llm 3 / hooks 1 / L3 7);行为套件 **21/21**;关键修复均做**变异验证**(还原旧语义 → 对应用例变红)。

**执行后严格审计(plan-auditor,114 tool uses)与修复**:结论"有条件通过(无契约/正确性阻断项)",9 项发现**全部处理** ——
- **[主要·已修]** `json_depth` 迭代版在"宽而浅"帧上比递归慢且内存放大(70 万容器 378 ms / 42.9 MB vs 递归 61 ms / 0 MB;"快 ≈2×"只对消息型形状成立)⇒ 由"逐容器压栈"改**迭代器栈 DFS** + 空容器不入栈 + `limit` 早退;复测宽浅 700k **50 ms**(快于递归 77 ms)、内存 **0 MB**、深 900 层 0.21 ms、`check_frame_depth`(5000 层) 0.012 ms。
- **[次要·已修]** `L3BatchSink.close()` 不 await 在途冲刷 ⇒ shutdown 期正在投递的那批事件**静默丢失**(审计探针实测 `got == []`)⇒ 改为 await(仅吞"自己刚取消的定时窗口"的 CancelledError),补红测试 `test_close_awaits_inflight_flush`。
- **[覆盖缺口·已补]** `_flush` 的 `while` 续送语义无用例(审计证明可达:1089 次触发中 817 次命中)⇒ 补 `test_inflight_flush_continues_draining_new_events`(变异 `while`→`if` 变红)。
- **[次要·已修]** `check_frame_limits` 在 `decode` 改口径后**零调用方 + 零覆盖** ⇒ 补 `test_check_frame_limits_semantics`。
- **[nit·已修]** stdio 逐帧异常日志限速(前 3 次 + 每第 100 次);`_SizeCache` 失效契约补 `history`/元素原地改写;`asyncio.timeout` 等价性声明加 `timeout<=0` 限定;`test_read_loop_survives_dispatch_error` 文案更正(不承重,`_dispatch` 自带隔离);审查报告 §6 与计划偏差#3 口径回写。
- **审计独立复测(承重确认)**:快照记账 16 000 步随机差分模糊 **0 处不一致**(含 revision 进位/截尾/回滚两分支);`apply_action_batch` 稳态 **0.0136 ms**(旧 3.2256 ms,≈237×)且冷路径无回归;L3 1000 并发投递守恒(delivered/unique 1000、dropped 0、pending 0);契约面(PROTOCOL 冻结面 / 11 键 / 无新依赖)零违规。

## 2026-09-10 审查问题修复执行:套件可信性 + 错误码锚定 + 契约回流

> 来源:同日两份审查（现状审查报告 / 深度审计-协议实现漂移与覆盖缺口，F-1~F-8）；执行计划:项目根 `docs/plans/2026-09-10-审查问题修复-执行计划.md`（Phase 0~6）。

**行为套件能力（F-1，根除"假绿"）**
- 断言守卫 `_check_supported`：用例出现 runner 未实现的断言键 → **显式失败**（此前 `final.persisted` / `invocations[].isolation` 被静默忽略，`suite.schema.json` 定义了却没实现）。
- 落盘可观测：`_EphemeralStorage` 由空操作改为**按调用序记录**（`direct` = flush 档 / `buffered` = background 档），新增 `persisted` 断言与 `text_blob`（供"注入永不落盘"负断言）。
- 隔离断言：`ScriptedBranch` 支持 `<name>_raise_on` 脚本化抛错；`isolation.error` 断言该扩展被跳过、对话不中断。
- 错误码断言：runner 新增 `_LogCapture`（logging handler 提取 `CODE:` 前缀）与 `expected.error_codes`（must_include / must_exclude）。

**新用例 4 条（17 → 21）**
- `storage-persist-18`：storage **S-1 落盘时机**（user 前置 direct → assistant buffered → tool_results 聚合 buffered；注入内容不落盘）——此前**零覆盖**且是历史事故集中地。
- `hook-isolation-19`：hooks **H-6** 钩子异常隔离（HOOK_EXCEPTION）。
- `error-codes-20`：错误码日志面矩阵（HOOK_EXCEPTION / HOOK_TERMINAL_ACTION_IGNORED / TOOL_NO_EXECUTOR / LOOP_MAX_REACHED）。
- `config-domain-21`：**config 域**（C-1/C-2 + 未知键/非法值拒绝，CONFIG_* 两码）——此前该域**零用例**。

**core 发射点（F-3 三面模型）**
- 事件面：`step.py` 四个 LLM 错误事件补 `code`；`pipeline.py` 消息序列非法 → HOOK_INVALID_ACTION。
- 日志面：`hooks`（HOOK_TIMEOUT/HOOK_EXCEPTION）、`loop`（LOOP_MAX_REACHED/TOOL_NO_EXECUTOR）、`pipeline`/`storage_writer`/`transport`（STORAGE_WRITE_FAILED/STORAGE_READ_FAILED）、`config`（CONFIG_* 三码 **由死码接活**）统一 `CODE: message` 前缀。
- 契约：`errors.spec` 新增 §5「错误码可观测面」三面模型 + §5.1 transport 10 码子命名空间；`errors.schema.json` 新增 `TransportErrorCode`；`transport.spec` 登记错误响应面；PENDING 新增 **P-8**。

**实现修正（F-5/F-6/F-7）**
- `snapshot.apply_action_batch`：快照体积超限由"仅记日志"改为**拒绝并回滚该批次**，与消息条数/单条体积两项语义对齐（§types T-8）。
- 补日志：`llm.py` 关流失败、`stdio.close` 三处清理、`routes.health` 指标降级 → debug 级（严格满足 R-1「捕获点必须带日志」）。
- 幽灵外键（F-7）：liu2 `core/history.py` 与 `storage-rust` 删 FK 声明（老系统保持不动，用户定案"liu2 不受老系统影响"），PENDING P-4 增语义条款⑨。

**契约回流（F-4/F-8）**
- `config.spec.md` §2 补 4 键（3 个资源上限 + `extensions_root`）；`CORE.md` §7 补 `extensions_root`、§10 补 `HistoryStore.get_session_messages(before_id)` 签名；`SPI` §7 同步；PENDING P-4 补 `before_id` 登记。
- `assembler.py` 去掉"现状缺陷标注"陈旧措辞；`CORE-缺口记录.md` 体例校正；`CODEBUDDY.md`/`PROTOCOL/README.md` 声明测试套件特例与套件交付物边界。

**验证**：行为套件 **21/21**；`pytest teage_liu2/tests_core teage_liu2/tests_branches` 全绿（本轮新增测试 21 条：runner 契约 9 + 错误码日志 2 + 错误码事件/配置 3 + 快照上限 5 + 双档落盘 2）；Rust `cargo build --release` 零 warning；一键审计 `scripts/audit_liu2.ps1` exit=0；e2e 通过（对话落盘 → 重启 → 历史重建 → 上下文续问）。**改动仍在工作树未提交。**

**与计划文本的偏差（登记）**：①用例 18 实际为 4 条落盘（计划写 3 条：user/assistant/user），因 step_end 每轮均落 assistant；②用例 19 删去计划的 `order: 2`（抛错扩展不产生 invocation 记录故无该序号）；③计划 T2.3/T2.4 要求的两处测试分别落在 `test_error_codes_events.py`（事件面 + CONFIG_*）与既有 `test_config.py` 覆盖范围，未单独改 `test_config.py`；④计划 T5.1 的"整批回滚"方向经评审证伪，改为"逐条拒绝 append + 保留 set 类"（见下）。

### 2026-09-10 评审后修复轮（同日，plan-auditor 执行后综合评审驱动）

> 评审报告：`docs/plans/2026-09-10-执行后综合评审报告.md`（结论"不建议提交"，4 项必须修复 + 12 项次要 + 4 nit）。

- **[回归修复] 快照体积上限语义（FIX-1）**：`snapshot.apply_action_batch` 由"整批回滚"改为 —— **体积上限只拒绝本批次的 append 类 action，set 类（SetStop/SetExtra/SetSystem/SetTools/ModifyToolSchema）一律保留**（其中 SetStop 承载安全拦截语义，且 set 类不随轮次累积增长）。此前整批回滚的两处后果均已消除：①同批 `[AppendMessage 超限, SetStop]` 把拦截一起丢掉；②入参快照已超限时该会话此后**所有**钩子批次被永久拒绝（拦截与 SetExtra 静默失效）。新增 2 条回归测试（`test_snapshot_limits.py` 共 5 条），并以变异测试证明非空（退化为整批回滚 → 测试变红，还原 → 恢复绿）。生产路径另经 e2e 复验：guardrails 拦截 `termination_reason=intercepted` 正常。
- **[CI 修复] 最小依赖补 `python-dotenv`（FIX-2）**：`core/config.py:25` 为模块级硬依赖，`tests_core/test_config.py` 与套件用例 21 都会 import 它 —— 缺则 pytest 与套件双双失败（用例 21 的 import 在 try 外，会中断整个套件）。此前"仅 yaml"的核验结论已更正。
- **[契约自洽] `suite.schema.json` 的 `Expected` 增补 `error_codes`（FIX-3）**：此前用例 19/20/21 携带该键却违反 schema（`additionalProperties: false`）→ 语言无关交付物不自洽。
- **[断言补锚] 事件面错误码（FIX-4）**：新增 `tests_core/test_error_codes_events.py` —— LLM 失败事件 `code == LLM_API_ERROR`、非法 action 的 `HOOK_INVALID_ACTION` 日志前缀、`CONFIG_*` 抛错码。
- **防复发（P-4/P-8/P-12/nit-3）**：runner 新增①**空壳用例守卫**（`expected` 无任何有效断言 → 失败）②**用例结构 schema 机检**（`jsonschema` 校验 `suite.schema.json#/definitions/TestCase`，依赖本就具备）③**终止事件恰好一次**断言 ④`_extract_codes` 加词边界；用例 06/11/12/17 的 `{"type":"boolean"}` 改为精确布尔（此前禁掉整个 transport 拒绝面仍 PASS）。
- **用例收紧（P-10/P-11）**：用例 18 去掉 `channels` 断言（flush/background 档位属 P-7 experimental 机制，不该进黄金用例；改由 `test_persist_channels.py` 做实现级覆盖），并新增 `has_blocks` 断言（补 D1 `content_blocks` 零覆盖空洞——顺带发现 `_persisted_view` 根本没暴露该字段）；用例 21 增 `observed_codes` 精确断言。
- **契约措辞更正（P-6/P-7/P-15）**：`errors.spec §5` 把 `TOOL_EXEC_FAILED` / `TOOL_REJECTED_BY_POLICY` / `TOOL_MODIFY_INVALID` 移入"⑤ 待落地"、新增"④ 异常/启动失败面（CONFIG_*）"、逐面写明当前锚定载体；`§5.1` 尾注降级为"锚定拒绝结果、未逐字断言码字符串"；`config.spec` 补录说明的域归属更正；T5.2 评估文档的"4 处"与"exe 仍旧产物"更正。
- **复验**：行为套件 **21/21**、`pytest` **128 passed**、`scripts/audit_liu2.ps1` exit=0、lints 0；残留数据与临时件清理见当日记忆。

## 2026-09-10 P-7 批量消息写落地(log_messages + 代理双档聚合)

> 评估:项目根 `docs/plans/2026-09-10-P2批量写立项评估.md`;执行计划:项目根 `docs/plans/2026-09-10-P7批量消息写-执行计划.md`。

- **Rust 后端**:`history.rs` 抽取 `insert_message` 助手(log_message 复用),新 `log_messages` op(数组单事务原子,任一元素非法整批回滚 = P-4 条款③范式),dispatch +1 臂;编译零 warning,安装态 exe 已更新。
- **宿主代理双档**:`log_message_buffered`(background 缓冲,窗口 25ms/32 条/flush 到达/直发与读路径前冲刷保 FIFO 序;`_batch_lock` 串行化四方竞争发送;批量失败降级逐条重发)+ close 先冲刷再关连接。flush 档全程不缓冲,"断连不丢"契约保全。
- **core 最小兼容改动**:`pipeline._persist` background 档 duck-typing `log_message_buffered`(可选能力,默认 SQLite 无此方法走原路径行为不变)。
- **验证全过**:仓库外冒烟(批量原子性/顺序/混合会话/错误路径)+ 双档驱动(缓冲合帧/保序/close 冲刷/flush 档不缓冲)+ 行为套件 17/17 + e2e(两轮对话 health 指标 `log_messages.count=2`、`log_message.count=2`、`ensure_session.count=1`、errors 全 0;重启历史完整恢复)。PENDING P-7 状态 experimental(套件 case 待 runner 支持后补,与 P-4 先例同口径)。
- **执行后评审(plan-auditor,带缺陷通过)与修复**:主要 ①关停期缓冲写入静默丢弃 → 补 `_closed` 守卫显式 error 日志;②批量失败一律降级逐条会因超时放大最坏 N×timeout 阻塞且产生重复消息 → 改为失败分流(超时/连接已关闭只记 error,仅后端 ok:false 类降级)。次要 ③冲刷线程改握手成功后启动 ④close join 冲刷线程 ⑤常量上提模块顶 + 指标口径/append 单线程前提入 docstring ⑥Rust 错误文案笔误修正。修复后复验:专项驱动(关停守卫/真挂起后端注入超时断言不降级/close join)FIX PASS + 套件 17/17 + e2e 指标不变。**修复期自身引入并当场抓获 1 处回归**(常量上提后 `self._BUFFER_MAX` 引用失效 → 验证脚本 AttributeError → 改模块级常量),印证"改后必验"。

## 2026-09-10 P2 立项评估:storage-stdio 批量消息写(→ P-7 登记 proposed)

- 评估文档:项目根 `docs/plans/2026-09-10-P2批量写立项评估.md`。硬数据(N=300):单条 log_message 成本 1.491ms 中**事务提交占 98%**(1.463ms),批量单事务 0.029ms/条;叠加单帧 IPC 税 1.3ms,每合并一条省 ~2.76ms(乘法关系)。
- **裁决**:①批量 `log_messages` op **立项**(P-7,proposed,已登记 PENDING;红线通过——现有 write op 只覆盖 doc_* 表,无法组合出消息批量写);②P-4 条款⑥流水线**不立项降级观察**(收益上限 = RTT 重叠隐藏,后端串行总执行时间不变,重构面大)。
- 关键设计约束:朴素代理层缓冲会静默破坏"断连不丢"契约(flush Future 在 fn() 返回时 resolve)→ **双档设计**(flush 档直发 / background 档缓冲合帧,core 仅 `_persist` duck-typing 最小兼容改动)。
- 执行计划已制定:项目根 `docs/plans/2026-09-10-P7批量消息写-执行计划.md`(5 任务,待实施)。

## 2026-09-10 扩展通道性能优化(P0+P1)

> 设计与执行计划:项目根 `docs/plans/2026-09-10-扩展通道性能优化-执行计划.md`(经 plan-verifier 执行前审查修订)。协议面零变更(PENDING/VERSION/抽象契约均不动),Rust 后端零改动。

- **P0 读路径出事件循环**(`core/pipeline.py`):`chat_stream` 的 ensure_session+get_session_messages 合并单次 `asyncio.to_thread`(新私有方法 `_load_history_sync`),修正"同步磁盘 IO 阻塞事件循环"既有违例——默认 SQLite 与 stdio 后端同时受益;HistoryStore 契约零变更,"先读历史再落盘 user"顺序契约保持。
- **P1 stdio 代理优化**(`server/storage_stdio_proxy.py`):①`ensure_session` 幂等短路(后端语义 INSERT OR IGNORE,同会话首次成功后进程内标记,后续零 IPC;仅成功标记,断连=代理整体失效故集合不失效);②`_OpMetrics` per-op 耗时聚合(count/total/max/avg/errors,宿主调用线程端到端口径,含异常路径),`metrics_snapshot()` 经 `/health` 暴露 `storage_metrics` 键(`server/routes.py` health 增加 request 形参;指标缺失降级不影响健康检查,默认 SQLite 后端无此键)。
- **验证全过**:行为套件 17/17;e2e(7878)两轮对话正常、health 指标 `ensure_session.count=1`(短路生效)/`log_message.count=4`/errors 全 0、重启历史完整恢复(含 content_blocks);临时基准件(d:/tmp)已清理。
- **基线数据(P2 立项依据)**:200 次/操作——log_message 进程内 1.20ms vs stdio 2.53ms(IPC 税 ~1.3ms),get_session_messages 0.16ms vs 0.65ms(~0.5ms)。结论:P2(批量 log_message op、条款⑥流水线)暂不立项——写路径已有 StorageWriter 异步兜底,读路径短路后每轮仅 1 次 IPC(~0.6ms),相对 LLM 秒级延迟可忽略;登记为观察项待写路径量级上涨再评估。

## 2026-09-09 Rust 存储后端扩展 storage_rust 落地(P-6 首个用方,P-2 兑现)

> 设计与执行计划:项目根 `docs/plans/2026-09-09-rust存储后端扩展-设计.md` / `-执行计划.md`。首个真实异语言扩展(Rust),同时兑现 P-2 异语言全链路实战验证。

- **Rust 后端**(`storage-rust/` 仓库内 cargo 单 crate,新入库):teage-storage-rust 实现 P-4 线协议全部 10 op;单线程同步循环零 async(条款⑥串行应答天然满足,rusqlite Connection 非 Sync 恰好绕开),rusqlite(bundled)+WAL,schema 与 core 兼容(新库从零);query 用 json_extract 过滤先于 limit(P-4 语义澄清项⑧),search=LIKE 子串(条款⑤),read 用 `.optional()` 对齐"不存在返回 null";`--db` 必填,父目录自动创建,缺参 exit 2。
- **P-6 宿主组件 backend 目录发现**(`core/extension_loader.py` + `server/host_components.py`):manifest 新增 `kind: branch(缺省)|host-component` + `slots` 字段(host-component 必填、capabilities 必须为空、branch 禁 slots、强制 language: other);扫描识别 host-component 不装载为枝干、core.branches 声明它 = 启动失败;stdio-proxy 工厂签名增补 specs 形参,`options.extension`(与 command 互斥)目录发现 + `options.args` 追加参数;`_resolve_command` 改公开名 `resolve_command`,新增 `find_host_component`;**app.py 将 wire_extensions 提前到 load_host_components 之前**(specs 依赖)。
- **验证全过**:任务 1-3 编译+管道冒烟(帧层/docs op/history op);任务 4 P-4 七条语义条款逐条核对(批量原子性:夹非对象元素整批失败无残留);任务 8 端到端(7879 实例对话落盘 → 重启历史完整重建含 content_blocks)+ 快速失败矩阵 6 项(未安装/kind=branch 引用/slots 未声明/command+extension 互斥/版本 major 不匹配/manifest 损坏)+ 默认路径回归。
- **PENDING 登记**:P-6 宿主组件 backend 目录发现(experimental);P-4 条款增补⑧(query 过滤先于 limit + args 参数通道);P-2 现状更新为"首个真实异语言扩展已落地"。SPI §13.2 同步 kind/slots 规范。

## 2026-09-09 存储扩展接管落盘:插槽制装载器 + stdio 通用代理(P-4/P-5 落地)

> 设计与执行计划:项目根 `docs/plans/2026-09-09-存储扩展接管落盘-设计.md` / `-执行计划.md`。**core 零改动**(仅新增外壳两个通用件 + app.py 装配点接线),默认行为完全不变。

- **插槽制装载器**(`server/host_components.py`,P-5 宿主组件插槽契约的宿主侧参考实现):插槽白名单 `SLOTS`(storage/history 各绑定 core ABC)+ backend 注册表 `BACKENDS`(sqlite 默认 / stdio-proxy)+ isinstance 快速失败(未知插槽/未知 backend/重复接管/ABC 不满足 = 启动失败);config 段 `host_components: [{slot, backend, options}]`,缺省省略 = SQLite 默认。未来新插槽/新 backend 均为"注册一行"。
- **stdio 通用代理**(`server/storage_stdio_proxy.py`,P-4 storage-stdio 线协议宿主侧参考实现):`StdioStorageProxy` 同时实现 StorageProvider + HistoryStore + MessageStore,方法调用机械映射 JSON 帧(`{"id","op","p"}`/`{"id","ok","r"|"e"}`),无任何后端知识;内嵌独立 IO 线程 + 专用事件循环(Proactor,子线程 create_subprocess_exec 可行),同步调用经 run_coroutine_threadsafe;一个实例覆盖双插槽(close 幂等,registry.shutdown 会调两次)。握手 hello 竞速"进程退出"(后端秒退立即失败,不等满超时),major 版本不匹配拒绝;帧编码强制 UTF-8(Python 后端 Windows 默认 GBK,须 reconfigure stdin/stdout/stderr)。
- **PENDING 登记**:P-4 storage-stdio 存储线协议 / P-5 宿主组件插槽契约(proposed→experimental);协议收契约文本,装载器与代理是宿主侧参考实现。
- **端到端联调验证**(仓库外 Python JSONL 参考后端,验证后已删):spawn→握手→双通道落盘(messages/sessions + audit 经 transport storage_write 的 doc_audit_*)→**重启后会话历史由后端完整重建**(LLM 准确引用重启前消息)→优雅关闭;快速失败路径(后端文件不存在/版本不匹配)均为可读错误。默认路径回归:health + 对话与改动前一致。
- 联调挖出并修复的实现缺陷(已回写计划文档):握手信号时序(_handshake_done 原在连接结束才置位)、_request 返回已解包 r 与握手判定不匹配、close 协程与 loop 停止竞态(改同步投递 bye + join IO 线程)、参考后端非重入锁自死锁(log_message 持锁调 _append)、子进程 stdin GBK 编码致中文帧崩溃。
- **已知代价**(core 零改动约束下不可消除):`pipeline.chat_stream` 直接同步调 `ensure_session/get_session_messages`(不经 to_thread),接管后每轮对话事件循环上有 ≤2 次 IPC 等待(最坏 2×request_timeout);`request_timeout_seconds` 建议 5-10s。彻底消除须改 core 读路径,留作后续独立计划。

## 2026-09-09 前端:海滩玻璃水强化 + 会话背板高透明

- **会话背板高透明**:`.messages` 白底 `.24→.10`、blur `14px→3px`(blur+saturate(1.15)),海洋/焦散从板后清晰透出;无 backdrop-filter 回退环境提实底 `.55` 保可读。
- **玻璃水焦散光网**(新增 `.sea-caustics-mask` + SVG `feTurbulence`):浅水区水下折射光网,`type=turbulence` + feColorMatrix 陡阈值(8x-12) + gamma(exponent 2.4) 压成脉络状,SMIL 动画 baseFrequency 26s 呼吸流动;屏幕空间 mask 深水弱→浅水强→出水(86%)即无。注意:首版用 repeating radial-gradient 波点网格,截图验证呈规则圆点阵、观感假,已废弃——**有机光网必须用 feTurbulence**。
- **日光粼光**(`.sea-glints` g1/g2):深水区两组错相位小高光点闪烁(4.6s/6.8s),screen 混合,mask 限定水区(52% 前全显,78% 淡出)。
- **薄水膜拍岸**(`.sea-swash`):玻璃水 sheet 沿岸线法向漫上沙滩再退回,8s 循环、缓推(45%)→停驻(60%)→急退,cubic-bezier(.3,.7,.4,1)。
- **浪沫碎片**(`.sea-foam`):水线处沿岸线走向(25°)的断续白点带,mask 收窄在 87.4-91.2%,与 swash 同节奏(8s)淡入淡出,相位同步模拟同一道浪。
- 用户消息靠右(`.message` 加 `width:fit-content`)与刷新恢复历史(初始化补 `loadHistory`)两处修复亦在本轮完成。
- **气泡内 markdown 排版**(用户:内容太贴气泡边框):气泡 padding `11/15→13/17px`;补齐缺失的间距规则——p `.5em`、h1-h3 `.9em/.45em` + 字号分级(1.25/1.15/1.05em)、ul/ol 缩进 1.5em + li `.3em`、嵌套列表/代码块/分割线间距、`>:first/:last-child` 贴边归零(边距由 padding 统一控制)。CSS v=20260909g。playwright 注入 markdown 气泡截图验证通过。
- **圆润/阴影/过渡质感轮**(用户:边缘毛刺感):①圆角令牌 `10/8/6→14/10/8`,气泡尾角 4→6px;②阴影全部改**双层柔影**(远投影定浮起 + 近投影定轮廓),气泡/会话项/发送按钮统一;③发送按钮加 `active` 按压缩放(.97)+ 顶部 inset 高光,过渡统一 `--ease-smooth: cubic-bezier(.25,.7,.3,1)`;④body 开字体抗锯齿(antialiased);⑤滚动条胶囊化+hover、新增 `:focus-visible` 柔和青色焦点环、copy-btn/icon-btn 补阴影过渡。CSS v=20260909h。
- **品牌区主题化重做**(用户:tea ge 文字平淡不体现主题)→ **二轮简化**(用户:字体不好看、特效不需要、名字意为 testAgent)→ **三轮收敛**(用户:不要点明 testAgent):最终形态 = 图标(26px+青色光晕) + "teage" 单词,字体 Bahnschrift(DIN 系,Win10+ 自带,兜底 Segoe UI Variable/Segoe UI),700 字重、字距 1px、纯色 wave-strong;**渐变文字/波浪下划线/test agent 副标全部移除**(用户明确不要特效、不要点明来源)。CSS v=20260909k。CSS 版本 v=20260909f、app.js v=20260909c。

## 2026-09-09 扩展端到端验证:拦截路径与终态钩子六处修复,audit 全链路打通

> 前端实测 guardrails 拦截 + audit 落盘(denylist=["测试拦截词"],用户在 config.yaml 自行添加),暴露拦截路径与路由层缺口,逐一修复并复测验证。行为套件 17/17 回归通过。

**core(pipeline.py)**
- 防陈旧快照:模式实例跨对话复用,SetStop 短路发生在 run_stream 重置 `final_snapshot` 之前,终态钩子(on_error/after)与会话态写回拿到**上一场对话的快照** → 构建快照后立即归位 `final_snapshot = snapshot`
- 拦截提示友好化:"对话已被枝干拦截" → "您的消息包含不允许的内容，已被安全策略拦截。请调整表述后再试。"(core 通用文案,不点名枝干,守依赖铁律)
- 拦截审计可区分:拦截分支先设 `error_message = "对话被安全策略拦截"`,on_error 文案不再是兜底"对话未完成(中断/失败/拦截)";语义锚定:拦截=round 0+拦截文案,中断/失败=round≥1+兜底文案

**server(routes.py)**
- **[P2] SSE 提前 return 关闭生成器,after 终态钩子永不触发**:event_source 在 done/error 后 return,`chat_stream` 中 async for 之后的 `after_all` 从未执行(生产环境 audit.conversations 从未落盘) → 删除提前 return(hooks._call 异常隔离,pipeline 自然收尾)
- /reload 响应序列化 500:返回值把 `registry.entries` 的 **Branch 实例**当名字(变量名误导),FastAPI 序列化钻进 host_port/bus 触 `_thread.lock` → 改为 `branch.name`(reload 本身一直成功,仅响应编码失败)
- /chat 非流式采用 done.response(原 P3):拦截路径返回空串、loop 多轮口径不一致 → done 事件优先取 `ev["response"]`,空才回退 text_parts

**验证**
- 行为套件 runner 17/17(termination_reason 语义未变,无破坏)
- 前端实测:拦截显示新文案;`doc_audit_conversations` 首次建表(3 场正常对话 termination_reason=normal);audit.errors 拦截记录 user_input/round 正确;audit 四类记录(steps/l3_events/conversations/errors)全链路打通
- /reload 返回 `{"reloaded":true,"branches":["guardrails","audit"]}`,热重载后新链拦截 + audit 落盘正常

**文档**:缺口记录 5 条移入已修复区(含原 /chat P3),缺口清单仅剩 SessionStore TTL(契约而非缺口)。

---

## 2026-09-09 新增 PROTOCOL/PENDING.md:待实验并入协议追踪文档

> 用户定案:待扩展协议（如 manifest 规范）统一用 md 文档追踪——新协议候选先写该文档,正式使用/迭代稳定后才正式更新进协议,并同步登记。

- 新增 `teage_liu2/PROTOCOL/PENDING.md`:演进面候选元素追踪（非契约）。流程 = 先写 PENDING（proposed/experimental）→ 实验迭代 → stable → 入协议（spec+schema+行为套件用例+VERSION 升版）→ 移入"已并入记录"区。含状态定义、条目模板、准入红线（evolution RFC:能由现有 custom:* 组合实现的不升级）。
- 首批登记 3 条:P-1 manifest.yaml 扩展声明规范（experimental,已落地运行待冻结字段集+补用例）、P-2 异语言 stdio 扩展全链路实战验证（experimental,代码就绪 0 案例）、P-3 custom:* 扩展点实战案例（proposed,观察项）。
- `PROTOCOL/README.md` 加"演进追踪"段指向 PENDING.md（契约源纯净不受影响,PENDING.md 本身不是契约）。

---

## 2026-09-09 会话历史动态加载:前端分页回放 + GET /sessions/{id}/messages

> 用户需求:打开旧会话看不到历史对话;要求动态加载、不一次性渲染全部(防浏览器卡顿)。

**后端**
- `core/history.py`:`get_session_messages` 增加 `before_id` 游标参数(只取 id < before_id,配合 limit 实现"最近 N 条 → 更早 N 条"向上翻页);抽象接口 `HistoryStore` 同步签名;向后兼容(不传即原语义)
- `server/routes.py` 新增 `GET /sessions/{session_id}/messages?limit=30&before_id=`:limit 夹取 [1,200],返回 `{messages, has_more}`;SQLite 同步 IO 经 `asyncio.to_thread` 包裹不阻塞事件循环

**前端(teage_liu2/web)**
- `js/app.js` 新增历史回放区块:切换会话拉最近 30 条渲染;user 文本气泡 / assistant 思考折叠块(reasoning 列,呈"思考完成"态)/ content_blocks 回放(text → 气泡,tool_use → 完成态工具卡,结果按 tool_use_id 从后继 user(tool_results) 聚合消息配对);旧行无 blocks 回退纯文本
- "加载更早消息"按钮置于消息区顶部,点击向上翻页,prepend 后保持视口锚定(scrollHeight 差值回推)
- 流式中禁止切换会话;切换后清空加载态;首次加载前隐藏欢迎屏
- `index.html` css/js 引用加 `?v=20260909b` 强缓存穿透

**验证**:py_compile / node --check 通过;d:\tmp 临时脚本验证游标分页(最近 3 条/翻页/翻到头/全量兼容)全部断言通过后即删。

---

## 2026-09-08 统一扩展目录树:扩展装载运行期化,branches/ 包消亡

> 用户判定"扩展应统一目录树管理、不进 liu2 源码"为大问题。按 `docs/plans/2026-09-08-统一扩展目录树-设计.md`(用户逐条定案)+ 同名实现计划执行完毕。

**架构变化**
- 新增 `core/extension_loader.py`:manifest.yaml 严格解析(ExtensionSpec)→ 目录扫描(discover_extensions)→ importlib 动态装载(load_python_extension,模块名含 manifest_hash 热重载隔离)→ wire_extensions(create_app 与 /reload 共用:校验声明/合并 stdio 字段/已装未启用统计)
- registry 新增 `set_directory_loader` 目录发现通道(解析优先级:factory > directory_loader > ValueError);`register_factory` 保留并重新定位 = **测试/行为套件/嵌入注入通道**(设计 §2.1,8 处标注点全链路落实)
- `server/app.py` 装配点例外消失:零 branches import、零 register_factory 调用;`/reload` 重扫 extensions_root + 重接 loader
- config 新增 `core.extensions_root`(默认 data2/extensions,仓库外不入库)
- **`teage_liu2/branches/` 整个消亡**:audit / guardrails 外迁为 `data2/extensions/<name>/{manifest.yaml, main.py}`(最小两文件形态,main.py 约定导出 create_branch)
- 文档同步:SPI §12 三步法重写 + §13.2 新章节(manifest 规范/装载语义/信任边界 + register_factory 定位专段)、CORE.md 接入步骤、INTERFACES.md §1/§2/§9 改写

**验证(全部亲自运行)**
- tests_core + tests_branches **103 passed**(新增 14 用例:parse 6/discover 2/load 2/registry 通道 2/wire 1/config 1);行为套件 runner **17/17**;lint 零错误
- 端到端(d:\tmp 脚本,已删):目录发现→装载→对话落盘 audit.*→热重载 manifest 变更换模块→安全默认(未声明不启用/声明未装启动失败)

**端到端发现并修复的真实生产缺口**:同语言扩展身份从未向 TransportBus 注册(仅 supervisor 为 stdio 扩展注册)——audit 的 storage_write 在生产路径被"扩展未注册,拒绝入站请求"静默拒绝(audit._write 吞异常仅 warning,落盘全部丢失,2026-08-21 接入验证脚本因手工注册身份而未暴露)。修复:`server/app.py` 新增 `register_inprocess_extension_identities(bus, registry)`,create_app 装配后与 /reload rebuild 后为链上全部普通 Branch 实例注册身份(stdio 扩展仍由 supervisor 负责)。

**取舍**(设计 §8):进程内信任执行(兜底 hook_timeout/异常隔离/L3 超时);guardrails 外迁后安全策略完整性 = 扩展目录完整性(用户定案接受);manifest 规范不入 PROTOCOL v1.0 冻结面。

---

## 2026-09-08 L3 旁路收尾修复:退订闭环 / close 冲刷 / 缓冲有界 / 投递超时

> 用户要求"妥善处理 P3 问题"。2026-09-08 深入检查发现的 4 项 P3 全部修复。

**修复内容**
- `server/app.py` `subscribe_inprocess_l3` 退订闭环:返回本次订阅名集合,`create_app` 挂 `app.state.inprocess_l3_names`;`/reload` 传 `prev_names` 对已移出链的 observe 扩展 `l3_sink.unsubscribe`(prev 集合仅含本函数历史订阅的同语言 name,不误伤 supervisor 的 stdio 订阅)。新增 `L3_DELIVER_TIMEOUT_SECONDS=5.0` 投递超时兜底(§3.2 旁路非阻塞义务的宿主侧实施,TimeoutError 交 `L3ObserverQueue.deliver` 统一计入 dropped,§15-A6)
- `core/event_stream.py` `L3BatchSink`:冲刷前全局 `_buffer` 加上限(`buffer_max`=1024),满则丢弃最旧 + 计数并计入 `total_dropped`(§15-A6,观测者受阻时缓冲不无界堆积);`close()` 从"清空丢弃"改为"尽力送达缓冲 + 各观测队列尾批"(先置 closed 拒新,投递失败仅计数不阻断 shutdown),docstring 与实现对齐
- `branches/audit.py`:`record_steps` 不再在 `__init__` `bool()` 强转,`setup` 校验实化(脏值如 `"yes"` → 启动失败,F2,而非静默吞成 true)
- `server/routes.py`:`/health` 与 `/` 显式返回 `protocol_version`(PROTOCOL/ v1.0.0),与应用自身版本(app.version 0.1.0)消歧

**验证(全部亲自运行)**
- 行为套件 runner **17/17**;tests_core + tests_branches **89 passed**;lint 零错误
- 端到端冒烟(d:/tmp 脚本,已删):①buffer_max=3 投 5 条 → 丢 2 计数正确、送达 3 ②close() 冲刷尾批 1 条 ③退订闭环:退订已移出的 name,保留在链的与模拟 stdio 订阅 ④audit 脏值 `"yes"` 被 setup 拒绝

---

## 2026-08-21 缺口修复:同语言 observe 扩展的 L3 观测投递接线(闭环)

> 用户要求"解决缺口问题"。修复 `CORE-缺口记录.md` 的 P2 缺口(同语言 observe 扩展收不到 L3 原始事件)。

**修复内容**
- `core/hooks.py` Branch 新增 `async def on_l3_events(events)` 默认空实现——同语言扩展的 L3 观测通知入口(非钩子,不经 HookChain,旁路异步投递)
- `server/app.py` 新增 `subscribe_inprocess_l3(l3_sink, hooks)`:装配时对声明 observe 的普通 Branch 实例(非 RemoteBranchAdapter,后者由 supervisor 走 stdio)调 `l3_sink.subscribe(name, deliver_fn)`,deliver_fn 进程内异步调 `branch.on_l3_events(events)`(异常隔离,旁路不阻断主对话流);create_app 装配后调用 + 挂 app.state 供热重载复用
- `server/routes.py` `/reload` 热重载成功后重新订阅(rebuild 产生新实例,覆盖同 name 订阅)
- `branches/audit.py` 增加 `on_l3_events` 演示(批量落盘 `audit.l3_events`);`_write` 改为支持批量 docs

**验证(全部亲自运行)**
- 行为套件 runner **17/17**;tests_core + tests_branches **89 passed**
- 端到端(d:/tmp 脚本,已删):工具路径对话产生 step_end/tool_use/tool_result 共 4 条 L3 事件,**全部经 on_l3_events 到达并落盘 audit.l3_events**;echo 工具枝干独立身份写 audit kind 被前缀隔离正确拒绝(脚本首版继承设计 bug,修正后干净)
- create_app 冒烟:l3 observers 含 `audit`(装配即订阅)

**文档**:CORE.md §2/§11、SPI §8、INTERFACES.md §0 补 on_l3_events;缺口记录 P2 移入已修复区。

---

## 2026-08-21 首个扩展 audit 落地 + core 终态快照缺陷修复（实验驱动）

> 用户要求"更新计划并执行计划,注意实验观察记录,发现解决 core 实际的问题"。按 `docs/plans/2026-08-21-首个扩展接入-观测枝干-执行计划.md` 执行完毕,实验驱动发现并修复 1 个 core 真实缺陷。

**新增枝干**
- `teage_liu2/branches/audit.py`:同语言 observe 观测枝干(after_step 每轮摘要 / after 对话完成 / on_error 失败记录,经 `host_port.storage_write` 落宿主存储,kind=audit.*,全部钩子返回 [] 遵守 observe 只读)
- `server/app.py` composition root 注册 `audit` 工厂;`config.yaml` 增 `core.branches.audit`(record_steps)

**core 真实缺陷修复(实验发现)**
- **P1 终态钩子收到初始快照而非终态快照**:`pipeline.py` 的 `after_all`/`on_error_all` 传构建时的初始 `snapshot`(round 恒 0、messages/extra 为对话前状态),与同处 `_persist_session_extra` 用 `final_snapshot` 不一致(§5 L-10 一致性缺陷)。修复:两处统一改为 `getattr(self._mode_instance, "final_snapshot", None) or snapshot`。
- 实验观察记录:验证脚本首次运行时发现 `after_step` 的 text/usage 空——实为脚本 FakeLLM 未按协议 yield `done`(step.py 只认 `done` 收集 content_blocks),非 core 问题;修正脚本后 text/usage/duration 正常。

**验证(全部亲自运行)**
- 端到端(d:/tmp 脚本,已删):audit.steps 1 条(round=1/text="回复 1"/usage 填充)、audit.conversations 1 条(rounds=1,验证终态快照修复)、audit.errors 0 条;observe 只读约束成立(返回 action 被忽略,对话正常)
- 行为套件 runner **17/17**;tests_core + tests_branches **89 passed**
- 装配冒烟:create_app 正常注册 audit

**实验缺口(未修复,见 CORE-缺口记录.md)**:同语言 observe 扩展的 L3 观测投递未接线(supervisor 仅对 stdio 异语言扩展 subscribe)——audit 走 L2 通道规避。

---

## 2026-08-21 首个扩展接入计划产出:audit 观测枝干(实验性)

> 用户要求"设计第一个扩展接入,写接入计划,要看接入步骤"。按 writing-plans 规范产出 `docs/plans/2026-08-21-首个扩展接入-观测枝干-执行计划.md`(计划文档仍在项目根目录)。

**设计定案**:
- 第一个实验性扩展 = **同语言观测枝干 `audit`**(声明 `observe` 只读能力),经 `after_step` / `after` / `on_error` 钩子把对话轮摘要、完成记录、错误记录经 `host_port.storage_write` 落宿主 SQLite——覆盖"实现 → 注册 → 配置 → 消息通道 → 落盘"完整接入链路。
- 4 任务:①建 `teage_liu2/branches/audit.py` ②装配注册(app.py 1 行 register_factory + config.yaml 声明,改 config.yaml 须用户同意)③d:/tmp 端到端验证脚本(FakeLLM + 真实 pipeline/TransportBus/SQLite,验证后删除,遵守纯净原则)④SPI §13 案例 + CHANGELOG 摘要。

**接入链路验证点**:钩子调用 → host_port 消息 → TransportBus(kind 前缀隔离/授权)→ StorageWriter 队列 → SQLite 落盘;observe 只读约束(返回 action 被忽略);`enabled: false` 不注册(可拔插)。

**实验发现缺口**:同语言 observe 扩展的 **L3 观测投递未接线**(supervisor 仅对 stdio 异语言扩展做 l3_sink.subscribe)——详见 `CORE-缺口记录.md` P2 项;audit 刻意走 L2 通道规避。

**文档同步**:`teage_liu2/docs/INTERFACES.md` 整体重写为 Snapshot+Action + host_port 消息通道新签名(原 BranchContext 旧签名滞后,与 CORE.md/SPI/core 代码不一致);新增 §0 新签名速查(11 钩子 + 6 Action + host_port 接口)。

---

## 2026-08-21 INTERFACES.md 同步至 Snapshot+Action 时代

- **问题**:`teage_liu2/docs/INTERFACES.md` 仍是 BranchContext 旧签名(BranchContext/tools 直改/core.storage_provider 对象直访/core.session_store 直访/core.task_registry.create_task),与 CORE.md / SUBSYSTEM-SPI.md / `core/` 代码不一致,会误导新枝干接入。
- **改动**:整体重写为 Snapshot+Action + host_port 消息通道新签名。
  - 新增 §0 新签名速查:11 钩子签名表 + 6 Action 表 + host_port 接口表(storage_*/invoke_llm/task_* 消息 + kind 前缀要求)
  - §1 weather 示例:before 返回 SetTools;after 审计走 host_port.storage_write(kind 带 weather. 前缀);setup(config, host) 展示 host 纯数据声明;声明 capabilities=["tool_executor"]
  - §4 拦截示例:ctx.stop=True → [SetExtra, SetStop]
  - §5 inject_round:返回 Injection(layer 强制 BEFORE_INPUT)
  - §6 三态模型:会话态改 snapshot.extra 会话内延续(不经 SessionStore 直访);持久态改 host_port.storage_write
  - §7 后台任务:asyncio.create_task + host_port.register_task 登记(T-4 任务归属扩展进程)
  - §8 钩子内调 LLM:host_port.invoke_llm(声明 llm capability + 冷路径条款 + 失败降级语义)
- **验证**:文档引用 API 逐一核对 core 代码(hooks/actions/injection/types/transport/registry/tasks/pipeline/history/llm),lint 零错误。
