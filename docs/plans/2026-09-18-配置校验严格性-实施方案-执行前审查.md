# 配置校验严格性 — 实施方案 · 执行前审查报告

> 被审查对象：`docs/plans/2026-09-18-配置校验严格性-实施方案.md`（v1）
> 审查性质：**只读审查**，不改代码/配置。审查后产出方案 v2。
> 审查日期：2026-09-18

## 0. 审查结论：**有条件通过**

Phase 1 的**设计方向成立**（"实现对齐既有 schema 约束"这一定性正确，非加严取舍），但 v1 方案有 **4 处必须修正**，另有 1 处既存漏洞被审查补录。修正后形成 v2，**可执行**。

## 1. 审查方式（两轨独立 + 主 agent 裁决）

| 轨道 | 角色 | 审查重点 |
|---|---|---|
| 轨一 | 可行性审查（plan-verifier 角色） | 改动完备性、校验顺序对既有断言的影响、错误路径可达性、语义边界、错误码、会导致回归的问题 |
| 轨二 | 协议一致性审查（plan-auditor 角色） | 契约事实核对、演进分类、文档同步义务完整性、C-1 条款、套件覆盖充分性 |
| 裁决 | 主 agent | 对两轨**冲突结论**亲自取证裁定（不采信单方） |

## 2. 必改项（v1 → v2，均已落实）

| # | v1 的问题 | 定性 | v2 处置 |
|---|---|---|---|
| 1 | **示意代码有 bug**：`sorted(SLOTS.get(slot, {}))` 中 `SLOTS` 的值是 **ABC 类元组**，`sorted` 会抛 `TypeError`（而非 `ValueError`）→ 照抄会让新测试**必红** | 执行级错误 | 改为 `sorted(BACKENDS.get(slot, {}))` 并加显式警告 |
| 2 | **文档同步清单失实**：v1 点名 `CORE.md`/`INTERFACES.md`/`SUBSYSTEM-SPI.md` 需改，经核实**三处均不含** backend 默认表述；而**真正**需改的 3 处（`host_components.py:11` docstring、`config.yaml.example:344`、`PENDING.md:77`）v1 反漏列 | 清单错误（假清单=漏改） | 按核实结果**整体替换**清单 |
| 3 | **C-1 修正方向错误**：v1 主张 C-1 括注"（收到自己的配置段）"与实现不符、应改措辞 —— **错误**。生产路径 `registry.setup_all` 传的是 `branch_cfg`（`core/registry.py:230-242`；`server/app.py:298` 调用的正是它），**C-1 是正确的**；v1 据以判断的 `HookChain.setup_all`（`core/hooks.py:256-261`）仅测试使用 | 若执行将**写入错误契约表述** | **撤销该修正**；改为记录"两名 `setup_all` 同名不同义"的观察 |
| 4 | **错误码建议不成立**：v1 建议"本轮先不补 `CODE:` 前缀"并称"不构成违规" | 依据不足 | **推翻**：`errors.spec.md:69` 的④面明确要求 `ValueError` 消息带 `CODE: message` 前缀；v2 改为**复用既有三码统一补齐**（无新增码 → 不触发 R-2 流程） |

## 3. 审查补录的既存漏洞（v2 新增 G4）

`load_host_components` 的 `entries = cfg.get("host_components") or []` 使 `host_components: null / {} / 0 / ""` 等 **falsy 非数组被静默当作"缺省"**，绕过紧随其后的 `isinstance(entries, list)` 检查 —— 与 `config.schema.json` 的 `type: array` 漂移，且属与 G1 同类的"静默容忍"。v2 已补一致性修正与测试项。

## 4. 两轨结论摘要（一致项）

- **改动完备性**：除已定位的 3 处测试外，**全仓无其他**代码/配置/用例依赖"条目内 backend 缺省"（已检索 `tests_core`/`tests_branches`/`cases/*.json`/`runner.py`/`scripts/`/根 `*.yaml`）。结论：**改动面收敛、可控**。
- **校验顺序**：新增检查须置于 entry 类型校验**之后**；受影响者仅 `test_duplicate_takeover_rejected`（`match="重复接管"`），v2 已通过补 `backend` 修复。
- **错误路径可达性**：有 3 条既有分支（未知插槽 `:134-137`、slot 级重复接管 `:138-139`、options 非映射 `:147-148`）在"缺 backend 条目"上会被提前拦截 → v2 通过给对应参数项补 `backend` **保住原意图**（否则会"静默变绿但测错分支"）。
- **语义边界**：省略整段 / 空数组 → 内置 SQLite 的路径**不经条目级校验**，确认无误伤（case 41 + `tests_core:15/21` 双锚定）。
- **演进分类**：本次**不增删/不改名配置键** → 依 `config.spec.md` §4 **不触发 minor/major，不升 VERSION**；errata 定性成立。但登记动作需补（`PENDING.md:39` 计数、`:77` 勘误、case 24 `description`）。
- **契约事实核对**：`config.schema.json:38-39`（`required` + `additionalProperties:false`）、`config.spec.md:41/49/53` 三处明文 —— **逐条核对成立**。

## 5. 遗留分歧与未决

| 项 | 状态 |
|---|---|
| 两轨关于错误码的分歧 | **已裁决**：轨二正确（errors.spec ④ 面要求前缀），v2 采纳"统一补齐" |
| 两轨关于 C-1 的分歧 | **已裁决**：轨二正确（生产走 `registry.setup_all`），v2 撤销修正 |
| G4 是否纳入本次 | **待你裁决**（v2 §1.5 建议纳入） |
| 错误码补齐范围（仅新分支 vs 全部分支） | **待你裁决**（v2 §1.5 建议全部，否则半对齐） |
| 未审查项 | Phase 2–4 仅作设计描述，**未做执行前审查**；各自执行前须单独审查 |

## 6. 审查方法学备忘

- 本次两次"我自己写错、审查抓出"的实例：① v1 示意代码 `SLOTS` 误用；② v1 的 C-1 修正方向（源于只看 `build`/`hooks` 路径而未核**生产装配路径** `registry.setup_all`）。
- 教训：**审计类结论必须以"生产调用路径"为准**，不可由同名函数推断（`HookChain.setup_all` vs `BranchRegistry.setup_all`）；且方案中的示例代码本身也须经审查（示例会被照抄）。
- 文档同步类任务**禁止凭印象列清单**（v1 的失实清单即属此类），须先 grep 取证再落清单。

---
*审查人：Trae（AI 协作，两轨独立审查 + 主 agent 裁决）；日期：2026-09-18。*