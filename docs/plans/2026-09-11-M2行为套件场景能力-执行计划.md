# M2 行为套件场景与用例扩展 执行计划（WP-A → WP-B）

> **来源 / 依赖**：`docs/plans/2026-09-10-协议收口与遗留债-解决计划.md` 的 §2（WP-A）与 §3（WP-B）。
> **本文件的定位**：**执行编排 + 事实复核修正**。代码骨架不在此重复 —— 以原计划 §2 的"已就地修订版"为准，凡与本文件 §0 冲突处，**以本文件为准**。
> **前置状态**：M1 已于 2026-09-11 完成（套件 21/21、pytest 159 passed、`audit_liu2.ps1` exit=0）。
> **本轮范围**：**WP-A（A1–A5）**；WP-B（13 条用例）紧随其后（依赖 WP-A 产出的断言能力）。

> **执行状态（2026-09-11）：WP-A + WP-B 全部完成** —— 用例 21 → **39**、套件 **39/39**、pytest **165 passed**、`audit_liu2.ps1` **exit=0**；13 条新用例**逐条变异验证**（退化实现 → 变红 → 恢复）。执行中发现并修复 **3 项缺口**（E-9 的 `HOOK_TERMINAL_ACTION_IGNORED` 未发射 / 部分层 `injection_budget` KeyError / 参考后端嵌套事务），登记 **2 项未修缺口**（H-17 无内核级只读、H-9 负例不可构造）—— 见 `teage_liu2/docs/plans/CORE-缺口记录.md`。PENDING 条件③：**P-2 / P-4 / P-6 已满足**，P-1 / P-5 / P-7 部分满足（余项属 M3/WP-E）。
> **§0.2 修正项在实现中全部得到证实**（C1 调用点 2 处、C3 无占位符、C8 无 `log_messages` 方法、C13 `BACKENDS` 仅 storage、C14 `start()` 非幂等、C16 schema 守卫层、C19 只改 pipeline 构造区）。**另新增两条计划外事实**：①用例除登记 `protocol_pipeline_ids` 外，**id 前缀必须与 `_run_protocol_case` 的分支前缀匹配**（用例 26/37 首跑因前缀不匹配落到 pipeline 类报"缺少事件"，已修）②`assemble_injections` 的预算 dict 需真值合并（见上）。
> **门禁命令（每阶段收尾必跑）**：
> - `D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py`
> - `powershell -ExecutionPolicy Bypass -File scripts\audit_liu2.ps1`（期望 `exit=0`）

---

## §0 事实复核（2026-09-11；双源 = code-explorer 子代理 + 主 agent 精读；行号为复核时真值）

### 0.1 确认项（原计划说法成立，可照办）

| # | 事项 | 复核证据 |
|---|---|---|
| C2 | 协议层分支函数返回 `List[str]`、`custom_events` 就地追加、以 `_run_config_case` 为范本 | `runner.py:1254-1255`（`async def _run_config_case(case, custom_events) -> List[str]`）、`runner.py:819`（`custom_events` 局部定义）、`:1024`（调用点） |
| C4 | `wire_extensions(cfg, extensions_root=None) -> (merged, specs, disabled)`；`extensions_root` 取自 `cfg["core"]["extensions_root"]`；**只处理 `core.branches`，不处理 `host_components`** | `extension_loader.py:262-323` |
| C5 | 子进程句柄 = `proxy._conn._proc`（**无** `proxy._proc`） | `storage_stdio_proxy.py:88`、`:183` |
| C6 | `StdioStorageProxy.write` 支持批量：`write(kind, [docs])` → 返回 doc_id 数组；`write(kind, doc)` 单条 → 返回标量 | `storage_stdio_proxy.py:364-370` |
| C7 | `StorageWriter.close()` 是**同步**方法（不可 await） | `core/storage_writer.py:228` |
| C9 | `_SUPPORTED_FINAL_KEYS = {termination_reason, is_complete, persisted}`（`llm_assert` 尚未登记） | `runner.py:126` |
| C10 | `cases/` 现有 21 个文件，最大编号 21 | `cases/01-*.json … 21-config-domain.json` |
| C11 | `SessionStore()` 无参构造 | `core/session.py:15` |
| C12 | `PROTOCOL_VERSION = "0.1.0"`、`PROTOCOL_NAME = "storage-stdio"` | `storage_stdio_proxy.py:29-30` |
| C17 | `match_value(assertion, actual) -> bool` 已存在（A2 可直接用） | `runner.py:81-104` |
| C18 | `_assert_event_stream(expected_events, actual_events) -> List[str]` | `runner.py:456` |

### 0.2 修正项（**执行时必须按新说法**）

| # | 原计划说法 | 复核结论 | 处置 |
|---|---|---|---|
| **C1** | R8 称 "`_assert_final` 无 `llm` 形参，需同步 **5 处**调用点" | 实际**只有 2 处**调用点：`runner.py:708`（`_run_pipeline_case`）、`runner.py:909`（协议层 storage 分支） | 新增参数 `llm=None`（默认值）→ **既有 2 处零改动**；仅在新分支显式传 `llm` |
| **C3** | A3 的 manifest `command` 写 `["{python}", "{backend}", "--db", "{tmp}/ref.db"]` 占位符，由 runner `str.format` 替换 | **占位符机制不存在** —— `resolve_command`（`extension_loader.py:247-259`）只做"路径在扩展目录内且存在 → 绝对化"，不做模板替换 | 改为**直接写绝对路径**：`[sys.executable, <reference_storage_backend.py 绝对路径>, "--db", <tmp 下 db 绝对路径>]`；runner 组装后再 `yaml.safe_dump` 落盘 |
| **C8** | A2/A5 用例的 `storage_ops[].op` 用 `log_messages` 等 | `StdioStorageProxy` 公开方法只有 `write / read / query / delete / ensure_session / log_message / log_message_buffered / get_session_messages / update_session_title / get_session_title / search_messages / metrics_snapshot / start / close`；**不存在** `log_messages` 公开方法 | 用例的 `op` 必须是真实方法名；**批量原子性用 `write(kind, [docs])`**（C6） |
| **C13** | A3 断言 `sorted(loaded.keys()) == ["history", "storage"]` | `SLOTS = {storage: (StorageProvider,), history: (HistoryStore, MessageStore)}`；`BACKENDS` **只有 `storage` 一个插槽键**（`{sqlite, stdio-proxy}`） | 原断言成立；用例 `host_components` 用 `slot: storage`（正确） |
| **C14** | — | `StdioStorageProxy.start()` **非幂等**（重复调用 → 冲刷线程二次 start 抛 `RuntimeError`）；`close()` **幂等** | 用例 `finally` 只 `close()`，不二次 `start()`；变异验证"注释掉 `start()`"后 `finally` 的 `close()` 仍须安全 |
| **C15** | B1 的用例 16 增 `error_codes` 断言（M1 已做） | `_run_error_matrix(case)` **不接收** `custom_events`（内部自造 `custom` 事件） | 若 WP-B 需给用例 16 注入自定义事件，须先改造该函数签名（列入 B1 前置核验） |
| **C16** | 全局约束的 schema 论述 | `Expected.additionalProperties = false`（`suite.schema.json:95`）；`FinalExpectation.additionalProperties = true`（`:71`）；`TestCase.inputs` **无 properties 约束**（`:114-117`） | 新增 `expected` 顶层键必须同步 schema；`final.*` / `inputs.*` 由 runner 守卫表拦截（不是 schema） |
| **C19** | A4 的 `_run_pipeline_case` 改造位置 | `storage_writer=None` 硬编码在 `runner.py:657`；`ChatPipeline(...)` 在 `:652-658`；**另有 2 处相同构造**：`:1141-1147`、`:1205-1208` | A4 只改 `:652-658`（pipeline 类）；另两处属协议层分支，不动 |

### 0.3 待核验项（**A3 步骤 0 必须先读**，不得凭想象写用例）

- `server/host_components.py` 的 `_stdio_proxy_backend`（约 `:40-96`）如何合并 `options.extension` / `options.args` / `options.command`：互斥判定、args 追加位置、`specs` 形参用途。**读完再写 A3 的 `inputs` 与断言**。

---

## §1 执行编排（WP-A）

> 每个阶段=一个 TDD 循环（红 → 绿 → 变异验证 → 门禁）。**阶段间不并行**（A2 依赖 A1 的参考后端，A3/A5 复用 A2 的 `_run_stdio_case`，A4 是 B 的前置）。

### 阶段 A1：P-4 参考存储后端（语言无关交付物）

| 项 | 内容 |
|---|---|
| 目标 | 落地 `PROTOCOL/behavior-suite/tools/reference_storage_backend.py`：**不 import `teage_liu2.core`**，仅用标准库 `sqlite3` 独立实现 P-4 全部语义条款 |
| 新建 | `teage_liu2/PROTOCOL/behavior-suite/tools/reference_storage_backend.py` |
| 新建（测试） | `teage_liu2/tests_core/test_reference_backend.py`（允许入库：`tests_core` 已有用户授权） |
| 代码骨架 | 原计划 §2 任务 A1 步骤 3（**已就地修订版**：hello 返回 `{"protocol": "storage-stdio", "version": "0.1.0"}`、`write` 恒返 ids 数组、`log_message` 扁平载荷、`log_messages` 用 `p["messages"]`、元素类型显式校验 + 整批回滚） |
| 验证 | ① `python -m pytest teage_liu2/tests_core/test_reference_backend.py -q` → PASS；② 与 `storage-rust/src/history.rs` 人工对拍同一份 P-4 条款清单（query filters 先于 limit、子串搜索、批量同事务） |
| 变异验证 | 把参考后端 `write` 的 `with self.conn:` 改为逐条 `commit()` → A5 用例（阶段 A5）必须变红；本阶段先记入待办 |
| 收尾 | 门禁两条命令全绿 |

**注意（C3 相关）**：参考后端 `_parse_db_arg` 要求 `--db` 必填；三流 `reconfigure(encoding="utf-8")`（Windows GBK 防线）。

### 阶段 A2：runner 真实子进程 stdio 场景（用例 22）

| 项 | 内容 |
|---|---|
| 目标 | runner 可 spawn 真实子进程（参考后端）→ hello 握手 → 双通道读写 → bye 关闭 |
| 新建 | `cases/22-stdio-proxy-roundtrip.json` |
| 修改 | `runner.py`：① import 段补 `from pathlib import Path`、`import copy`、`import yaml`（当前**均无**，见 `runner.py:21-33`）；② `_run_protocol_case` 增 `elif case_id.startswith("stdio-proxy"):`；③ 新增 `_run_stdio_case(case, custom_events) -> List[str]`；④ `protocol_pipeline_ids` 加 `"stdio-proxy-roundtrip-22"` |
| 接口消费 | `from teage_liu2.server.storage_stdio_proxy import StdioStorageProxy`（**runner 首次依赖 `server/`** → 须在 `PROTOCOL/README.md` 的"依赖边界"段同步声明，该段已在 M1 建立） |
| 观测 | 结果经 `custom:stdio_result` 事件交 `expected.event_stream` 断言（与既有 `custom:host_port_result` 同模式） |
| 关键事实（C5/C8/C14） | pid 取 `getattr(getattr(proxy, "_conn", None), "_proc", None).pid`；`storage_ops[].op` 用**真实方法名**（`ensure_session`/`log_message`/`get_session_messages`）；`start()` 只调一次 |
| 验证 | `runner.py` → `[PASS] stdio-proxy-roundtrip-22`，且**原 21/21 不回归** |
| 变异验证（必做） | ① 注释 `proxy.start()` → 用例红；② `child_pid_gt_0` 恒 `True` → 用例红（证明断言承重）；③ 恢复 |

**登记清单（三处缺一不可）**：`protocol_pipeline_ids`（`runner.py:1308-1313`）+ `_run_protocol_case` 的 `elif` + 分支函数签名 `(case, custom_events) -> List[str]`。**漏登记第①处 → 用例落 `_run_pipeline_case` 假绿**（原计划 R6 已踩过）。

### 阶段 A3：host-component 场景（用例 23 / 24）

| 项 | 内容 |
|---|---|
| 前置 | 读 `_stdio_proxy_backend`（§0.3 待核验项）并**据实写用例** |
| 新建 | `cases/23-host-component-slot.json`（同实例接管 storage + history 双插槽）、`cases/24-host-component-reject.json`（快速失败矩阵：未知插槽 / 未知 backend / `kind: branch` 被引用 / `options.command` 与 `options.extension` 同时给出） |
| 修改 | `runner.py`：`elif case_id.startswith("host-component"):` + `_run_host_component_case(...) -> List[str]`；两个 id 登记 |
| 接口消费 | `core.extension_loader.wire_extensions`、`server.host_components.load_host_components`、`core.storage.StorageProvider`、`core.history`/`MessageStore` |
| **修正点（C3）** | manifest 的 `command` **直接写绝对路径**，不写 `{python}/{backend}/{tmp}` 占位符 |
| 观测 | `custom:host_component_result`（`ok`/`slots`/`same_instance`/`storage_isinstance`/`history_isinstance`）、`custom:host_component_reject` |
| 验证 | 两条 `[PASS]`；原有不回归 |
| 变异验证 | 把 `load_host_components` 的"同实例覆盖双插槽"改成各建一个实例 → 用例 23 的 `same_instance` 断言必须变红 |

### 阶段 A4：runner 断言与场景能力（用例 25，WP-B 的前置）

| 项 | 内容 |
|---|---|
| 目标 | 新增 `final.llm_assert`（4 子键）+ `inputs` 新键：`custom_events` / `mutate_snapshot` / `storage_writer` / `session_store` / `core_overrides` / `expect_error_regex` |
| 修改 | `runner.py`：`_SUPPORTED_FINAL_KEYS` 增 `llm_assert`（`runner.py:126`）；`_LLM_ASSERT_KEYS` 常量；`_assert_final` 增 `llm=None` 形参（**C1：既有 2 处调用点无需改动**）；`_run_pipeline_case` 的 `ChatPipeline(...)`（`runner.py:652-658`）支持 `storage_writer` / `session_store` / `core_overrides`；`ScriptedBranch` 增 `mutate_snapshot` 行为（`runner.py:292-392`） |
| 新建 | `cases/25-injection-dedup-budget.json`（T-1：同 key 后声明覆盖先声明） |
| 边界 | `core_overrides` **只覆盖 `ChatPipeline` 构造参数**（`mode`/`injection_budget`/`history_window_messages`/`max_loops`），**不覆盖 `max_*` 资源上限**（那是 `core/snapshot.py` 的模块常量 `RESOURCE_LIMITS`，config 键未接线 —— 原计划 R16） |
| 未识别子键 | `llm_assert` 出现未登记子键 → **显式失败**（防"假绿"重演） |
| 验证 | `[PASS] injection-dedup-25`；`storage_writer=True` 时 `close()` 后断言落盘序（**同步方法，不 await**，C7） |
| 变异验证 | ① `llm_assert` 的 `system_contains` 改成恒 `True` → 用例红；② `mutate_snapshot` 实际能改到 core 状态（去掉只读保护）→ 用例 31（WP-B）应变红 |

### 阶段 A5：批量原子性断言（用例 26）

| 项 | 内容 |
|---|---|
| 新建 | `cases/26-storage-batch-atomic.json`（**修正 C8：用 `write` 批量**） |
| 修改 | `_run_stdio_case` 的 op 循环支持 `expect_error` + 聚合判据 `rejected_non_dict` / `final_rows` |
| 用例语义 | 合法批量 `write("batch_atomic", [{a:1},{a:2}])` → 2 条；含非法元素 `write("batch_atomic", [{a:3}, 123])` → 抛错且**整批回滚**；再 `query("batch_atomic")` → 仍 2 条 |
| 验证 | `[PASS] storage-batch-atomic-26`；套件 **26/26**；`audit_liu2.ps1` exit=0 |
| 变异验证（必做） | 参考后端 `write` 由单事务改逐条提交 → `final_rows` 变 3 → 用例必须红；恢复 |

### 阶段 B（预告，另起一轮执行）

WP-B = **13 条黄金用例**（编号 27–39，映射表见原计划 §3 B1–B4），是 P-1③/P-2③/P-4③/P-5③/P-6③/P-7③ **六条协议入协议条件③的共同卡点**。执行前须：
1. 复核 A4 交付的断言键是否覆盖 B1–B4 的全部 `inputs`/`expected` 用法；
2. B1 前先核验 C15（用例 16 的 `custom_events` 注入通道）；
3. 每条用例**逐一变异验证**（原计划 §3 步骤 3），不变红 = 空断言，必须重写。

---

## §2 门禁与验收

| 检查点 | 命令 | 期望 |
|---|---|---|
| 每阶段 | `D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py` | 全绿（阶段 A2 起逐条增加，A5 收尾 **26/26**） |
| 每阶段 | `powershell -ExecutionPolicy Bypass -File scripts\audit_liu2.ps1` | `exit=0`、pytest ≥ 159 |
| A1 | `pytest teage_liu2/tests_core/test_reference_backend.py -q` | PASS |
| 每条新用例 | 变异验证（退化实现 → 变红 → 恢复） | 必须变红 |
| M2 收尾 | 上述全部 + `PENDING.md` 六条协议条件③勾稽 | 全绿 + 勾稽完成 |

**产物清单（预期）**：新建 1 个参考后端 + 1 个测试文件 + 5 个用例（22–26）；修改 `runner.py`（import/守卫表/`_assert_final`/`_run_pipeline_case`/`ScriptedBranch`/3 个新分支 + 登记表）、`case/suite.schema.json`（`llm_assert` 登记）、`PROTOCOL/README.md`（依赖边界更新）。

---

## §3 风险与回滚

| 风险 | 触发 | 处置 |
|---|---|---|
| runner 引入 `teage_liu2.server` import 使"语言无关交付物"边界松动 | A2 | `README.md` 已建"依赖边界"段，A2 落地时同步补 `server/`；`cases/*.json` + schema 保持零依赖 |
| 参考后端与 Rust 后端语义漂移 | A1/A5 | 同一份 P-4 条款清单双向核对（`storage-rust/src/history.rs` vs 参考后端），差异记入 `PENDING.md` P-4 |
| 子进程用例拖慢套件 | A2/A3/A5 | 子进程用例**控制 ≤6 条**；套件总时长目标 ≤30s |
| 用例 FAIL 暴露 core 真实缺口 | B | **不得放宽断言** → 记入 `teage_liu2/docs/plans/CORE-缺口记录.md`，单独修复后回填 |
| CI（ubuntu）无 `data2/extensions` | A3 | 用例用临时目录自建 manifest（不依赖安装态） |
| Windows GBK 致子进程 UTF-8 解码失败 | A1/A2 | 参考后端三流 `reconfigure(encoding="utf-8")`（P-4 条款⑦） |
| **回滚** | 任一阶段红且 30 分钟内无法定位 | 该阶段改动整体 `git checkout -- <files>` 回退（**须先获用户批准**），保留已完成阶段；不带着红用例进入下一阶段 |

---

## §4 明确不做（沿用原计划 §11）

- 不新增 Rust 代码；不升 `PROTOCOL/VERSION`（只补齐入协议**条件**）；
- 不重构老系统 `teage_liu/`；不新增第三方依赖（含 `pytest-asyncio`）；
- 不做 SessionStore TTL；不改 `config.yaml` 等常驻配置；
- 不动冻结面（12 消息 / 6 Action / 11 钩子）。

---

## §5 自审

- **行号来源**：§0 全部行号来自 2026-09-11 的 code-explorer 实读 + 主 agent 精读，**非引用原计划**（原计划 R8 的"5 处调用点"已在 §0.2 C1 更正）。
- **与 M1 的衔接**：M1 已修 `register_task` 未 await、已建 README"依赖边界"段与"条款编号约定"段 —— A2 依赖前者（无）、复用后者。
- **未决项**：§0.3 的 `_stdio_proxy_backend` 实现细节必须在 A3 前读完；`options.command` 与 `options.extension` 的互斥文案以代码为准（计划文案不作为验收标准）。
