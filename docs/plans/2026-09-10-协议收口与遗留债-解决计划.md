# 协议收口与遗留债 解决计划

> **给执行者:** 按 WP 顺序逐任务实现，每个任务走 TDD 循环（红 → 绿 → 重构 → 提交）。
> 步骤用复选框（`- [ ]`）语法跟踪进度。

> **执行状态（2026-09-11）**：**M1 已完成** = WP-C（P-8 三码发射点）+ WP-D（协议域归档与契约同步）+ WP-G（文档一致性），并**并入**两处同源小修：`InProcessHostPort.register_task/cancel_task` 未 `await` 修复（`core/transport.py`）、`teage_liu2/docs/INTERFACES.md` 的 `cancel_all` 漂移修正。验证：行为套件 **21/21**、pytest **159 passed**、`audit_liu2.ps1` **exit=0**。**M2（WP-A + WP-B）已完成（2026-09-11，见 `docs/plans/2026-09-11-M2行为套件场景能力-执行计划.md`）**：用例 21 → 39、套件 39/39、pytest 165 passed、audit exit=0。**M3 部分执行**（2026-09-11：WP-F 全部 + WP-E 无 `fastapi` 部分已落地；**routes 级锚定 / L-11 端到端仍未执行**）；**M4（WP-H）未执行**。WP-E 步骤 0 的 CI 依赖冲突**已裁决并落地**（`.github/workflows/liu2-audit.yml` 装机行追加 `fastapi httpx`，用户 2026-09-11 批准）。另：`SessionLocks` 边界互斥漏洞已修（`server/session_locks.py`）、`LLM_CANCELED` 改由套件**事件面**锚定（套件错误码并集 12 → 13）。M1 + M2 的逐项复选框均未逐条回填（**0/77 已勾**），**以本条与 `docs/plans/2026-09-11-M2行为套件场景能力-执行计划.md` 为准**（2026-09-11 落地审查更正：原免责仅覆盖 M1）。

**目标:** 把 liu2 全部未办事项（协议收口 8 条 + 协议条款覆盖债 20 条 + 代码发射点缺口 + server/core 测试债 + 运维项）收敛为**可执行、可验证、可复跑**的闭环，使 P-1..P-8 具备转入 `stable` 的全部前置。

**架构:** 以「行为套件（runner）场景与断言能力」为最大杠杆点——一处扩展同时解锁 6 条协议的入协议条件③（P-1/P-2/P-4/P-5/P-6/P-7）。其余工作包按「低风险文档归档 / core 代码发射点 / 测试锚定 / 运维」四类推进，彼此不阻塞，可并行。

**技术栈:** Python 3.11（全链路 async）+ pytest + jsonschema + pyyaml；既有 Rust 后端 `storage-rust/` 本计划**不新增 Rust 代码**。

## 全局约束

- Python 解释器固定 `D:\soft\Python311\python.exe`（`python`/`py` 是 Store stub，不可用）。
- 仓库纯净例外**仅限测试文件**：`teage_liu2/tests_core/`、`teage_liu2/tests_branches/`、`teage_liu2/PROTOCOL/behavior-suite/`。临时调试脚本一律落 `d:/tmp`，用完即删。
- **依赖（已按审查修正）**：本计划主体不新增第三方依赖（`pytest` / `pyyaml` / `jsonschema` / `python-dotenv` 已具备）。**但 WP-E 是例外**：`fastapi.testclient.TestClient` 需要 `fastapi` + `httpx`，而 CI 的 `Install minimal deps` 只装 4 个包（`.github/workflows/liu2-audit.yml:48`）且不安装 `pyproject.toml` 依赖 → 本地绿、**CI 必红**。处置见 §6 任务 E1 的"前置步骤"（修改 workflow 属**配置改动，须先获用户批准**）。
- **冻结面纪律（已按审查修正）**：`suite.schema.json` 中 **`Expected` 是 `additionalProperties: false`**（L95，顶层 `expected.*` 新键会被机检拒绝）；而 **`FinalExpectation`（L71）与 `TestCase.inputs`（L117）都是 `additionalProperties: true`** —— `final.*` / `inputs.*` 新键**不会被 schema 拒绝**，真正拦截者是 runner 守卫表 `_SUPPORTED_*`（`runner.py:123-131`）+ `_check_supported`。故：新增 `expected` 顶层键必须同步 schema；新增 `final.*` / `inputs.*` 键必须同步 runner 守卫表（并建议同步 schema 保持文档一致）。
- **新增用例类型的登记清单（三处缺一不可，否则用例会被误路由到 pipeline 类）**：①用例 id 加入 `runner.py:1308-1313` 的 `protocol_pipeline_ids`；②在 `_run_protocol_case`（`runner.py:811`）内新增 `elif case_id.startswith("<前缀>")` 分支；③分支函数签名对齐 `_run_config_case(case, custom_events) -> List[str]`（`runner.py:1254-1256`）—— **就地 `custom_events.append(...)`、返回 errors 列表，禁止返回 dict**。
- 依赖铁律：`core/` 不 import `branches/`；枝干互不可见；外壳只依赖 core（唯一例外 = composition root 装配点）。
- 钩子内调 LLM 必须直调 `core.llm_client`，**禁止**调 `core.pipeline.chat_stream`。
- git 写操作（commit/push）一律先向用户说明并获批准。
- 文档归档：本计划（跨工作包）落根 `docs/plans/`；liu2 内部变更摘要追加到 `teage_liu2/docs/plans/开发日志.md` 顶部。
- 门禁命令（每个任务收尾必跑）：
  - `D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py`
  - `powershell -ExecutionPolicy Bypass -File scripts\audit_liu2.ps1`（期望 `exit=0`）

---

## §0.0 审查与修订记录（2026-09-10，两轮 plan-verifier + 主 agent 复核）

> 本节记录对本计划的审查结论。**凡标注"已就地修订"的，正文已是修正后版本；执行时以正文为准。**

| # | 原问题（审查发现） | 处置 |
|---|---|---|
| R1 | A1 参考后端 `hello` 返回 `{"protocol_version": "v1.0.0"}`，而宿主读 `r["version"]` 并比对主版本（`storage_stdio_proxy.py:210-217`，`PROTOCOL_VERSION="0.1.0"` 见 L30）→ `proxy.start()` 必抛 | **已就地修订**：hello 改 `{"protocol": "storage-stdio", "version": "0.1.0"}`；测试增版本断言 |
| R2 | A1 的 `dispatch` 引用了 8 个**未定义**方法（`read`/`delete`/`ensure_session`/`log_message`/`update_session_title`/`get_session_title`/`search_messages`/`_insert_message`）→ 首个 op 即 `AttributeError` | **已就地修订**：补齐全部方法 |
| R3 | A1 `write` 单条返回标量，违反其自列的 P-4 条款④（"write 响应恒为 doc_id 数组"） | **已就地修订**：恒返 `ids` 列表 |
| R4 | 线协议载荷不符：`log_message` 应扁平 `{session_id, role, content, ...}`、`log_messages` 应用 `p["messages"]`（原写 `p["item"]`/`p["items"]`） | **已就地修订** |
| R5 | A2/A3 假设的"`inputs.case_type` 分派 + `run_case` 加分支"**不存在**：runner 按 **id 前缀**在 `_run_protocol_case`（`runner.py:811-1027`）分派，分支函数返回 **`List[str]`**、`custom_events` 是 `_run_protocol_case` 内的**局部共享列表**（`runner.py:819`，就地 append） | **已就地修订**：改为「id 登记 `protocol_pipeline_ids`（L1308-1313）+ `_run_protocol_case` 内 `elif` + 返回 errors 列表」 |
| R6 | A2 用例按原文会**静默 PASS**（id 不在协议白名单 → 落 `_run_pipeline_case`，`case_type`/`storage_ops` 被忽略），与"步骤 2 预期 FAIL"自相矛盾 | **已就地修订**：改为可达失败路径，并让 `storage_ops[].assert` 真正被消费 |
| R7 | A3 断言 `sorted(["storage","history"]) == loaded.keys()` 恒 False | **已就地修订**：两侧 `sorted()` |
| R8 | A4 声明 4 个 `llm_assert` 子键却只实现 2 个；`core_overrides`/`mutate_snapshot`/`custom_events`/`session_store` 只声明未实现；`_assert_final` 无 `llm` 形参（`runner.py:533-535`） | **已就地修订**：补齐实现 + 同步 5 处调用点 |
| R9 | A5 原子性用例**未注入非法元素**，非事务实现在该判定式下同样通过（空断言，违反变异验证纪律）；参考后端 `write` 对元素类型无校验 | **已就地修订**：用例真注入非法元素 + 后端显式校验并整批回滚 |
| R10 | 全局约束的 schema 论证错误：`FinalExpectation.additionalProperties` 实为 **`true`**（`suite.schema.json:71`），`false` 的是 `Expected`（L95） | **已就地修订** |
| R11 | WP-E 与"CI 最小依赖集不变"**正面冲突**：CI 只装 `pytest pyyaml jsonschema python-dotenv`（`liu2-audit.yml:48`），不装 fastapi/httpx → 本地绿、**CI 必红** | **已就地修订**：§6 E1 增前置步骤（改 workflow 属配置改动，**须用户批准**），并给出三个备选 |
| R12 | E1 fixture 的 `llm` 段键名不符实际（真实键为 `main_provider`/`main_model`/`main_api_key`，`llm.py:1022-1027`） | **已就地修订** |
| R13 | §4 步骤 5 计划在**用例 20** 加 `TOOL_REJECTED_BY_POLICY` —— 用例 20 的 scenarios 无 reject（reject 在**用例 16**，`runner.py:1115-1122`），加了必然变红 | **已就地修订**：改到用例 16 |
| R14 | §4 步骤 1 测试代码 `HookChain([branch])` 无此构造签名（实为 `HookChain(hook_timeout=...)` + `chain.register(branch)`，`hooks.py:169-175`） | **已就地修订** |
| R15 | B1 `hooks-round-merge-30` 用 `system_regex` 断言轮级注入 —— `inject_round` 进的是 **messages**（`BEFORE_INPUT` 层，`assembler.py:83-89`），跨字段不可表达 | **已就地修订**：system 与 messages 分别断言 |
| R16 | B3 `types-limit-reject-38` 用 `core_overrides` 调 `max_message_bytes` —— 上限是 `core/snapshot.py` 的**模块常量** `RESOURCE_LIMITS`（`snapshot.py:79-96`），config 键**未接线**（`config.py:418-421` 只校验） | **已就地修订**：改为"按协议常量边界构造样本"，并把"config 接线"单列为前置任务 |
| R17 | 数字/表述不一致：§1"≥14 条" vs §10"13 条"；§3 文件区间写到 40（实际 39）；§11"7 钩子"（PROTOCOL 冻结面为 **11 钩子**） | **已就地修订** |
| R18 | A2 步骤 5 的 `proxy._proc` 属性名不存在（子进程句柄在 `proxy._conn._proc`，`storage_stdio_proxy.py:88`） | **已就地修订** |
| R19 | A2/A3/A4 的实现片段用到 `Path`/`copy`/`yaml`，而 runner 模块头**未 import** 它们（`runner.py:23-33`） | **已就地修订**：A4 增 import 补充步骤 |

**审查结论**：**未发现编造的符号或路径** —— §0 总账抽查 17 条代码事实全部属实（含用例 17 description 确实声称"防重入/并发上限"）。问题集中在 **WP-A 的接口形态假设**（R1-R9）与 **WP-E 的 CI 依赖**（R11），修订后可执行。审查同时确认：`html 冻结面为 11 钩子 / 12 消息 / 6 Action`（`PROTOCOL/README.md:14`）。

## §0 未办事项总账（2026-09-10 代码事实核查结论）

| 编号 | 事项 | 代码事实（核查证据） | 性质 | 归属 |
|---|---|---|---|---|
| 1 | P-1..P-8 全部未入协议 | `PENDING.md`：P-1/P-2/P-4/P-5/P-6/P-7/P-8 = experimental，P-3 = proposed，**merged 0** | 真债 | WP-A/B/D |
| 2 | runner 无真实子进程 stdio 场景 | `runner.py` 全文 0 处 `subprocess`/`create_subprocess_exec`/`StdioChannel` | 真债·杠杆 | WP-A |
| 3 | runner 无 host-component 场景 | `runner.py` 未 import `server/host_components.py`，未调 `load_host_components`；用例 17 只对拍 `TransportBus` 进程内 host_port | 真债·杠杆 | WP-A |
| 4 | runner 无 StorageWriter 链路 | `runner.py:657/1146/1207` 三处 `storage_writer=None` 硬编码；`core/storage_writer.py` 在 runner 0 引用 | 真债 | WP-A |
| 5 | runner 无法断言批量原子性 | `runner.py:873-876` 批量只断言返回 id 的 `length`，无回滚/部分失败断言 | 真债 | WP-A |
| 6 | 协议条款覆盖缺口 20 条 | 逐域勾稽：hooks 缺 H-3/H-8/H-9/H-16/H-17/H-19；events 缺 E-2/E-4/E-8/E-9；lifecycle 缺 L-5/L-6/L-10/L-11；types 缺 T-1/T-3/T-5/T-7/T-8；config 缺 C-2 | 真债 | WP-B/F/E |
| 7 | P-8 三码无发射点 | `TOOL_EXEC_FAILED`/`TOOL_REJECTED_BY_POLICY`/`TOOL_MODIFY_INVALID` 仅 `core/errors.py:51-53` 常量 + `hooks.py` docstring；三处实际路径无码前缀 | 真债（小） | WP-C |
| 8 | `MessageStore` ABC 签名漂移 | `core/storage.py:233` `get_session_messages(self, session_id, limit=None)` **缺 `before_id`**；`core/history.py:59-64` 与 `SQLiteHistoryStore` 已是三参 | 真债（小） | WP-D |
| 9 | manifest 规范未入协议域文件 | `PROTOCOL/lifecycle/` 仅 `lifecycle.spec.md`/`.schema.json`；规范仍记 `docs/SUBSYSTEM-SPI.md §13.2` | 真债（文档） | WP-D |
| 10 | host_components 契约未入 config 域 | `PROTOCOL/config/config.spec.md` 无 `host_components` 段；`config.schema.json` 无对应 schema | 真债（文档） | WP-D |
| 11 | 三个资源上限键无 PENDING 条目 | `config.spec.md:32` 自述："三个资源上限键**尚无 PENDING 条目**" | 真债（登记） | WP-D |
| 12 | server 层零测试锚定 | 全仓 `teage_liu2.server` 在 `*.py` 测试中 0 命中；`tests_core`/`tests_branches` 无 `TestClient`/`httpx` | 真债 | WP-E |
| 13 | 4 个 route/pipeline 生产修复无回归防护 | 修复已入库（`154e6ae`），但无自动化断言 | 真债 | WP-E |
| 14 | 6 个 core 模块零测试 | `assembler`/`modes`/`step`/`remote_adapter`/`storage_writer`/`supervisor` 无测试文件 import | 真债 | WP-F |
| 15 | 用例 17 描述与实现不符 | 用例 description 声称含"防重入/并发上限"，`runner.py:865-904` 未实现该检查 | 真债（一致性） | WP-G |
| 16 | transport 与 types 域 `T-*` 编号冲突 | 两域均用 `T-1..T-N`，跨域引用歧义 | 缺陷（排版） | WP-G |
| 17 | 前端 fail-closed 未浏览器实测 | `teage_liu2/web/js/app.js:27` 代码已就位（DOMPurify 缺失 → `escapeHtml`） | 欠实测 | WP-H |
| 18 | 两系统配置层残留耦合 | 库已因 `host_components` 接管分离（`data2/storage_rust/sessions.db`），配置仍共用根 `config.yaml` | 待裁决（涉配置） | WP-H |
| 19 | 运维残留 | `d:/tmp/liu2-7878.r2.*.log` 被运行进程占用；Rust 库含 2 个 e2e 会话（用户已裁决保留） | 运维 | WP-H |
| 20 | SessionStore 无 TTL | `CORE-缺口记录.md` 定性为**外壳责任**、非 core 缺口 | 登记项 | §4 不做 |

> **已消解（不必再办）**：①`final.persisted` / `invocations[].isolation` 断言**已实现**（`runner.py:533-559`、`_EphemeralStorage` 分 `direct`/`buffered`）；②storage 域 S-1 落盘时机已由用例 18 锚定；③`web/static/js/utils.js:58` 的 fail-open 属**老系统冻结前端**，liu2 不受其约束。

> **2026-09-11 更新（第 6 项）**：未覆盖条款 **20 → 6 条**（T-8 已实质满足移出）；这 6 条**测试已就位**（`tests_core/test_snapshot_identity.py` / `test_supervisor.py` / `test_session_extra_continuity.py` / `test_session_locks.py`），**逐条限定语见 §13**；**routes 级锚定（WP-E·E1/E4）已完成**（新增 `tests_core/test_server_routes.py`：E1 四个生产修复 + E4 L-11 路由层真持锁；§0 第 12 项"server 层零测试"随之闭合）。

---

## §1 工作包与依赖

```
WP-A 场景与断言能力扩展 (P0)  ──┬─→ WP-B 协议条款补用例 (P0)
                                └─→ WP-E server 测试锚定 (P1，复用参考后端)
WP-C 三码发射点 (P1，独立)
WP-D 协议域文件归档 (P1，独立)
WP-F core 模块锚定 (P2，独立)
WP-G 文档一致性修补 (P2，独立)
WP-H 运维与实测 (P3，独立)
```

| 工作包 | 目标 | 前置 | 估算 | 里程碑 |
|---|---|---|---|---|
| WP-A | runner 可表达真实子进程 stdio / host-component / StorageWriter / 批量原子性 + 新增 4 类断言能力 | 无 | 大（主体） | M2 |
| WP-B | 20 条未覆盖条款 → 套件侧新增 **13 条**黄金用例（T-1 由 WP-A 的 case 25/38 承担；T-3/T-5/T-8 与 L-5/L-6 归单测、L-10/L-11 归 server 测试） | WP-A | 大 | M2 |
| WP-C | 三码落地发射点 + 断言 + 契约回流 | 无 | 小 | M1 |
| WP-D | manifest 规范迁入 lifecycle 域 / host_components 入 config 域 / 资源上限键登记 / ABC 签名同步 | 无 | 小 | M1 |
| WP-E | routes / host_components / stdio-proxy 测试锚定（含 L-10/L-11） | WP-A（参考后端） | 中 | M3 |
| WP-F | 6 个 core 模块测试锚定（含 T-3/T-5 条款） | 无 | 中 | M3 |
| WP-G | 编号歧义、用例描述一致性、README 纪律 | 无 | 小 | M1 |
| WP-H | 前端浏览器实测、日志/数据清理、配置解耦立项 | 无 | 小 | M4 |

---

## §2 WP-A 行为套件场景与断言能力扩展（P0）

**文件:**
- 新建: `teage_liu2/PROTOCOL/behavior-suite/tools/reference_storage_backend.py`
- 修改: `teage_liu2/PROTOCOL/behavior-suite/runner.py`
- 修改: `teage_liu2/PROTOCOL/behavior-suite/suite.schema.json`
- 修改: `teage_liu2/PROTOCOL/README.md`

**接口:**
- 消费: `server.host_components.load_host_components(cfg, specs) -> Dict[str, Any]`；`core.extension_loader.wire_extensions(cfg) -> (cfg, specs, disabled)`；`server.storage_stdio_proxy.StdioStorageProxy(command, request_timeout)`；`core.storage_writer.StorageWriter()`；`core.storage.SQLiteStorageProvider(path)`
- 产出: `final.llm_assert`（本 WP 唯一新增的 `final` 断言键）+ `inputs` 新键（`storage_ops` / `custom_events` / `mutate_snapshot` / `storage_writer` / `session_store` / `core_overrides` / `expect_error_regex`）+ 两个协议层分支前缀（`stdio-proxy` / `host-component`）。观测结果统一经 `custom:*` 事件 + `expected.event_stream` 断言（与既有 `custom:host_port_result` 模式一致），供 WP-B 的用例消费

### 任务 A1: 落地 P-4 参考存储后端（语言无关交付物）

**设计决策（需在 README 记录）:** 参考后端**不 import `teage_liu2.core`**，仅用标准库 `sqlite3` 自建表，以保证"任意语言按 P-4 可实现"的可信度；它与 Rust 后端构成"同一协议的两个独立实现"，互相印证。

- [ ] **步骤 1: 写失败测试**

```python
# teage_liu2/tests_core/test_reference_backend.py
import json
import subprocess
import sys
from pathlib import Path

BACKEND = Path(__file__).resolve().parents[1] / "PROTOCOL" / "behavior-suite" / "tools" / "reference_storage_backend.py"


def test_hello_and_bye_roundtrip(tmp_path):
    db = tmp_path / "ref.db"
    p = subprocess.Popen(
        [sys.executable, str(BACKEND), "--db", str(db)],
        stdin=subprocess.PIPE, stdout=subprocess.PIPE, stderr=subprocess.PIPE,
        text=True, encoding="utf-8",
    )
    p.stdin.write(json.dumps({"id": 1, "op": "hello",
                              "p": {"protocol": "storage-stdio", "version": "0.1.0"}}) + "\n")
    p.stdin.flush()
    hello = json.loads(p.stdout.readline())
    assert hello["ok"] is True and hello["id"] == 1
    # 宿主握手契约（审查 R1）：StdioStorageProxy 读 r["version"] 并比对主版本，
    # 见 storage_stdio_proxy.py:210-217；PROTOCOL_VERSION="0.1.0" 在同文件 L30。
    assert hello["r"]["version"].split(".")[0] == "0"
    p.stdin.write(json.dumps({"id": 2, "op": "bye", "p": {}}) + "\n")
    p.stdin.flush()
    assert json.loads(p.stdout.readline())["ok"] is True
    assert p.wait(timeout=5) == 0
```

- [ ] **步骤 2: 跑测试确认它失败**

运行: `D:\soft\Python311\python.exe -m pytest teage_liu2/tests_core/test_reference_backend.py -q`
预期: FAIL（文件不存在 / `FileNotFoundError`）

- [ ] **步骤 3: 写最简实现**

参考后端必须实现 PENDING P-4 的**全部**语义条款：①kind 白名单 `^[a-z0-9_.]+$` ②`query` filters 先于 limit、按写入序 ③批量 `docs[]` 同事务原子 ④`write` 响应恒为 doc_id 数组 ⑤`search_messages` 子串匹配 ⑥单进程串行应答 ⑦三流 UTF-8 ⑧`bye` 后退出 ⑨不声明 `FOREIGN KEY` ⑩`WAL` + `busy_timeout=5000`、`synchronous` 保持 FULL。

```python
"""P-4 storage-stdio 线协议参考后端（独立实现，不依赖 teage_liu2.core）。

用途：①协议语言无关性的可执行参照 ②behavior-suite 真实子进程场景的被测进程。
启动：python reference_storage_backend.py --db <path>
"""
from __future__ import annotations

import json
import sqlite3
import sys
import uuid
from datetime import datetime
from pathlib import Path

KIND_OK = set("abcdefghijklmnopqrstuvwxyz0123456789_.")


def _now() -> str:
    return datetime.now().isoformat()


class RefBackend:
    def __init__(self, db_path: str) -> None:
        Path(db_path).parent.mkdir(parents=True, exist_ok=True)
        self.conn = sqlite3.connect(db_path)
        self.conn.row_factory = sqlite3.Row
        self.conn.execute("PRAGMA journal_mode=WAL;")
        self.conn.execute("PRAGMA busy_timeout=5000;")
        self._ensure_base_tables()

    def _ensure_base_tables(self) -> None:
        self.conn.executescript(
            """
            CREATE TABLE IF NOT EXISTS sessions (
                id TEXT PRIMARY KEY, created_at TEXT NOT NULL, updated_at TEXT NOT NULL,
                title TEXT
            );
            CREATE TABLE IF NOT EXISTS messages (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                session_id TEXT NOT NULL, role TEXT NOT NULL, content TEXT NOT NULL,
                tool_name TEXT, tool_call_id TEXT, token_count INTEGER DEFAULT 0,
                is_error INTEGER DEFAULT 0, reasoning TEXT,
                content_blocks TEXT, message_type TEXT, created_at TEXT NOT NULL
            );
            """
        )
        self.conn.commit()

    # ---- docs 通道 ----
    def write(self, kind: str, docs) -> list:
        self._check_kind(kind)
        items = docs if isinstance(docs, list) else [docs]
        if not items:
            raise ValueError("批量写入 docs 不能为空列表")
        for d in items:                       # 审查 R9：显式元素校验（原子性用例依赖）
            if not isinstance(d, dict):
                raise ValueError(f"docs 元素必须是对象, 实际 {type(d).__name__}")
        table = f"doc_{kind.replace('.', '_')}"
        self.conn.execute(
            f"CREATE TABLE IF NOT EXISTS {table} (doc_id TEXT PRIMARY KEY, doc TEXT NOT NULL, created_at TEXT NOT NULL)"
        )
        ids = [uuid.uuid4().hex for _ in items]
        with self.conn:  # 条款③：单事务——任一失败整批回滚
            for doc_id, doc in zip(ids, items):
                self.conn.execute(
                    f"INSERT INTO {table} (doc_id, doc, created_at) VALUES (?, ?, ?)",
                    (doc_id, json.dumps(doc, ensure_ascii=False), _now()),
                )
        return ids                            # 条款④：恒返数组（不因单条而返回标量）

    def read(self, kind: str, doc_id: str):
        self._check_kind(kind)
        row = self.conn.execute(
            f"SELECT doc FROM doc_{kind.replace('.', '_')} WHERE doc_id = ?", (doc_id,)
        ).fetchone()
        return json.loads(row["doc"]) if row else None

    def delete(self, kind: str, doc_id: str):
        self._check_kind(kind)
        with self.conn:
            self.conn.execute(
                f"DELETE FROM doc_{kind.replace('.', '_')} WHERE doc_id = ?", (doc_id,)
            )
        return None

    def query(self, kind, limit=None, **filters):
        """条款②⑧：filters 过滤先于 limit；按 rowid 写入序返回。"""
        self._check_kind(kind)
        rows = [json.loads(r["doc"]) for r in self.conn.execute(
            f"SELECT doc FROM doc_{kind.replace('.', '_')} ORDER BY rowid ASC")]
        rows = [d for d in rows if all(d.get(k) == v for k, v in filters.items())]
        return rows[:limit] if limit is not None else rows

    # ---- sessions / messages 通道 ----
    def ensure_session(self, session_id: str):
        with self.conn:
            self.conn.execute(
                "INSERT OR IGNORE INTO sessions (id, created_at, updated_at) VALUES (?, ?, ?)",
                (session_id, _now(), _now()),
            )
        return None

    def _insert_message(self, session_id: str, item: dict) -> int:
        blocks = item.get("content_blocks")
        cur = self.conn.execute(
            "INSERT INTO messages (session_id, role, content, tool_name, tool_call_id,"
            " token_count, is_error, reasoning, content_blocks, message_type, created_at)"
            " VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)",
            (session_id, item.get("role"), item.get("content") or "",
             item.get("tool_name"), item.get("tool_call_id"),
             int(item.get("token_count") or 0), 1 if item.get("is_error") else 0,
             item.get("reasoning"),
             json.dumps(blocks, ensure_ascii=False) if blocks is not None else None,
             item.get("message_type"), _now()),
        )
        self.conn.execute(
            "UPDATE sessions SET updated_at = ? WHERE id = ?", (_now(), session_id)
        )
        return int(cur.lastrowid)

    def log_message(self, **p):
        """条款：载荷为**扁平 dict**（宿主 proxy 把 kwargs 直接作为 p 发送）。

        实现前先读 `storage_stdio_proxy.py` 的 `log_message` 分支（约 L412-418）
        核对键名全集，勿凭想象增删。
        """
        session_id = p.pop("session_id")
        with self.conn:
            return self._insert_message(session_id, p)

    def log_messages(self, session_id: str, items: list) -> list:
        """P-7 批量写：单事务原子，任一元素非法整批回滚。

        载荷形态 = `p["messages"]`（宿主 proxy 约 L484）；元素结构与 log_message 同。
        """
        for it in items:
            if not isinstance(it, dict):
                raise ValueError(f"messages 元素必须是对象, 实际 {type(it).__name__}")
        ids = []
        with self.conn:
            for it in items:
                ids.append(self._insert_message(session_id, it))
        return ids

    def update_session_title(self, session_id: str, title: str):
        with self.conn:
            self.conn.execute(
                "UPDATE sessions SET title = ?, updated_at = ? WHERE id = ?",
                (title, _now(), session_id),
            )
        return None

    def get_session_title(self, session_id: str):
        row = self.conn.execute(
            "SELECT title FROM sessions WHERE id = ?", (session_id,)
        ).fetchone()
        return row["title"] if row else None

    def search_messages(self, keyword: str, limit=None):
        """条款⑤：子串匹配即可（FTS 是 SQLite 实现细节，非契约）。"""
        sql = ("SELECT * FROM messages WHERE content LIKE ? ORDER BY id ASC")
        args = [f"%{keyword}%"]
        if limit is not None:
            sql += " LIMIT ?"
            args.append(limit)
        return [dict(r) for r in self.conn.execute(sql, args)]

    def dispatch(self, op: str, p: dict):
        table = {
            # 审查 R1：宿主读 r["version"] 并比对主版本（storage_stdio_proxy.py:210-217）
            "hello": lambda: {"protocol": "storage-stdio", "version": "0.1.0"},
            "bye": lambda: None,
            "write": lambda: self.write(p["kind"], p["docs"]),
            "read": lambda: self.read(p["kind"], p["doc_id"]),
            "query": lambda: self.query(p["kind"], p.get("limit"), **(p.get("filters") or {})),
            "delete": lambda: self.delete(p["kind"], p["doc_id"]),
            "ensure_session": lambda: self.ensure_session(p["session_id"]),
            "log_message": lambda: self.log_message(**p),
            "log_messages": lambda: self.log_messages(p["session_id"], p["messages"]),
            "update_session_title": lambda: self.update_session_title(p["session_id"], p["title"]),
            "get_session_title": lambda: self.get_session_title(p["session_id"]),
            "get_session_messages": lambda: self.get_session_messages(
                p["session_id"], p.get("limit"), p.get("before_id")),
            "search_messages": lambda: self.search_messages(p["keyword"], p.get("limit")),
        }
        if op not in table:
            raise ValueError(f"未知 op: {op!r}")
        return table[op]()

    @staticmethod
    def _check_kind(kind: str) -> None:
        """条款①：kind 白名单 ^[a-z0-9_.]+$（宿主先行校验，此处为双重防线）。"""
        if not kind or any(c not in KIND_OK for c in kind):
            raise ValueError(f"非法 kind: {kind!r}")

    def get_session_messages(self, session_id, limit=None, before_id=None):
        """条款：before_id 游标向上翻页——id < before_id 取更早，取最近 N 条后反转成正序。"""
        sql = "SELECT * FROM messages WHERE session_id = ?"
        args = [session_id]
        if before_id is not None:
            sql += " AND id < ?"
            args.append(before_id)
        sql += " ORDER BY id DESC"
        if limit is not None:
            sql += " LIMIT ?"
            args.append(limit)
        rows = [dict(r) for r in self.conn.execute(sql, args)]
        for r in rows:
            if r.get("content_blocks"):
                r["content_blocks"] = json.loads(r["content_blocks"])
            r["is_error"] = bool(r["is_error"])
        return list(reversed(rows))


def _parse_db_arg(argv):
    if "--db" not in argv:
        sys.stderr.write("用法: reference_storage_backend.py --db <path>\n")
        raise SystemExit(2)
    return argv[argv.index("--db") + 1]


def main() -> int:
    # 条款⑦：三流 UTF-8（Windows 默认 GBK 会让含中文的帧解码崩溃）
    sys.stdin.reconfigure(encoding="utf-8")
    sys.stdout.reconfigure(encoding="utf-8")
    sys.stderr.reconfigure(encoding="utf-8")
    backend = RefBackend(_parse_db_arg(sys.argv[1:]))
    try:
        for line in sys.stdin:                      # 条款⑥：单进程串行，一请求一响应
            line = line.strip()
            if not line:
                continue
            req = json.loads(line)
            try:
                resp = {"id": req.get("id"), "ok": True,
                        "r": backend.dispatch(req["op"], req.get("p") or {})}
            except Exception as e:
                resp = {"id": req.get("id"), "ok": False, "e": str(e)}
            sys.stdout.write(json.dumps(resp, ensure_ascii=False) + "\n")
            sys.stdout.flush()
            if req.get("op") == "bye":              # 条款⑧：bye 后退出
                break
    finally:
        backend.conn.close()
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **步骤 4: 跑测试确认通过**

运行: `D:\soft\Python311\python.exe -m pytest teage_liu2/tests_core/test_reference_backend.py -q`
预期: PASS

- [ ] **步骤 5: 用 Rust 后端反向校验语义一致性**

运行: `D:\soft\Python311\python.exe -m pytest teage_liu2/tests_core/test_reference_backend.py -q -k batch -v`
预期: PASS（批量原子性用例对参考后端成立）；同时人工核对 `storage-rust/src/history.rs` 的同类断言，确认两实现对 P-4 条款理解一致。

### 任务 A2: runner 支持真实子进程 stdio 场景

**接口:**
- 消费: `from teage_liu2.server.storage_stdio_proxy import StdioStorageProxy`（runner 是测试工具，允许依赖宿主实现——须在 `PROTOCOL/README.md` 显式声明）
- 产出: 协议层分支前缀 `stdio-proxy`；新增 `inputs.storage_ops[]` 形态 = `{op, args?, kwargs?, assert?}`

**前置（审查 R5/R6）:** runner 的协议层分派是 `_run_protocol_case`（`runner.py:811`）+ `protocol_pipeline_ids` 白名单（`runner.py:1308-1313`），**不存在** `inputs.case_type` 分派。新场景必须"先登记 id，再跑，才可能得到预期的失败"。

- [ ] **步骤 1: 写用例**

```json
// teage_liu2/PROTOCOL/behavior-suite/cases/22-stdio-proxy-roundtrip.json
{
  "id": "stdio-proxy-roundtrip-22",
  "protocol_version": "v1.0.0",
  "description": "P-4 条款①③④⑥⑦⑧: 真实子进程 stdio 后端 spawn→hello→双通道读写→bye 全链（参考后端）。",
  "setup": { "extensions": [] },
  "inputs": {
    "session_id": "s-22",
    "storage_ops": [
      { "op": "ensure_session", "args": ["s-22"] },
      { "op": "log_message", "kwargs": { "session_id": "s-22", "role": "user", "content": "你好" } },
      { "op": "get_session_messages", "args": ["s-22"] }
    ]
  },
  "expected": {
    "event_stream": [
      { "type": "custom:stdio_result",
        "payload": { "handshake_ok": true, "op_count": 3, "child_pid_gt_0": true,
                     "message_count": 1, "has_more_than_one_proc": false } }
    ],
    "final": { "is_complete": true }
  }
}
```

- [ ] **步骤 2: 登记用例 id 并跑测试确认它失败**

在 `runner.py:1308-1313` 的 `protocol_pipeline_ids` 集合中加入 `"stdio-proxy-roundtrip-22"`，然后运行:

运行: `D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py`
预期: FAIL（`未知用例类型: stdio-proxy-roundtrip-22`，来自 `_run_protocol_case` 的 else 分支 `runner.py:1026-1027`）

> **注意（审查 R6）**：**不要**跳过登记这一步。若 id 不在白名单，用例会落到 `_run_pipeline_case`，`storage_ops` 被静默忽略并**假绿通过**——这正是套件纪律要防的场景。

- [ ] **步骤 3: 写最简实现**

在 `_run_protocol_case` 的 `config-domain` 分支之后（`runner.py:1023-1024`）新增一行分派，并实现分支函数（**签名与返回契约对齐 `_run_config_case`**：就地 append `custom_events`、返回 `List[str]`）：

```python
    elif case_id.startswith("stdio-proxy"):
        errors += await _run_stdio_case(case, custom_events)
```

```python
async def _run_stdio_case(case: Dict[str, Any],
                          custom_events: List[Dict[str, Any]]) -> List[str]:
    """P-4 真实子进程场景：spawn 参考后端 → 逐 op 调用 → bye 关闭。

    返回 errors 列表（同 runner.py:1254-1256 `_run_config_case` 契约）；
    观测结果经 `custom:stdio_result` 事件交给 expected.event_stream 断言。
    """
    from teage_liu2.server.storage_stdio_proxy import StdioStorageProxy

    errors: List[str] = []
    inputs = case.get("inputs") or {}
    expected = case.get("expected") or {}
    backend = Path(__file__).resolve().parent / "tools" / "reference_storage_backend.py"
    with tempfile.TemporaryDirectory(prefix="bs_stdio_") as td:
        proxy = StdioStorageProxy(
            command=[sys.executable, str(backend), "--db", str(Path(td) / "ref.db")],
            request_timeout=10.0,
        )
        proxy.start()                     # 握手失败/主版本不符 → 抛（R1 修复后不再抛）
        try:
            proc = getattr(getattr(proxy, "_conn", None), "_proc", None)
            pid = int(getattr(proc, "pid", 0) or 0)
            results: List[Dict[str, Any]] = []
            for spec in inputs.get("storage_ops") or []:
                fn = getattr(proxy, spec["op"])
                value = fn(*spec.get("args") or [], **(spec.get("kwargs") or {}))
                want = spec.get("assert")        # 审查 R6：真正消费 storage_ops[].assert
                if want is not None and not match_value(want, value):
                    errors.append(
                        f"op {spec['op']} 断言失败: expect={want} actual={value!r}"
                    )
                results.append({"op": spec["op"], "value": value})
        finally:
            proxy.close()
    custom_events.append({
        "type": "custom:stdio_result",
        "payload": {
            "handshake_ok": True,
            "op_count": len(results),
            "child_pid_gt_0": pid > 0,
            "message_count": sum(
                len(e["value"]) for e in results if e["op"] == "get_session_messages"
            ),
            "has_more_than_one_proc": False,
        },
    })
    errors += _assert_event_stream(expected.get("event_stream") or [], custom_events)
    errors += _assert_final(
        expected.get("final") or {},
        {"type": EV_DONE, "is_complete": True},   # 协议层无真实 done 事件：按 storage 分支范式合成
        [],
        None,
    )
    return errors
```

- [ ] **步骤 4: 跑测试确认通过**

运行: `D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py`
预期: `[PASS] stdio-proxy-roundtrip-22`

- [ ] **步骤 5: 变异验证（防假绿，必做）**

① 把 `proxy.start()` 注释掉 → 用例必须变红（子进程未起）；② 把 `payload.child_pid_gt_0` 恒置 `True`（去掉 `pid > 0` 依赖）→ 用例应变红（证明该断言承重）。两次均恢复后再进入下一步。

> 注：子进程句柄在 `proxy._conn._proc`（`storage_stdio_proxy.py:88`），**没有** `proxy._proc` 这个属性（审查 R18）。

### 任务 A3: runner 支持 host-component 场景

**接口:**
- 消费: `teage_liu2.core.extension_loader.wire_extensions(cfg)`、`teage_liu2.server.host_components.load_host_components(cfg, specs)`
- 产出: 协议层分支前缀 `host-component`；`inputs.manifest` / `inputs.expect` / `inputs.expect_error_regex`

- [ ] **步骤 1: 写用例**

```json
// teage_liu2/PROTOCOL/behavior-suite/cases/23-host-component-slot.json
{
  "id": "host-component-slot-23",
  "protocol_version": "v1.0.0",
  "description": "P-5/P-6: host-component 经 manifest 目录发现接管 storage+history 双插槽,同一实例覆盖双槽。",
  "setup": { "extensions": [] },
  "inputs": {
    "manifest": {
      "name": "ref_storage", "version": "1.0.0", "language": "other",
      "transport": "stdio", "command": ["{python}", "{backend}", "--db", "{tmp}/ref.db"],
      "kind": "host-component", "slots": ["storage", "history"], "capabilities": []
    },
    "expect": { "slots": ["storage", "history"] }
  },
  "expected": {
    "event_stream": [
      { "type": "custom:host_component_result",
        "payload": { "ok": true, "slots": ["history", "storage"], "same_instance": true,
                     "storage_isinstance": true, "history_isinstance": true } }
    ],
    "final": { "is_complete": true }
  }
}
```

> manifest 约束（`extension_loader.py:126-131`）：host-component 必须 `language: other` + `transport: stdio` + 非空 `slots` + **`capabilities: []`**；`name` 必须与目录名一致。

- [ ] **步骤 2: 登记用例 id 并跑测试确认它失败**

在 `runner.py:1308-1313` 的 `protocol_pipeline_ids` 加入 `"host-component-slot-23"`。

运行: `D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py`
预期: FAIL（`未知用例类型: host-component-slot-23`）

- [ ] **步骤 3: 写最简实现**

在 `_run_protocol_case` 的 `stdio-proxy` 分支之后新增：

```python
    elif case_id.startswith("host-component"):
        errors += await _run_host_component_case(case, custom_events)
```

```python
async def _run_host_component_case(case: Dict[str, Any],
                                   custom_events: List[Dict[str, Any]]) -> List[str]:
    """P-5/P-6:临时 extensions_root + manifest → wire_extensions → load_host_components。"""
    from teage_liu2.core.extension_loader import wire_extensions
    from teage_liu2.core.history import HistoryStore
    from teage_liu2.core.storage import MessageStore, StorageProvider
    from teage_liu2.server.host_components import load_host_components

    errors: List[str] = []
    inputs = case.get("inputs") or {}
    expected = case.get("expected") or {}
    manifest = copy.deepcopy(inputs["manifest"])
    with tempfile.TemporaryDirectory(prefix="bs_hc_") as td:
        ext_dir = Path(td) / "extensions" / manifest["name"]
        ext_dir.mkdir(parents=True)
        manifest["command"] = [
            t.format(python=sys.executable,
                     backend=str(Path(__file__).resolve().parent / "tools"
                                 / "reference_storage_backend.py"),
                     tmp=td)
            for t in manifest["command"]
        ]
        (ext_dir / "manifest.yaml").write_text(
            yaml.safe_dump(manifest, allow_unicode=True), encoding="utf-8")
        cfg = {
            "core": {"extensions_root": str(Path(td) / "extensions")},
            "host_components": [{
                "slot": "storage", "backend": "stdio-proxy",
                "options": {"extension": manifest["name"],
                            "args": ["--db", str(Path(td) / "ref.db")]},
            }],
        }
        try:
            cfg, specs, _ = wire_extensions(cfg)
            loaded = load_host_components(cfg, specs)
        except Exception as e:
            errors.append(f"host_components 装载失败: {type(e).__name__}: {e}")
            return errors
        proxy = loaded["storage"]
        try:
            # 审查 R7:两侧都排序后比较(原写法按字面比较恒为 False)
            want_slots = sorted(inputs["expect"]["slots"])
            got_slots = sorted(loaded.keys())
            custom_events.append({
                "type": "custom:host_component_result",
                "payload": {
                    "ok": want_slots == got_slots,
                    "slots": got_slots,
                    "same_instance": loaded.get("history") is proxy,   # P-5 同实例覆盖双槽
                    "storage_isinstance": isinstance(proxy, StorageProvider),
                    "history_isinstance": isinstance(proxy, (HistoryStore, MessageStore)),
                },
            })
        finally:
            proxy.close()
    errors += _assert_event_stream(expected.get("event_stream") or [], custom_events)
    errors += _assert_final(expected.get("final") or {},
                            {"type": EV_DONE, "is_complete": True}, [], None)
    return errors
```

> **import 前置（审查 R19）**：runner 模块头（`runner.py:23-33`）**没有** `pathlib` / `copy` / `yaml`。本任务须在 import 段补 `from pathlib import Path`、`import copy`、`import yaml`（`yaml` 已是项目依赖且属 CI 最小依赖集）。

- [ ] **步骤 4: 跑测试确认通过**

运行: `D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py`
预期: `[PASS] host-component-slot-23`

- [ ] **步骤 5: 补快速失败矩阵用例**

新增 `cases/24-host-component-reject.json`：用 `inputs.expect_error_regex` 对四种非法配置各断言一次异常文案 —— ①`slot: "nope"`（未知插槽）②`backend: "nope"`（未注册）③manifest `kind: branch` 被 `host_components` 引用 ④`options.command` 与 `options.extension` 同时给出。实现里逐项 `try/except` 捕获 `(ValueError, TypeError)`，用 `re.search(expect_error_regex, str(e))` 断言，结果写进 `custom:host_component_reject` 事件供 `event_stream` 匹配。

### 任务 A4: runner 新增断言与场景能力（WP-B 的前置）

**接口（产出，WP-B 的用例按此写）:**
- `expected.final.llm_assert`: `{ "system_contains": str, "system_regex": str, "messages_roles": [str], "message_count": int }` — 对 `FakeLLMClient.last_system` / `last_messages` 断言；**未识别的子键必须显式失败**（防"假绿"重演）
- `inputs.custom_events`: `[{type, payload}]` — 由协议层分支就地注入 `custom_events`（E-2/E-9 用）
- `inputs.mutate_snapshot`: `bool` — `ScriptedBranch` 在钩子内尝试**原地**改写 `snapshot.messages` / `snapshot.extra`（H-17/T-3 用）
- `inputs.storage_writer`: `bool` — 为真时构造 `StorageWriter()` 传入 `ChatPipeline`，收尾 `close()` 后断言落盘序（同时解决 §0 第 4 项）
- `inputs.session_store`: `bool` — 为真时构造 `SessionStore()` 传入 pipeline（§6 E4 的 L-10 用例用）
- `inputs.core_overrides`: `{key: value}` — **仅**覆盖 `ChatPipeline` 构造参数（`mode` / `injection_budget` / `history_window_messages` / `max_loops`）；**不覆盖 `max_*` 资源上限**（那是 `core/snapshot.py` 的模块常量 `RESOURCE_LIMITS`，config 键未接线 —— 审查 R16）

- [ ] **步骤 1: 写用例**

```json
// cases/25-injection-dedup-budget.json（同时也是 WP-B 的 T-1 用例）
{
  "id": "injection-dedup-25",
  "protocol_version": "v1.0.0",
  "description": "types T-1: 同 key 后声明覆盖先声明（层内去重）。",
  "setup": { "extensions": [
    { "name": "ext_a", "lang": "python", "transport": "in-process", "protocol_version": "v1.0.0",
      "hooks_implemented": ["build_injections"], "capabilities": [] },
    { "name": "ext_b", "lang": "python", "transport": "in-process", "protocol_version": "v1.0.0",
      "hooks_implemented": ["build_injections"], "capabilities": [] }
  ] },
  "inputs": {
    "session_id": "s-25", "user_input": "hi",
    "ext_a_injections": [{ "layer": "PREFIX", "content": "AAA", "key": "dup" }],
    "ext_b_injections": [{ "layer": "PREFIX", "content": "BBB", "key": "dup" }]
  },
  "expected": { "final": { "llm_assert": { "system_contains": "BBB" },
                           "termination_reason": "normal" } }
}
```

- [ ] **步骤 2: 跑测试确认它失败**

运行: `D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py`
预期: FAIL（`llm_assert` 不在 `_SUPPORTED_FINAL_KEYS`，被 `_check_supported` 显式拒绝 —— **注意是 runner 守卫拦的，不是 schema**，见审查 R10）

- [ ] **步骤 3: 写最简实现**

```python
# 1) runner import 段补充（runner.py:23-33 现无这些）
from pathlib import Path
import copy
import yaml

# 2) 守卫表登记新键
_SUPPORTED_FINAL_KEYS = {"termination_reason", "is_complete", "persisted", "llm_assert"}
# 说明：storage_batch / host_component / writer 不进守卫表 —— 它们的观测已改为经
# `custom:*` 事件 + expected.event_stream 断言（见 A2/A3/A5），避免新增无法自洽的 final 子键。
_LLM_ASSERT_KEYS = {"system_contains", "system_regex", "messages_roles", "message_count"}

# 3) _assert_final 增关键字参数 llm（默认 None → 既有 5 处调用点无需全改）
def _assert_final(expected_final, done_event, error_events,
                  persisted_view=None, llm=None) -> List[str]:
    ...
    spec = expected_final.get("llm_assert")
    if spec:
        unknown = sorted(set(spec) - _LLM_ASSERT_KEYS)
        if unknown:
            errors.append(f"llm_assert 含未识别子键: {unknown}")   # 防假绿
            return errors
        if llm is None:
            errors.append("llm_assert 出现在无 LLM 观测的用例类型中(该类型不支持)")
            return errors
        system = llm.last_system or ""
        if spec.get("system_contains") and spec["system_contains"] not in system:
            errors.append(f"llm system 未包含 {spec['system_contains']!r}: {system!r}")
        if spec.get("system_regex") and not re.search(spec["system_regex"], system):
            errors.append(f"llm system 不匹配正则 {spec['system_regex']!r}: {system!r}")
        if spec.get("message_count") is not None and len(llm.last_messages or []) != spec["message_count"]:
            errors.append(f"llm message_count 不匹配: {len(llm.last_messages or [])}")
        if spec.get("messages_roles") is not None:
            roles = [m.get("role") for m in (llm.last_messages or [])]
            if roles != spec["messages_roles"]:
                errors.append(f"llm messages_roles 不匹配: {roles}")
    return errors

# 4) _run_pipeline_case（runner.py:652-658）替换硬编码 None 并支持 session_store / core_overrides
writer = None
if inputs.get("storage_writer"):
    from teage_liu2.core.storage_writer import StorageWriter
    writer = StorageWriter()
session_store = None
if inputs.get("session_store"):
    from teage_liu2.core.session import SessionStore
    session_store = SessionStore()
overrides = inputs.get("core_overrides") or {}
pipeline = ChatPipeline(
    llm_client=llm, history_store=storage, hooks=hooks,
    max_loops=int(overrides.get("max_loops", inputs.get("max_loops", 50) or 50)),
    mode=overrides.get("mode", MODE_LOOP),
    injection_budget=overrides.get("injection_budget"),
    history_window_messages=overrides.get("history_window_messages", 100),
    storage_writer=writer, session_store=session_store,
)
try:
    ...  # 原有事件循环
finally:
    if writer is not None:
        writer.close()          # 收尾排空在途写，之后才断言落盘序

# 5) ScriptedBranch 支持 mutate_snapshot（runner.py:292-392 内）
if self._behaviors.get("mutate_snapshot"):
    try:                        # Snapshot 是 frozen dataclass：仅原地改 list/dict 元素可行
        cur.messages.append({"role": "user", "content": "注入篡改"})
        cur.extra["hacked"] = True
    except Exception:
        pass                    # 被冻结属性赋值会抛 FrozenInstanceError，属预期
```

- [ ] **步骤 4: 跑测试确认通过**

运行: `D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py`
预期: `[PASS] injection-dedup-25`，且**原有 21/21 不回归**

- [ ] **步骤 5: 同步 schema 与 README**

`suite.schema.json` 的 `FinalExpectation.properties` 增 `llm_assert`（该层 `additionalProperties: true`，登记是为文档一致 —— 见审查 R10）；`PROTOCOL/README.md` 增段说明 runner 允许 import `teage_liu2.server`（测试工具边界显式化）。

### 任务 A5: 批量原子性断言

> **审查 R9**：原用例的两次 `write` 入参**全部合法**，非事务实现下判定式同样成立（空断言）。用例必须**真注入非法元素**，且参考后端须显式校验元素类型（A1 步骤 3 已加）。

- [ ] **步骤 1: 写用例**

```json
// cases/26-storage-batch-atomic.json
{
  "id": "storage-batch-atomic-26",
  "protocol_version": "v1.0.0",
  "description": "P-4 条款③: 批量写 docs[] 同事务原子——含非法元素则整批回滚(query 为空),合法批量全部落盘。",
  "setup": { "extensions": [] },
  "inputs": {
    "session_id": "s-26",
    "storage_ops": [
      { "op": "write", "args": ["batch_atomic", [{ "a": 1 }, { "a": 2 }]], "assert": { "length": 2 } },
      { "op": "write", "args": ["batch_atomic", [{ "a": 3 }, 123]], "expect_error": true },
      { "op": "query", "args": ["batch_atomic"], "assert": { "length": 2 } }
    ]
  },
  "expected": {
    "event_stream": [
      { "type": "custom:stdio_result",
        "payload": { "op_count": 3, "rejected_non_dict": true, "final_rows": 2 } }
    ],
    "final": { "is_complete": true }
  }
}
```

- [ ] **步骤 2: 跑测试确认它失败**

运行: `D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py`
预期: FAIL（`custom:stdio_result.payload` 缺 `rejected_non_dict` / `final_rows`）

- [ ] **步骤 3: 写最简实现**

在 `_run_stdio_case` 的 op 循环里支持 `expect_error` 与两个聚合判据：

```python
rejected_non_dict = False
for spec in inputs.get("storage_ops") or []:
    fn = getattr(proxy, spec["op"])
    try:
        value = fn(*spec.get("args") or [], **(spec.get("kwargs") or {}))
    except Exception as e:
        if spec.get("expect_error"):
            rejected_non_dict = rejected_non_dict or ("必须是对象" in str(e))
        else:
            errors.append(f"op {spec['op']} 意外失败: {type(e).__name__}: {e}")
        continue
    if spec.get("expect_error"):
        errors.append(f"op {spec['op']} 期望报错但成功返回")
    if spec.get("assert") is not None and not match_value(spec["assert"], value):
        errors.append(f"op {spec['op']} 断言失败: expect={spec['assert']} actual={value!r}")
    results.append({"op": spec["op"], "value": value})
```

`final_rows` = 循环结束后再补一次 `proxy.query("batch_atomic")` 的条数（在 `proxy.close()` 之前）。

- [ ] **步骤 4: 跑测试确认通过**

运行: `D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py`
预期: `[PASS] storage-batch-atomic-26`

- [ ] **步骤 5: 变异验证 + 回归门禁**

① **变异**：把参考后端 `write` 的 `with self.conn:` 改为"逐条 `self.conn.commit()`"（退化非原子）→ 用例必须变红（`final_rows` 变 3）；恢复。② **门禁**：`powershell -ExecutionPolicy Bypass -File scripts\audit_liu2.ps1` → 套件全绿（21 + 5 = **26** 条）、pytest 全绿、`exit=0`。

---

## §3 WP-B 协议条款补用例（P0，依赖 WP-A）

**文件:** 新建 `teage_liu2/PROTOCOL/behavior-suite/cases/27-*.json` … `39-*.json`（编号顺延；**所有新增用例 id 必须登记进 `protocol_pipeline_ids`**，见 §全局约束的登记清单 —— 审查 R5）

**接口:** 消费 WP-A 的 `llm_assert` / `custom_events` / `mutate_snapshot` / `storage_writer` / `session_store` / `core_overrides` / `storage_ops` 与两个协议层分支（`stdio-proxy` / `host-component`）

### 任务 B1: hooks 域 6 条（H-3 / H-8 / H-9 / H-16 / H-17 / H-19）

| 用例 id | 文件 | 条款 | inputs 要点 | expected 要点 |
|---|---|---|---|---|
| `hooks-append-role-27` | `27-hooks-append-role.json` | H-3 | ext 在 `before` 返回 `AppendMessage(role="assistant")` 一条 + `role="user"` 一条 | `error_codes.must_include=["HOOK_INVALID_ACTION"]`；`final.llm_assert.message_count` 只含 user 追加 |
| `hooks-atomic-batch-28` | `28-hooks-atomic-batch.json` | H-8 | ext_a（注册在前）返回 3 条 action 且第 3 条非法；ext_b 用 `snapshot_assert` 看快照 | ext_b 看到的 `messages` 长度不含 a 的半批次；`error_codes` 含 `HOOK_INVALID_ACTION` |
| `hooks-semantic-validate-29` | `29-hooks-semantic-validate.json` | H-9 | `before` 追加 `user` 造成连续两条 user（语义级非法） | `event_stream` 含 `error` 且 **无** `step_start`（未送 LLM） |
| `hooks-round-merge-30` | `30-hooks-round-merge.json` | H-16 | ext_a/ext_b 各返回一条 `inject_round`（同层同 key，b 注册在后）；另各声明一条 `build_injections` | **分通道断言（审查 R15）**：build_injections 进 system（`PREFIX` 层）→ `llm_assert.system_contains` 验"同 key 取 b"；`inject_round` 进 **messages**（`BEFORE_INPUT` 层，`assembler.py:83-89`）→ 用 `llm_assert.message_count`/`messages_roles` 验其已追加（比无轮级注入基线 +1） |
| `hooks-readonly-31` | `31-hooks-readonly.json` | H-17 | `inputs.mutate_snapshot=true`（ScriptedBranch 在 `before` 内改写 `snapshot.messages`/`extra`） | `final.llm_assert.message_count` 与未突变基线一致；`snapshot_assert` 证明 core 状态未受影响 |
| `hooks-reject-eventflow-32` | `32-hooks-reject-eventflow.json` | H-19 | ext 在 `pre_tool_call` 返回 reject | `event_stream` 依次含 `tool_use`（原始 input）与 `tool_result(is_error=true, termination_reason="tool_rejected")` |

- [ ] **步骤 1: 写完 6 个用例 JSON**

（每个用例按上表规格，结构照 `cases/18-storage-persist-timing.json` 模板）

- [ ] **步骤 2: 跑套件确认 6 条全绿**

运行: `D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py`
预期: 6 条 `[PASS]`；若某条 FAIL 且原因是 core 行为缺失 → **停下**，记入 `CORE-缺口记录.md` 并按 §5 风险流程处理，不得放宽断言

- [ ] **步骤 3: 变异验证（每条）**

对每条用例，人为回退对应 core 行为（如把 H-8 的批次原子应用改成逐条应用），确认该用例**变红**，再恢复。任一条不变红 → 该用例是空断言，必须重写。

### 任务 B2: events 域 4 条（E-2 / E-4 / E-8 / E-9）

| 用例 id | 文件 | 条款 | inputs 要点 | expected 要点 |
|---|---|---|---|---|
| `events-unknown-type-33` | `33-events-unknown-type.json` | E-2 | `custom_events` 注入 `custom:unknown_xyz` | `final.termination_reason="normal"`（透传不崩溃）+ `event_stream` 含该类型 |
| `events-l2-only-34` | `34-events-l2-only.json` | E-4 | observe ext 实现 `after_step` | `invocations[].snapshot_assert` 断言 L2 摘要字段齐整；`event_stream` 断言 L1 `text_delta` 对扩展不可见（用 `absent` 在 L3 收集器上断言） |
| `events-stepend-dual-35` | `35-events-stepend-dual.json` | E-8 | 同一 ext 同时实现 `after_step` 与 `observe` | L2 与 L3 各收一份（计数各为 1，不重复） |
| `events-observe-readonly-36` | `36-events-observe-readonly.json` | E-9 | observe ext 的 `before` 返回 `SetExtra` | `error_codes.must_include=["HOOK_TERMINAL_ACTION_IGNORED"]`；`final.llm_assert` 证明 extra 未生效 |

- [ ] **步骤 1: 写完 4 个用例 JSON**
- [ ] **步骤 2: 跑套件确认全绿**

运行: `D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py`
预期: 4 条 `[PASS]`

- [ ] **步骤 3: 变异验证（每条同 B1 步骤 3）**

### 任务 B3: types 域 3 条（T-1 / T-7 / T-8）

> T-3（不可变只读）与 T-5（结构共享/禁 deepcopy）**不在套件实现**——它们需要对象同一性断言，归 WP-F 单测（任务 F3）。

| 用例 id | 文件 | 条款 | inputs 要点 | expected 要点 |
|---|---|---|---|---|
| `injection-dedup-25` | 已由 A4 产出 | T-1 | 同 key 后声明覆盖先声明 | `llm_assert.system_contains` |
| `types-budget-drop-38` | `38-types-budget-drop.json` | T-1 | `core_overrides={"injection_budget": {"PREFIX": 8}}` + 一条 40 字符的 `PREFIX` 注入 | `llm_assert.system_contains` 断言该注入**完全不出现**（整段丢弃、不截半） |
| `types-doc-opaque-37` | `37-types-doc-opaque.json` | T-7 | 经 `stdio-proxy` 的 `storage_ops` 写入嵌套任意结构 doc → `read` 取回 | 返回结构与写入逐字段相等（内核不解析不改写） |

> **T-8（三个资源上限）不在套件内实现（审查 R16）**：上限是 `core/snapshot.py:79-96` 引用的**模块常量** `RESOURCE_LIMITS`，`config.core.max_*` 只校验未接线（`config.py:418-421`），`core_overrides` 覆盖不到 → 归 **WP-F 单测**（`tests_core/test_snapshot_limits.py` 已存在，任务 F4 中确认三条上限各有断言）。日后若把上限接进 `ChatPipeline` 构造参数，再回填套件用例。

- [ ] **步骤 1: 写完 2 个用例 JSON**（`core_overrides` 在 A4 一并支持，**仅**覆盖 `ChatPipeline` 构造参数）
- [ ] **步骤 2: 跑套件确认全绿**
- [ ] **步骤 3: 变异验证（每条）**

### 任务 B4: config 域 1 条（C-2 敏感字段）

- [ ] **步骤 1: 写用例**

```json
// cases/39-config-domain-secret.json
{
  "id": "config-domain-secret-39",
  "protocol_version": "v1.0.0",
  "description": "config C-2: ${VAR} 占位注入 + .env 分离 + 脱敏——敏感明文不出现在错误消息与日志。",
  "setup": { "extensions": [] },
  "inputs": {
    "env": { "BS_TEST_KEY": "sk-secret-9f3a" },
    "yaml": "llm:\n  main_provider: anthropic\n  main_model: m\n  main_api_key: ${BS_TEST_KEY}\ncore:\n  max_loops: 5\n",
    "probe": { "field": "llm.main_api_key" }
  },
  "expected": {
    "event_stream": [
      { "type": "custom:config_result",
        "payload": { "placeholder_resolved": true, "secret_not_in_errors": true } }
    ],
    "final": { "is_complete": true },
    "error_codes": { "must_exclude": ["sk-secret-9f3a"] }
  }
}
```

> 用例 id **必须以 `config-domain` 开头**（`runner.py:1023` 的分支条件），并登记进 `protocol_pipeline_ids`；`inputs` 字段须与 `_run_config_case`（`runner.py:1254-1300`）及 `cases/21-config-domain.json` 的既有约定对齐 —— 实现前先读这两处，新增 `env`/`probe` 两键并在 `_run_config_case` 中消费。

- [ ] **步骤 2: 跑套件确认全绿**
- [ ] **步骤 3: 变异验证**（把占位替换逻辑改成保留原文 → 用例应因 `must_exclude` 命中而变红）

### 任务 B5: 覆盖矩阵回流

- [ ] **步骤 1: 更新 `PENDING.md` 各条的"入协议条件"勾稽**

逐条把"行为套件补用例"从未满足改为已满足（P-1③/P-2③/P-4③/P-5③/P-6③/P-7③），并注明新用例编号。

- [ ] **步骤 2: 更新 `PROTOCOL/README.md` 与开发日志**

---

## §4 WP-C P-8 三码发射点（P1，独立）

**文件:**
- 修改: `teage_liu2/core/hooks.py:341`（TOOL_MODIFY_INVALID）、`:368-372`（TOOL_EXEC_FAILED）
- 修改: `teage_liu2/core/loop.py:217-220`（TOOL_REJECTED_BY_POLICY）
- 新建: `teage_liu2/tests_core/test_tool_error_codes.py`
- 修改: `teage_liu2/PROTOCOL/errors/errors.spec.md` §5

- [ ] **步骤 1: 写失败测试**

```python
# teage_liu2/tests_core/test_tool_error_codes.py
import asyncio
import logging

from teage_liu2.core.actions import ToolDecision
from teage_liu2.core.errors import TOOL_EXEC_FAILED, TOOL_MODIFY_INVALID
from teage_liu2.core.hooks import Branch, HookChain
from teage_liu2.core.types import Snapshot


def _snap() -> Snapshot:
    # 字段以 core/types.py 的 Snapshot dataclass 为准（可照 tests_core/test_hooks.py 的构造）
    return Snapshot(session_id="s-tool", round=0, messages=[], system_text="",
                    extra={}, tools=[], revision=0)


class _ModifyNoneBranch(Branch):
    name = "modify_none"

    async def pre_tool_call(self, snapshot, name, input):
        return ToolDecision(decision="modify", input=None)


class _BoomBranch(Branch):
    name = "boom"

    async def on_tool_call(self, snapshot, name, input):
        raise RuntimeError("执行炸了")


def test_modify_without_input_logs_code(caplog):
    """pre_tool_call 返回 modify 但 input=None → TOOL_MODIFY_INVALID 日志面。"""
    chain = HookChain()                   # 审查 R14：HookChain 只接受 hook_timeout
    chain.register(_ModifyNoneBranch())   # 枝干必须经 register() 接入
    with caplog.at_level(logging.ERROR):
        asyncio.run(chain.pre_tool_call_all(_snap(), "echo", {"x": 1}))
    assert TOOL_MODIFY_INVALID in caplog.text


def test_tool_exec_failed_logs_code(caplog):
    """on_tool_call 抛异常 → TOOL_EXEC_FAILED 日志面。"""
    chain = HookChain()
    chain.register(_BoomBranch())
    with caplog.at_level(logging.ERROR):
        result = asyncio.run(chain.dispatch_tool_call(_snap(), "echo", {"x": 1}))
    assert isinstance(result, Exception) and TOOL_EXEC_FAILED in caplog.text
```

> `TOOL_REJECTED_BY_POLICY` **不在单测覆盖**：它由 `loop.py` 在 tool 路径发射，单测需驱动整个 `ReactLoop`；改由**套件用例 16**的日志面断言覆盖（见步骤 5，审查 R13）。

- [ ] **步骤 2: 跑测试确认它失败**

运行: `D:\soft\Python311\python.exe -m pytest teage_liu2/tests_core/test_tool_error_codes.py -q`
预期: FAIL（2 条全红：日志无码前缀）

- [ ] **步骤 3: 写最简实现**

```python
# core/hooks.py:341 —— 补 else 分支（当前 modify 但 input 为 None 时静默忽略）
if decision.is_modify and decision.input is not None:
    cur_input = decision.input
elif decision.is_modify:
    logger.error(
        "%s: 枝干 %s 的 pre_tool_call 返回 modify 但 input 为 %r，按 allow 处理",
        TOOL_MODIFY_INVALID, branch.name, decision.input,
    )

# core/hooks.py:368-372 —— 加码前缀
except Exception as e:
    logger.error("%s: 枝干 %s 执行工具 %s 异常: %s", TOOL_EXEC_FAILED, branch.name, tool_name, e)
    return e

# core/loop.py:217-220 —— 加码前缀
logger.warning(
    "%s: 工具 %s 被策略拒绝(pre_tool_call),tool_rejected 回喂",
    TOOL_REJECTED_BY_POLICY, tool_name,
)
```

> `core/hooks.py` 需在 import 段补 `TOOL_MODIFY_INVALID, TOOL_EXEC_FAILED`；`core/loop.py` 补 `TOOL_REJECTED_BY_POLICY`。

- [ ] **步骤 4: 跑测试确认通过**

运行: `D:\soft\Python311\python.exe -m pytest teage_liu2/tests_core/test_tool_error_codes.py -q`
预期: 2 passed

- [ ] **步骤 5: 套件断言 + 契约回流**

**在 `cases/16-error-responsibility.json`** 增 `"error_codes": {"must_include": ["TOOL_REJECTED_BY_POLICY"]}` —— 用例 16 的 `tool_rejected` 场景（`cases/16:35`，驱动代码 `runner.py:1115-1122`）才是 reject 路径；**不要**加到用例 20（其 scenarios 无 reject，且日志捕获按用例安装/移除，必然变红 —— 审查 R13）。随后把 `errors.spec.md §5` 的"⑤ 待落地"三码移入"② 日志面"（先读该表实际结构再改），并勾稽更新 `PENDING.md` P-8 条件④。

---

## §5 WP-D 协议域文件归档与契约同步（P1，独立）

- [ ] **步骤 1: manifest 规范迁入 lifecycle 域（P-1 条件④）**

把 `docs/SUBSYSTEM-SPI.md §13.2` 的 manifest 字段集（`name`/`version`/`language`/`entry`/`transport`/`command`/`protocol_version`/`capabilities`/`description`/`requirements`/`kind`/`slots` 的必填与类型）迁移进 `PROTOCOL/lifecycle/lifecycle.spec.md` 新增章节「§N 扩展 manifest 声明规范」，并在 `lifecycle.schema.json` 增 `ExtensionManifest` 定义（与 `core/extension_loader.py:65-157` 的校验逐条对应）。

- [ ] **步骤 2: host_components 契约入 config 域（P-5 条件④）**

`PROTOCOL/config/config.spec.md` 增「§N 宿主组件插槽（host_components）」章节：`[{slot, backend, options}]` 结构、`SLOTS` 白名单语义、`factory(cfg, options, specs) -> {slot: obj}` 工厂签名、同实例覆盖多插槽、非法配置 = 启动失败；`config.schema.json` 增对应 schema。

> P-5 条件④原文是"明确归入 **config/lifecycle** 域文件"；本计划的归属裁决 = **config 域**（manifest 声明面已由 P-1 落在 lifecycle，两者不重复）。请在同一提交里于 `PENDING.md` P-5 处注明该裁决，使条件④判定可审计。

- [ ] **步骤 3: 三个资源上限键登记 PENDING（§0 第 11 项）**

`PENDING.md` P-1 条目的"现状"段补一句：三个资源上限键（`max_snapshot_bytes` / `max_message_bytes` / `max_messages_per_conversation`）语义由 `types.spec.md` §7 T-8 承载，配置键登记随本条一并评审。

- [ ] **步骤 4: 修复 `MessageStore` ABC 签名漂移（§0 第 8 项）**

```python
# core/storage.py:233 —— 与 HistoryStore / SQLiteHistoryStore 对齐
@abc.abstractmethod
def get_session_messages(
    self, session_id: str, limit: Optional[int] = None,
    before_id: Optional[int] = None,
) -> List[Dict[str, Any]]:
    """获取会话消息（按时间正序;before_id 游标向上翻页）。"""
```

> 说明：`MessageStore` ABC 是**唯一**仍是两参的定义，实现侧（`SQLiteHistoryStore`、`StdioStorageProxy`）与测试 stub 均已三参；Python 不校验重写签名，故本步**不产生行为变化**，纯属契约一致性修复。改后确认无调用方经 `MessageStore` 抽象类型传 `before_id`（现网 0 处）。

- [ ] **步骤 5: 门禁回归**

运行: `powershell -ExecutionPolicy Bypass -File scripts\audit_liu2.ps1`
预期: `exit=0`（本 WP 全为文档/schema/签名，不应有测试变化）

---

## §6 WP-E server 层测试锚定（P1）

**文件:**
- 新建: `teage_liu2/tests_core/test_server_routes.py`
- 新建: `teage_liu2/tests_core/test_server_host_components.py`
- 新建: `teage_liu2/tests_core/test_server_stdio_proxy.py`
- 新建: `teage_liu2/tests_core/conftest.py`（共享 `client` fixture —— 该目录现无 conftest，E2/E3 复用）

### 任务 E1: routes 层锚定 4 个生产修复

- [ ] **步骤 0（前置·阻塞项）: 处置 CI 依赖冲突 —— 审查 R11**

现状：`.github/workflows/liu2-audit.yml:48` 只装 `pytest pyyaml jsonschema python-dotenv`，且 CI 不安装 `pyproject.toml` 依赖；本任务测试顶层 `from fastapi.testclient import TestClient` 会 import fastapi → **CI 收集期 ImportError、audit job 必红**（本地因已装 fastapi/httpx 而绿）。

三种处置，**需用户选择**（修改 workflow 属配置改动）：

| 方案 | 动作 | 代价 |
|---|---|---|
| **A（推荐）** | workflow 安装行追加 `fastapi httpx`（二者本就在 `requirements.txt` 与 `pyproject.toml`） | 改 1 行；CI 稍慢 |
| B | server 测试拆到 `teage_liu2/tests_server/`，CI 与 `scripts/audit_liu2.ps1` 都**不**纳入 | 本地/CI 覆盖不一致，易漏跑 |
| C | 不经 FastAPI：直接调路由函数 + 构造 `starlette.requests.Request` stub | 失去真实装配面覆盖，与"锚定生产修复"目标不符 |

选定方案前**不要**开始步骤 1。

- [ ] **步骤 1: 写失败测试**

```python
# teage_liu2/tests_core/test_server_routes.py
import pytest
from fastapi.testclient import TestClient

@pytest.fixture()
def client(tmp_path, monkeypatch):
    """最小可测 app：假 LLM + 临时库 + 无扩展。"""
    import teage_liu2.server.app as app_mod
    monkeypatch.setattr(app_mod, "LLMClient", _FakeLLMClient)  # 免 API Key
    cfg = {
        # 审查 R12：llm 段键名按 core/llm.py:1018-1037 的真实强校验键
        "llm": {"main_provider": "anthropic", "main_model": "m",
                "main_api_key": "dummy", "main_base_url": "http://127.0.0.1:1"},
        "core": {"extensions_root": str(tmp_path / "extensions"), "max_loops": 3},
        "storage": {"sqlite_path": str(tmp_path / "s.db")},
    }
    (tmp_path / "extensions").mkdir()
    app = app_mod.create_app(config=cfg)
    with TestClient(app) as c:
        yield c


def test_chat_uses_done_response_on_intercept(client, monkeypatch):
    """回归 154e6ae:拦截路径无 text_delta,/chat 必须取 done.response 而非空拼接。"""
    _make_next_chat_intercepted(monkeypatch)
    r = client.post("/chat", json={"session_id": "s1", "user_input": "hi"})
    assert r.status_code == 200
    assert r.json()["response"] != ""
    assert r.json()["termination_reason"] == "intercepted"


def test_reload_returns_branch_names(client):
    """回归 154e6ae:/reload 返回分支名字符串，不得 500。"""
    r = client.post("/reload")
    assert r.status_code == 200
    assert all(isinstance(n, str) for n in r.json()["branches"])


def test_history_pagination_before_id(client):
    """回归 00eb545:before_id 游标向上翻页。"""
    ...
```

- [ ] **步骤 2: 跑测试确认它失败**

运行: `D:\soft\Python311\python.exe -m pytest teage_liu2/tests_core/test_server_routes.py -q`
预期: FAIL（文件不存在 → 导入错误）

- [ ] **步骤 3: 写测试实现（不写生产代码——生产修复已在 `154e6ae` 入库）**

补 `_FakeLLMClient`（脚本化流式事件）与 `_make_next_chat_intercepted`（用 `monkeypatch.setattr` 替换 `HookChain.before_all` 返回 `SetStop`）。`client` fixture 提到 `tests_core/conftest.py` 供三个新测试文件复用。`TestClient` 依赖 `httpx`（本地已装、`pyproject.toml:13` 有声明，但 **CI 侧须按步骤 0 先处置**）。

- [ ] **步骤 4: 跑测试确认通过**

运行: `D:\soft\Python311\python.exe -m pytest teage_liu2/tests_core/test_server_routes.py -q`
预期: PASS

- [ ] **步骤 5: 变异验证**

把 `server/routes.py` 的 `ev.get("response") or "".join(text_parts)` 改回 `"".join(text_parts)` → `test_chat_uses_done_response_on_intercept` 必须变红；恢复。

### 任务 E2: host_components 快速失败矩阵

- [ ] **步骤 1: 写测试**（未知插槽 / 未知 backend / 重复接管 / `command` 与 `extension` 互斥 / ABC 不满足 / backend 返回空映射 → 各断言 `ValueError`/`TypeError` 与可读文案片段）
- [ ] **步骤 2: 跑测试确认通过**（生产代码已具备，预期直接 PASS；若 FAIL 记录为真实缺口）
- [ ] **步骤 3: 门禁回归**

### 任务 E3: stdio-proxy 真实子进程锚定

- [ ] **步骤 1: 写测试**（用 A1 的参考后端作为子进程）：握手成功 / `ensure_session` 幂等短路计数 / 批量与直发保序 / `close()` 幂等（连调两次）/ 关停后 buffered 写入有 error 日志不静默
- [ ] **步骤 2: 跑测试确认通过**
- [ ] **步骤 3: 变异验证**（把 `close()` 的 `join(flusher)` 删掉 → 对应断言变红）

### 任务 E4/E5: 补 L-11 / L-10 条款

- [ ] **步骤 1: L-11 会话并发互斥** —— `test_server_routes.py` 增用例：同一 `session_id` 两个并发 `/chat`（`TestClient` + 线程），断言两次响应顺序串行、历史无交错（`UPDATE sessions` 不丢更新）
- [ ] **步骤 2: L-10 会话态 extra 延续** —— 两轮对话，第一轮 `before` 置 `extra.marker=1`，第二轮断言 `snapshot_assert.extra.marker == 1`（runner 的 `session_store` 注入由 A4 提供）
- [ ] **步骤 3: 跑全量门禁**

---

## §7 WP-F core 模块测试锚定（P2）

- [ ] **F1 `test_storage_writer.py`**：单写者 FIFO 保序、队列满丢弃最旧 + 计数、`close()` 排空在途写、写线程异常不静默
- [ ] **F2 `test_supervisor.py` / `test_remote_adapter.py`**：心跳 3 次失败 → 僵死重建；`major` 不匹配 → 握手拒绝；`close()` 后 `request()` 抛可读错而非悬挂
- [ ] **F3 `test_snapshot_identity.py`**（覆盖 types T-3 / T-5）：`snapshot.with_messages()` 后未变元素**对象同一**（`is`）；`deepcopy` 调用被禁止（monkeypatch `copy.deepcopy` 计数为 0）
- [ ] **F4 `test_assembler.py` / `test_modes.py` / `test_step.py` + T-8 上限确认**：`incremental_finalize` 的 merge/语义校验分支；`MODE_BARE` 与 `MODE_LOOP` 差异；`step` 的 4 个 LLM_* 错误码与超时原语；并确认 `test_snapshot_limits.py` 对 `RESOURCE_LIMITS` 三条上限（`max_snapshot_bytes` / `max_message_bytes` / `max_messages_per_conversation`）**各有断言**（缺则补齐 —— types **T-8 条款由本任务承担**，不进套件，见 §3 B3 的说明）
- [ ] **F5 门禁回归**：`audit_liu2.ps1` → `exit=0`，并把 `pytest` 通过数回写 `MEMORY.md` 基线

---

## §8 WP-G 协议文档一致性修补（P2）

- [ ] **步骤 1: 消解 `T-*` 跨域歧义**：在两个 spec 的条款编号处各加域内限定（`transport.spec.md` 写「本域条款编号 `T-1..T-6`，跨域引用一律写作 `transport T-5`」；`types.spec.md` 同理），并在 `PROTOCOL/README.md` 增"条款编号前缀域内唯一、跨域必须带域名前缀"的约定。**不改动既有编号**（避免破坏 PENDING 与已有引用）。
- [ ] **步骤 2: 修用例 17 描述与实现不符**：`cases/17-host-port-inprocess.json` 的 description 删去"防重入/并发上限"表述（runner 的 `host_port_ops` 分支未实现该检查）；并发上限的真实覆盖由 WP-E 的 `test_server_routes.py` 承担。
- [ ] **步骤 3: `PROTOCOL/README.md` 更新附录**（runner 允许依赖范围、新断言键清单、参考后端定位）
- [ ] **步骤 4: 开发日志追加本轮摘要**

---

## §9 WP-H 运维与实测（P3）

- [ ] **步骤 1: 前端 fail-closed 浏览器实测**：临时脚本落 `d:/tmp`（playwright + Edge 通道），注入 `window.DOMPurify = undefined` 后调用 `renderMarkdown("<img src=x onerror=alert(1)>")`，断言输出为转义文本；测毕删除脚本。
- [ ] **步骤 2: 停服后清理 `d:/tmp/liu2-7878.r2.{out,err}.log`**
- [ ] **步骤 3: e2e 残留会话**：保持现状（用户 2026-09-10 裁决"先不动"）；若后续清理，需先获批。
- [ ] **步骤 4: 两系统配置解耦**（待用户批准）：立项评估"liu2 独立配置文件 + 独立默认库路径"，明确与老系统共用 `config.yaml` 的现状与迁移代价；**本步只产出评估，不改配置**。

---

## §10 里程碑与验收门禁

| 里程碑 | 内容 | 验收 |
|---|---|---|
| **M1** | WP-C + WP-D + WP-G | 套件 21/21 不回归；`test_tool_error_codes.py` 3 passed；`audit_liu2.ps1` exit=0 |
| **M2** | WP-A → WP-B | 套件 21 → 39 条全绿（WP-A 新增 5 条 + WP-B 新增 13 条）；每条新用例通过**变异验证**；PENDING 六条协议条件③勾稽完成 |
| **M3** | WP-E + WP-F | `tests_core` 新增 ≥6 个测试文件；server 层首次有锚定；audit exit=0 |
| **M4** | WP-H | 前端实测结论落地；临时件清零 |

**每个里程碑收尾必跑:**

```powershell
D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py
powershell -ExecutionPolicy Bypass -File scripts\audit_liu2.ps1
```

预期: 套件 100% 通过、`pytest` 全绿、`exit=0`、lints 0。

---

## §11 明确不做（范围外）

- **不新增 Rust 代码**：参考后端用 Python 标准库实现；CI 不依赖 `cargo`（保持最小依赖集）。
- **不升 `PROTOCOL/VERSION`**：本计划只补齐入协议**条件**；P-1..P-8 转 `stable`/`merged` 需条件全部满足后单独评审。
- **不重构老系统 `teage_liu/`**（冻结）。
- **不新增第三方依赖**（含 `pytest-asyncio`；异步测试统一用 `asyncio.run` 或 `TestClient`）。
- **不做 SessionStore TTL**（`CORE-缺口记录.md` 定为外壳责任，属独立评估）。
- **不改 `config.yaml` 等常驻配置**（WP-H 步骤 4 只出评估文档）。
- **不动冻结面 12 消息 / 11 键 / 6 Action / 11 钩子**（PROTOCOL 冻结面口径 = 11 钩子，见 `PROTOCOL/README.md:14` 与 `hooks.spec.md:29`；旧表述"7 钩子"是 CORE 文档口径，已按审查 R17 更正）。

---

## §12 风险与回滚

| 风险 | 触发条件 | 处置 |
|---|---|---|
| runner 引入 `teage_liu2.server` import 使"语言无关交付物"边界松动 | WP-A 任务 A2/A3 | `PROTOCOL/README.md` 显式声明 runner 是**本实现的参考执行器**、允许依赖宿主实现；`cases/*.json` + schema 保持零依赖（F-2 结论） |
| 参考后端与 Rust 后端语义漂移 | WP-A 任务 A5 | 同一套 P-4 条款清单双向核对（`storage-rust/src/history.rs` vs 参考后端）；差异记入 `PENDING.md` P-4 条款 |
| 新增用例使套件变慢（参考后端每条起子进程） | WP-B | 子进程用例控制在 ≤6 条；其余用进程内 stub；套件总时长目标 ≤30s |
| 用例 FAIL 暴露 core 真实行为缺失 | WP-B 步骤 2 | **不得放宽断言**；记入 `CORE-缺口记录.md` 并单独修复，修复后再回填用例 |
| CI（ubuntu）无 `data2/extensions` | WP-E/E3 | 子进程用例用临时目录自建 manifest；`tests_branches/test_guardrails.py` 的 skipif 现状不变 |
| Windows 控制台 GBK 致子进程 UTF-8 解码失败 | WP-A | 参考后端启动 `sys.stdin/stdout/stderr.reconfigure(encoding="utf-8")`（P-4 条款⑦） |

---

## §13 自审

**审查一致性检查（2026-09-10）:** 本文档已按 §0.0 的 R1-R19 逐条修订；两轮 plan-verifier 的致命项（R1 参考后端握手契约、R2 方法缺失、R5 runner 分派/返回契约、R11 CI 依赖冲突）均已闭合。若正文与本记录冲突，**以正文为准**并回报。

**规格覆盖检查:** §0 的 20 项未办事项 → 第 1 项（协议收口）由 WP-A + WP-B + WP-D 覆盖；第 2-5 项（runner 能力）由 WP-A 覆盖；第 6 项（条款覆盖缺口）由 WP-A（T-1）+ WP-B（套件 13 条）+ WP-E（L-10/L-11）+ WP-F（T-3/T-5/T-8、L-5/L-6）覆盖；第 7 项 WP-C；第 8-11 项 WP-D；第 12-13 项 WP-E；第 14 项 WP-F；第 15-16 项 WP-G；第 17-19 项 WP-H；第 20 项列入 §11 不做。**无遗漏。**

**条款覆盖检查（2026-09-11 落地后 · 逐条附锚定与限定）:** 20 条未覆盖条款中，**13 条进套件**（hooks H-3/H-8/H-9/H-16/H-17/H-19；events E-2/E-4/E-8/E-9；types T-1/T-7；config C-2），**6 条进单测 / server 测试**，**T-8 已实质满足**（`tests_core/test_snapshot_limits.py` 三条资源上限各有断言；2026-09-11 落地审查 E-5 更正，未覆盖 20 → 6 条）。6 条的**实际落点与限定语**：

| 条款 | 锚定文件 | 覆盖度 |
|---|---|---|
| types **T-5** | `tests_core/test_snapshot_identity.py` | **全覆盖**（未变元素 `is` 同一 + `copy.deepcopy` 计数 0） |
| types **T-3** | 同上 + `core/types.py::readonly_view` | **全覆盖**（2026-09-11 S2 落地）：内核侧不变量 + **扩展侧边界只读视图**（`messages`/`tools`/`history`→tuple、`extra`→`MappingProxyType`）；H-17 已修 |
| lifecycle **L-5** | `tests_core/test_supervisor.py` | **部分**：锚"每启用扩展一条进程条目"；未锚"重建只影响被替换者" |
| lifecycle **L-6** | `tests_core/test_supervisor.py` + `tests_core/test_remote_adapter.py` | **部分**：锚 supervisor 行为（心跳→`on_dead` / 重建成功计数 / 重建失败降级）；"主对话不中断"端到端未直接断言 |
| lifecycle **L-10** | `tests_core/test_session_extra_continuity.py` | **基本覆盖**：恢复 + 写回 + 跨对话；**仅 done 路径**，error 路径写回未单独锚 |
| lifecycle **L-11** | `tests_core/test_session_locks.py`（机制）+ `tests_core/test_server_routes.py`（接线） | **全覆盖**（2026-09-11）：锁机制（含"刚返回锁不淘汰"边界用例）+ **routes 真持锁串行**（路由层确定性并发）；另修 `SessionLocks` 边界漏洞 |

> **结论（2026-09-11 更新）**：6 条**已锚定**；其中 **T-3 / L-11 已达全覆盖**（H-17 边界只读视图 + routes 接线均于同日落地），仅 **L-10 为带限定的部分覆盖**（error 路径写回未单独锚）。

**类型一致性检查:** `_SUPPORTED_FINAL_KEYS` **仅新增 `llm_assert`**（A4 定义，B1/B2/B3 使用）；`_LLM_ASSERT_KEYS` 四个子键在 A4 定义、B1 的 case 30 使用（`system_contains` / `message_count`）；`inputs` 新键（`storage_ops` / `custom_events` / `mutate_snapshot` / `storage_writer` / `session_store` / `core_overrides` / `expect_error_regex`）在 A2-A5 定义、B1-B4 使用；两个新分支前缀（`stdio-proxy` / `host-component`）与 `_run_protocol_case` 的 `elif` 一一对应，且**必须同时登记进 `protocol_pipeline_ids`**（否则落 pipeline 类假绿）。
