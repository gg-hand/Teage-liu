# 「部分落实」项统一落地审查

> **定位**：对 `2026-09-11` 执行状态分析中被标记为**「部分落实」**的全部条目做一次统一落地审查——逐项以代码/契约/用例为准核验真实落地程度、缺口、归属与阻塞，并给出最小落地动作。
> **性质**：**审查报告（只读产物）**，不改任何代码/契约；落地动作需另行批准（部分涉及 `PROTOCOL/PENDING.md` 与测试锚定）。
> **审查方式**：独立审查智能体（只读核验 + 复跑门禁）+ 主 agent 抽查复核（`extension_loader.py` / `host_components.py` / `cases/*.json` / `tests_core/test_snapshot_limits.py` / `core/errors.py`）。
> **证据基线**：行为套件 **39/39**、`pytest teage_liu2` **165 passed**（本轮复跑一致）。分支 = `dev`；M1+M2 改动**未提交**。

---

## 0. 审查范围

来自执行状态分析的「部分落实」清单共 6 项：

| # | 事项 | 原登记口径 |
|---|---|---|
| 1 | PENDING **P-1 条件③** 余项「已装未启用统计」 | 部分满足，缺口属 M3/WP-E |
| 2 | PENDING **P-5 条件③** 余项「缺省省略 = 内置 SQLite」 | 部分满足，缺口属 M3/WP-E |
| 3 | PENDING **P-7 条件③** 余项「双档分级观测」 | 部分满足，缺 runner 缓冲观测能力（M3/WP-E） |
| 4 | PENDING **P-8 条件②**「套件对拍 ≥ 12 码」 | 未达成，实际锚定 7 码 |
| 5 | **协议条款覆盖余 7 条**（T-3/T-5/T-8、L-5/L-6、L-10/L-11） | 归 WP-F / WP-E |
| 6 | 计划 **checkbox 文档债** | M1 未回填 |

---

## 1. 总表（审查结论）

| # | 判定 | 关键结论 | 归属 | 裁决阻塞 |
|---|---|---|---|---|
| 1 | **真缺口（小）+ 归属错记** | 统计能力**已在 core 实现且有单测**；缺的是套件用例 + 端点/热重载暴露；**WP-E 无对应任务** | 套件层（无现成归属） | 否 |
| 2 | **真缺口（小）+ 归属错记** | 缺省路径**实现已存在**，**零覆盖**；**WP-E·E2 未包含该路径** | 套件/单测层 | 否 |
| 3 | **真缺口（套件层）+ 表述低估** | 双档**实现完整**、档位选择已有单测；缺套件用例；「runner 缺观测能力」不准确 | 部分落 WP-E·E3 | 否 |
| 4 | **真缺口 + 登记数字过期 + 无归属** | 套件实际 **8 码**（非 7）；套件+单测并集 **12 码**；余 **6 码有发射点零锚定**；**无 WP 承担** | 无（需新增） | 否（口径待裁定） |
| 5 | **1 条已满足未更新 + 6 条真缺口** | **T-8 已实质满足（被低估）**；T-3/T-5/L-5/L-6/L-10/L-11 确无锚定 | WP-F / WP-E | 否（仅 L-11 端到端受 CI 影响） |
| 6 | **文档债** | **0/77 复选框已勾**，其中 **53 项已完成未勾**；顶部免责**只声明 M1**、未覆盖 M2 | WP-G | 否 |

> **未发现「自述已满足实则缺」**；发现 **3 处被低估/归属错记**（#1、#2、#5-T8）与 **1 处汇总数字未达成**（#4）。

---

## 2. 逐项详审

### 2.1 P-1 条件③「已装未启用统计」

| 核验点 | 证据 | 结论 |
|---|---|---|
| 能力是否存在 | `core/extension_loader.py:274-276,317-323` 返回 `disabled_installed` | **存在** |
| 是否记日志 | `server/app.py:136-138` 启动路径 `logger.info("已安装未启用扩展(安装 ≠ 激活)…")` | **有（仅启动）** |
| 是否端点暴露 | `/health`、`/` 均无该字段 | **无** |
| 热重载路径 | `server/routes.py` 热重载处 `_disabled` **被显式丢弃**，重载后统计不更新 | **缺失** |
| 是否有锚定 | `tests_core/test_registry.py:440-443` 断言 `disabled == ["remote_ext"]`；**套件 0 用例** | 单测有，套件无 |

**判定：真缺口（小）+ 归属错记。** 登记「仍属 server 层（M3/WP-E）」不成立：统计已在 core 实现且有单测；真缺 = (a) 套件用例、(b) 端点/热重载暴露；而 WP-E 四任务（routes / host_components / stdio-proxy / L-10·L-11）**无一承担**。

**最小落地动作**
1. 套件：`runner.py::_run_host_component_case` 把丢弃的 `_` 改为 `disabled`，写入 `custom:host_component_result.payload`；新增用例断言「已装未声明 → 不装载且列入 disabled」。
2. 可选（server 层）：热重载记日志；`/health` 增 `installed_disabled` 字段。

### 2.2 P-5 条件③「缺省省略 = 内置 SQLite」

| 核验点 | 证据 | 结论 |
|---|---|---|
| 缺省实现 | `server/host_components.py:35-45` `_sqlite_backend`；`:116-119` 空/缺省 `entries` → `{}`；`:130` `backend` 缺省 `"sqlite"` | **存在** |
| 装配兜底 | `server/app.py:142-147` `loaded.get("history") or SQLiteHistoryStore(...)` | **存在** |
| 是否已覆盖 | 套件 23/24 **均显式给 `host_components`**；`tests_core` **无文件 import `load_host_components`** | **零覆盖** |
| WP-E·E2 是否含 | E2 列举：未知插槽/未知 backend/重复接管/互斥/ABC 不满足/空映射——**无「缺省省略」** | **归属落空** |

**判定：真缺口（小）+ 归属错记。** 实现无须改动，仅缺锚定。

**最小落地动作**：新增用例（套件或 `tests_core/test_server_host_components.py`）：`load_host_components({}, specs) == {}` 且不抛；`host_components: []` 同；并断言装配兜底取 SQLite。

### 2.3 P-7 条件③「双档分级观测」

| 核验点 | 证据 | 结论 |
|---|---|---|
| flush 直发档 | `server/storage_stdio_proxy.py:398-418` `log_message`（先冲刷保 FIFO） | **存在** |
| background 缓冲档 | `:420-458` `log_message_buffered`（关停守卫 + 32 条阈值） | **存在** |
| 窗口/合帧/降级 | `:460-507` `_drain_and_send`、`:509-515` `_flush_loop`（25ms）、`:550-569` `close` 先冲刷 + join | **存在** |
| 观测点 | `metrics_snapshot`（`log_messages` 计**帧数**）；`runner.py` 通用 `storage_ops` 分发 + `assert` **已能驱动** `log_message_buffered`/`metrics_snapshot` | **部分具备** |
| 套件用例 | 用例 18 描述**明文声明双档不在断言范围内**；`custom:stdio_result` 载荷无双档字段 | **缺** |
| 现有锚定 | `tests_core/test_persist_channels.py:75-92`（进程内假 store，仅档位选择；未触 stdio 代理缓冲） | 代理缓冲未锚定 |

**判定：真缺口（套件层）+ 表述低估。** 「runner 缺缓冲观测能力」不准确：`storage_ops` 泛型分发已可调用并断言；缺的是**用例 + 聚合字段**，以及「合帧/窗口」的确定性表达（时序断言不宜进黄金用例）。

**最小落地动作**
1. `tests_core/test_server_stdio_proxy.py`（WP-E·E3）：直发与批量保序 / `close()` 前冲刷 / 关停后 buffered 记 error 不静默。
2. 套件口径：新增用例用 `storage_ops` 调 `log_message`×N + `log_message_buffered`×N + `metrics_snapshot`，断言帧数并把结果写入 `custom:stdio_result`。

### 2.4 P-8 条件②「套件对拍 ≥ 12 码」——逐码清单

**套件 `error_codes.must_include` 并集（实测）**：用例 16（`TOOL_REJECTED_BY_POLICY`）、19（`HOOK_EXCEPTION`）、20（`HOOK_EXCEPTION` / `HOOK_TERMINAL_ACTION_IGNORED` / `TOOL_NO_EXECUTOR` / `LOOP_MAX_REACHED`）、21（`CONFIG_UNKNOWN_KEY` / `CONFIG_INVALID_VALUE`）、27/28（`HOOK_INVALID_ACTION`）、36（`HOOK_TERMINAL_ACTION_IGNORED`）→ **套件并集 = 8 码**。

**单测另锚定 4 码**：`LLM_API_ERROR`、`HOOK_TIMEOUT`、`TOOL_MODIFY_INVALID`、`TOOL_EXEC_FAILED` → **套件+单测并集 = 12 码**。

**对照 `core/errors.py:63-72`（18 码）**：

| 状态 | 码 |
|---|---|
| ✅ 套件锚定（8） | `LOOP_MAX_REACHED`、`HOOK_EXCEPTION`、`HOOK_INVALID_ACTION`、`HOOK_TERMINAL_ACTION_IGNORED`、`TOOL_NO_EXECUTOR`、`TOOL_REJECTED_BY_POLICY`、`CONFIG_UNKNOWN_KEY`、`CONFIG_INVALID_VALUE` |
| ✅ 仅单测锚定（4） | `LLM_API_ERROR`、`HOOK_TIMEOUT`、`TOOL_MODIFY_INVALID`、`TOOL_EXEC_FAILED` |
| ❌ **零锚定（6）** | `LLM_TIMEOUT`、`LLM_CANCELED`、`LLM_STREAM_FAILED`、`STORAGE_WRITE_FAILED`、`STORAGE_READ_FAILED`、`CONFIG_MISSING_KEY` |

> 上述 6 码**发射点均已存在**（`core/step.py`、`core/pipeline.py`、`core/storage_writer.py`、`core/transport.py`、`core/config.py`），属「有发射点、无断言」。

**判定：真缺口。** ① 套件口径 **8 < 12**，条件②未达成；② 登记文本「套件锚定 7 码」**已过期**；③ 若口径改为「套件+单测」，恰为 **12 = 阈值**（边界），**需协议侧裁定口径**；④ **计划无任何 WP 承担补足**。

**最小落地动作**：新增/扩展现有用例断言 6 码中可驱动者（LLM 三码用 `FakeLLMClient` 桩发射 `error.code`；STORAGE_* 用故障 store；`CONFIG_MISSING_KEY` 走 config 域），或明确把口径改为「套件+单测 ≥ 12」并回写 `errors.spec §5` + PENDING。

### 2.5 协议条款覆盖余项（逐条）

| 条款 | 语义（契约源） | 现有锚定 | 判定 |
|---|---|---|---|
| **T-8** 三资源上限 | `types.spec.md` §7 | `tests_core/test_snapshot_limits.py` 三条上限**各有断言**（`max_message_bytes` / `max_messages_per_conversation` / `max_snapshot_bytes`） | **已实质满足 · 登记低估** |
| **T-3** 不可变只读 | `types.spec.md` §T-3 | 无专项测试；`cases/31-hooks-readonly` **如实锚定「篡改生效」**（缺口已登记 `CORE-缺口记录.md` H-17，P2 未修） | **真缺口（且为 core 真实缺口）** |
| **T-5** 结构共享/禁 deepcopy | `types.spec.md` §T-5 | 无对象同一性 / `deepcopy` 计数断言 | **真缺口** |
| **L-5** 每扩展一进程 | `lifecycle.spec.md` | 无 `test_supervisor.py`；实现见 `core/supervisor.py` | **真缺口** |
| **L-6** 崩溃隔离 | `lifecycle.spec.md` | 无测试；实现见 `core/supervisor.py`（心跳 → on_dead → 重建/降级） | **真缺口** |
| **L-10** 会话态 extra 延续 | `lifecycle.spec.md` | 实现完整（`core/pipeline.py` 恢复/写回 + `server/app.py` 接线）；测试仅容器语义 | **真缺口** |
| **L-11** 会话并发互斥 | `lifecycle.spec.md` | 实现完整（`server/session_locks.py` 严格 LRU + 持有中不淘汰）；**零测试** | **真缺口** |

**判定**：**1 条已满足未更新（T-8）+ 6 条真缺口。** 计划 §13 把 T-8 计入未覆盖，属**低估**。

**最小落地动作**：`test_snapshot_identity.py`（T-3/T-5，WP-F·F3）、`test_supervisor.py` / `test_remote_adapter.py`（L-5/L-6，WP-F·F2）、L-10 用 runner 的 `session_store` 输入加套件用例或 `test_pipeline` 两轮用例、L-11 用 `SessionLocks` 纯异步单测（并发 `get` 同 session → 串行）。

### 2.6 checkbox 文档债

| 核验点 | 结论 |
|---|---|
| 未回填 | 步骤复选框 **0/77 已勾**（`- [x]` = 0） |
| 分档 | M1 = 14、M2 = 39、M3 = 20、M4 = 4；**已完成未勾 53 项**，真待办未勾 24 项 |
| 矛盾 | 顶部注记**只声明 M1** 未回填；**M2（WP-A 25 + WP-B 14）同样已完成却未声明**，读者会误判 WP-A/WP-B 未执行 |

**判定：文档债（非事实矛盾）。** **最小落地动作**：把顶部免责改为「M1 + M2 复选框未回填，以本条与 `2026-09-11-M2行为套件场景能力-执行计划.md` 为准」，或直接回填 M1/M2 共 53 项。

---

## 3. 登记勘误（本次审查新增）

| # | 原文/自述 | 事实 | 处置建议 |
|---|---|---|---|
| E-1 | P-1 ③「余项属 server 层（M3/WP-E）」 | 统计已在 core 实现 + 单测；缺套件用例与端点暴露；WP-E 无该任务 | 改登记：拆为「套件用例（新增）」+「端点/热重载暴露（可选）」 |
| E-2 | P-5 ③「属默认路径，M3/WP-E 覆盖」 | 实现已有、零覆盖；WP-E·E2 不含该路径 | 改登记：明确归属为「新增 host_components 缺省路径用例」 |
| E-3 | P-7 ③「runner 缺缓冲观测能力」 | 观测点已具备（`storage_ops` 泛型分发 + `metrics_snapshot`）；缺用例 | 改为「缺套件用例与聚合字段」，非能力缺失 |
| E-4 | P-8 ②「实际套件锚定 **7** 码」 | **8 码**（M2 后 +1）；并集 12 码 | 更新数字 + 裁定口径（套件 vs 套件+单测） |
| E-5 | 计划 §13「T-8 归未覆盖」 | T-8 **已实质满足**（三条上限均有断言） | 从未覆盖清单移除，未覆盖降为 6 条 |

---

## 4. 归属真空与阻塞

**归属真空（4 项）**：P-1 ③、P-5 ③、P-7 ③（套件层）、P-8 ② —— **现有 M3 任务清单均无对应任务**。若照计划执行 M3，这四项仍不会闭合。→ 建议 M3 执行前**补任务**（或在 PENDING 显式改判条件）。

**裁决阻塞现状**

| 阻塞项 | 影响 | 状态 |
|---|---|---|
| M3·WP-E 步骤 0（CI 只装 `pytest pyyaml jsonschema python-dotenv`，`TestClient` 需 `fastapi`+`httpx`） | 任何顶层 `from fastapi.testclient import TestClient` 的新测试 → CI 收集期 ImportError、audit 必红 | **未裁决**（方案 A 改 workflow 一行 / B 拆 `tests_server/` / C 不用 FastAPI；可另评估 `pytest.importorskip`） |
| L-11 端到端并发 | 仅「经 routes 的真并发」受上项影响；`SessionLocks` 单元级并发**不受影响** | 部分阻塞 |
| T-3 只读强制 | 是否强制只读 = 需决策（当前 `cases/31` 如实锚定未修复缺陷） | 待决策 |

**其余 5 项均无裁决依赖**（套件/单测层可在无 `fastapi` 下完成）。

---

## 5. 可立即落地 vs 待裁决

**可立即落地（零裁决依赖）**

1. 登记勘误 E-1..E-5（`PROTOCOL/PENDING.md` + 计划 §13）。
2. checkbox 文档债（计划顶部免责或回填 53 项）。
3. P-1 ③ 套件用例 + 载荷加 `disabled`。
4. P-5 ③ 缺省路径用例。
5. P-7 ③ 套件用例（+ E3 单测层）。
6. P-8 ② 补 6 码锚定（或裁定口径）。
7. WP-F 全部（T-3/T-5/T-8/L-5/L-6 单测）；L-10、L-11 单元级。
8. P-8 条件③ 机械校验：加一条 `set(ERROR_CODES) == set(schema enum)` 单测（当前人工比对一致、无回归防护）。

**待裁决（勿动）**

- WP-E 步骤 0 的 CI 依赖方案（涉及 `.github/workflows/*` 配置改动，须批准）。
- L-11 的 routes 级真并发端到端（同上）。
- T-3 是否强制只读（core 语义决策）。

---

## 6. 结论

**有条件通过。** M1/M2 完成度属实（39/39 + 165 passed 已复跑）；「部分落实」6 项经审查后精确化为：

- **登记低估 3 处**（P-1 ③、P-5 ③、T-8）——能力/实现已具备，仅缺锚定或登记未更新；
- **表述失准 1 处**（P-7 ③「缺能力」实为「缺用例」）；
- **真缺口且无归属 4 处**（P-1 ③/P-5 ③/P-7 ③ 套件层、P-8 ②）——需在 M3 前补任务；
- **真缺口有归属 6 条**（T-3/T-5/L-5/L-6 → WP-F；L-10/L-11 → WP-E），均可无 `fastapi` 落地（L-11 仅端到端受 CI 裁决影响）；
- **文档债 1 处**（复选框 0/77，53 项已完成未勾、免责未覆盖 M2）。

**P-8 ② 是唯一"数字未达阈值"的硬缺口**（套件 8 < 12；并集恰 12），口径需协议侧裁定。

---

## 7. 落地执行结果（2026-09-11，用户批准后）

> 本节记录 §5「可立即落地（零裁决依赖）」清单的执行结果。**未触碰待裁决项**（WP-E 步骤 0 的 CI 依赖、L-11 的 routes 级端到端真并发、T-3 是否强制只读）。

| 项 | 结果 |
|---|---|
| 登记勘误 E-1..E-5 | **已完成**：`PROTOCOL/PENDING.md` P-1/P-5/P-7 三条现状勘误 + 条件③改判；P-8② 数字与口径更新、P-8③ 补机械校验说明；协议收口计划 §13 把 T-8 移出未覆盖（20 → 6 条）、顶部 checkbox 免责覆盖 M1+M2（0/77 未勾） |
| P-1③ 套件用例 | **已完成**：`host-component-disabled-40`（未声明不启用）+ `host-component-uninstalled-43`（声明未装＝启动失败）；`wire_extensions` 的 `disabled` 载荷并入 `custom:host_component_result` |
| P-5③ 套件用例 | **已完成**：`host-component-default-41`（缺省省略＝空映射）+ `tests_core/test_server_host_components.py`（缺省/空数组/快速失败矩阵 11 条） |
| P-7③ 套件用例 | **已完成**：`stdio-proxy-dual-channel-42`（真实子进程双档 + 保序）+ `tests_core/test_server_stdio_proxy.py`（5 条） |
| P-8② 补锚定 | **已完成**：`error-codes-20` 扩 4 场景（套件并集 8 → **12 码**）；新增 4 个单测文件 → **并集 18/18 全码（脚本实测，缺失 0）** |
| P-8③ 机械校验 | **已完成**：`tests_core/test_error_codes_schema.py` 双向比对 ERROR_CODES ↔ `errors.schema.json` |
| WP-F 全部 | **已完成**：`test_snapshot_identity.py`（T-3/T-5）、`test_supervisor.py` + `test_remote_adapter.py`（L-5/L-6）、`test_assembler.py` / `test_modes.py` / `test_step_error_codes.py` |
| L-10 / L-11 单元级 | **已完成**：`test_session_extra_continuity.py`（跨对话 extra 延续）、`test_session_locks.py`（同 session 串行 / LRU / 持有不淘汰） |

**门禁（实测）**：行为套件 **43/43**；`pytest teage_liu2` **218 passed**；`scripts/audit_liu2.ps1` **exit=0**。
**变异验证**：5 处退化（`ERROR_CODES` 去码 / `STORAGE_WRITE_FAILED` 去前缀 / `log_message` 免冲刷 / `disabled_installed` 恒空 / 缺省路径抛错）→ 单测 7 红 + 套件 3/3 红；恢复后全绿（另：错误码矩阵首版因传入"异常实例"而非"工厂"导致 `TypeError` 被兜底为 `LLM_API_ERROR`，反证该断言承重）。

**PENDING 条件状态（本次后）**

| 条目 | 条件③ | 备注 |
|---|---|---|
| P-1 | **已满足**（23/24/40/43 四例） | 四条条件全满足，具备转 `stable`（转档待单独评审） |
| P-5 | **已满足**（23/24/41 + 单测） | 四条条件全满足 |
| P-7 | **已满足**（26/42 + 单测） | ①②③满足 |
| P-8 | ②③ **已满足** | 四条条件全满足；套件 12 码 + 并集 18/18 |

**残余（未做，非本次范围）**：P-1 的"端点/热重载暴露 `disabled_installed`"（可选观测项）；`P-2` 条件②的"未上报协议版本"为显式拒绝路径（与设计一致，非缺口）；**严格套件口径**下 `LLM_CANCELED` 仍只在单测锚定（`core/step.py` 用 `logger.info`，低于套件日志捕获的 warning 阈值）——如需套件内锚定，可选①改 `logger.warning` ②runner 在运行时下调包 logger 级别，均属独立小改，未擅自改动 core 日志级别。
