# liu2 综合评审修复计划（2026-09-11）

> **执行状态（2026-09-11）**：**WP-A..G 全部完成**。门禁实测 = 套件 **45/45**（新增用例 45-resource-limits）、pytest **258 passed**、lints 0。变异验证承重记录：WP-A（缓存实例 → 并发隔离测试红）、WP-B（negotiate 恒 proceed / encoding 放行 / L1 过滤删除 / rebuild 不回滚 → 用例 15/10/13/14+04/34 红）、P1-1（先落盘旧序 → 红）、P1-5（删 try → 红）、P1-3（删重注册 → 红），还原后逐字节核对（MUTATION 标记零残留）。执行期现场发现并当场修复 1 处自身回归：变异还原时误删校验后 `_persist` 落盘行（user 消息不落盘），由门禁前自检抓获补回。未执行项见 §3"不做"。

> 依据：`docs/plans/2026-09-11-liu2深度综合评审报告.md`（P0×2 / P1×11 / P2×18）。
> 范围 = P0 全部 + P1 全部 + 可低风险落地的 P2 与覆盖缺口 + 文档一致性；**不含**：P-1..P-8 转档与 VERSION 升版（转档须在本计划完成后再评审）、workflow/custom:*/capability（已单独立项）、Rust 侧、CI workflow 触发路径调整（涉 `.github/` 另批）。
> 纪律：全程变异验证（退化必红 + 还原逐字节）；不写临时脚本入库；git 写操作另行请批。

## §0 事实复核（相对评审报告的精确化）

1. **P0-1 根因确认**：`pipeline.py:110-118` 形态实例构造一次跨对话复用；`loop.py:107,118,172,183,275,286,345,360` 与 `modes.py:41` 各推进点覆写 `self.final_snapshot`；`pipeline.py:193,332-334,363,371` 四处读它。`routes.py:44-45,87-88` 锁按 session，**不同 session 真并发**。
2. **P0-2 断言值确认**：用例 15 七场景 = proceed **2**（equal/older）、downgrade **2**（newer/missing）、reject **3**（mismatch_newer/mismatch_older/invalid_version）；`match_value`（runner.py:90-113）`{type:"boolean"}` 对 False 通过。`_run_lifecycle_reload`（runner.py:1206）payload 三键对应用例 14 三断言。
3. **P1-2 顺序确认**：`app.py:251-268`：`registry.shutdown`（内部 :279-285 先 cancel_all→teardown→message_store.close→storage_provider.close）→ supervisor.shutdown → l3_sink.close → **最后** storage_writer.close()。teardown 期间扩展冲刷写（经 StorageWriter 队列）与尾部残余写都打在已 close 的连接上。
4. **registry.shutdown 已支持部分关闭**（`registry.py:279-285`：传 None 跳过对应 close）→ 外壳层重排即可，core 零改动。
5. **P1-5 确认**：`schema_check.py:59-80` 数值/长度比较直接用 schema 提供的值；`hooks.py:378-394` 调用点无 try。
6. **P1-7 确认**：`_check_unknown_extension_inputs`（runner.py:501）仅在 `_run_pipeline_case`（:725）内调用；`_run_protocol_case` 未调用。`cases/11` 声明 `read` op 但 storage 分支未实现。
7. **P1-9 需双查**：套件 `setup.extensions[].lang` 用 `lang`；`lifecycle.schema.json` 的 Extension 定义属性名待执行时核对（报告称 `:74` 为 `lang`）；**manifest**（ExtensionManifest）字段名 `language` 是另一对象，勿混改。
8. `configure_limits` 为全局（`snapshot.py:81-101`），reload 重放后对运行中 pipeline 即时生效（模块级），可接受（单 pipeline 形态）。
9. **（plan-verifier 补）** `_mode_instance` 全仓共 **7 处**引用，全部在 `core/pipeline.py`：:118（构造）、:193、:332-334（getattr）、:363（getattr）、:371（getattr）、:396（rebind_hooks）、**:422（`_iter_events` 内 `self._mode_instance.run_stream(...)`）**——tests_core/runner 零引用。WP-A 须覆盖全部 7 处。
10. **（plan-verifier 补）** runner `_run_pipeline_case` 每用例只执行**一次** `chat_stream`（:787-793），无多轮驱动机制；且构造 ChatPipeline（:764-775）**未把 `overrides["resource_limits"]` 接入** `ChatPipeline(resource_limits=)`。另：`tests_core/test_session_extra_continuity.py` 已存在（L-10 跨对话 extra 延续单测锚定）。`supervisor._register_extension`（supervisor.py:122-127）= `bus.register_extension` + observe 时 `l3_sink.subscribe` —— P1-3 回滚重注册修法可行。

## §1 工作包

### WP-A（P0-1）终态快照对话级交付 —— core

**修法**（形态实例改"每对话新建"，删共享实例字段）：

1. `core/pipeline.py`（覆盖全部 7 处 `_mode_instance` 引用，§0.9）：
   - `__init__`：删除预构造的 `mode_instances`/`self._mode_instance`（:108-118）；**保留启动期未知 mode 校验**（构造时对静态 mode 集合 `{MODE_BARE, MODE_LOOP}` 校验并抛 ValueError——维持 Q8"构造即失败"设计意图），并补 `self.max_loops = max_loops` 存储；
   - 新增 `_make_mode_instance()`：按 `self.mode` 返回 `BareMode(self.step)` / `ReactLoop(self.llm_client, self.hooks, max_loops=self.max_loops)`（未知 mode 兜底抛 ValueError，同文案）；
   - `chat_stream` 内局部 `mode_instance = self._make_mode_instance()`（:193 之前），四处引用（:193/:332-334/:363/:371）改局部变量；删 `:190-193` 的"防陈旧快照"注释与赋值（每对话新实例天然无残留）；
   - `_iter_events`（:413-429）：增加 `mode_instance` 形参，:422 改 `mode_instance.run_stream(...)`；调用点（:259）传局部实例；
   - `rebind_hooks`（:390-399）：删除对 `mode_instance.hooks` 的同步（新对话经 `_make_mode_instance()` 用 `self.hooks` 新建实例，旧对话继续用旧链——语义正确且消除跨对话引用污染）；保留 self.hooks 赋值与日志。
2. `core/modes.py` BareMode / `core/loop.py` ReactLoop：**不改**（final_snapshot 仍是实例字段，但实例生命周期 = 单对话）。
3. 全仓 grep `self._mode_instance` / `_mode_instance` 兜底（tests_core/runner 若有引用一并改）。

**验收**：新增 `tests_core/test_pipeline_concurrency.py`：
- `test_concurrent_sessions_isolated_final_snapshot`：两个 session 并发 `chat_stream`（FakeLLM + 门控 await），断言各自 after/on_error 收到的快照 `session_id` 正确、`session_store.get(s1)` 不含 s2 的 extra；
- `test_rebind_hooks_uses_new_chain_next_conversation`。
**变异验证**：临时把 `_make_mode_instance` 改回共享实例 → 新测试红；还原逐字节。

### WP-B（P0-2 + P1-7/P1-8）行为套件承重断言 + 守卫上移

1. **用例改精确断言**（`cases/`）：
   - `14-lifecycle-reload.json`：`reload_success_entries: 1`、`rollback_preserved_old_chain: true`、`setup_idempotent_reentrant: true`；
   - `15-evolution-negotiation.json`：`proceed_cases: 2`、`downgrade_cases: 2`、`reject_cases: 3`、`all_actions_match: true`；
   - `10-transport-frame.json`：九键全部改 `true`（对齐 `errors.spec.md:85` 精确布尔断言纪律）；
   - `13-l3-observe.json`：`observe_received_types: ["tool_use","tool_result","step_end"]`、`l1_never_in_transport: true`、`bounded_queue_no_drop: true`。
2. **runner 硬校验**（`runner.py:1147-1168` negotiation 分支）：`actual != expect` 时除置 `all_match=False` 外**同步 `errors.append(f"[{case_id}] {label}: expect {expect}, got {actual}")`**（逐场景失败立即红，不依赖 payload 回环）。
3. **守卫上移**（P1-7）：`_run_protocol_case`（:961 附近）对 `inputs` 同样调用 `_check_unknown_extension_inputs`；对 storage 分派循环（:1021 起合并 `inputs.storage_ops` 与 `inputs.host_port_ops`）的 **op 加合并枚举白名单**（`write/read/query/write_cross_prefix/invoke_llm/task_register/task_cancel`，两类 ops 同一循环统一校验）。
4. **`read` op 接线**：`_run_storage_case` 实现 `read`（`host_port.storage_read` 回读 write 写入的 doc，断言往返一致）；`cases/11` 的 read 场景补 `assert` 使其承重。
5. `cases/23,40,43` 的 `inputs.extension_name` 零消费 → 在 `_run_host_component_case` 中真实消费（用其构造 manifest name / 断言被拒名）。
6. 删除 `runner.py:753-754` 陈旧注释（"config 键未接线"→ 改述为"经 `ChatPipeline(resource_limits=)` 接线"）。

**验收**：套件全绿 + 计数不降。
**变异验证**：①negotiate 全返回 proceed → 用例 15 红（errors 硬校验）；②`registry.rebuild` 回滚注释退化 → 用例 14 红；③`TransportFrame.decode` 去非法 encoding 校验 → 用例 10 红；④`route_l3` 去 L1 过滤 → 用例 13 红。

### WP-C（core P1-1/P1-4/P1-5 + P1-3 + P2 群）

1. **P1-1**（`pipeline.py:148-189`）：把 `snapshot_with_user_message` 校验与 `budget_problem` 处理**移到 `_persist(user, flush=True)` 之前**（构造 snapshot 后立即校验；失败 yield error + return，**不落盘**）；成功后按原序落盘。注释同步。
2. **P1-4**（`loop.py:131-136`）：error 事件补 `"code": HOOK_INVALID_ACTION`（import 补 `HOOK_INVALID_ACTION`）。
3. **P1-5**（`hooks.py:376-394`）：H-20 校验段包 `try except (TypeError, ValueError, KeyError, RecursionError)` → 记 `TOOL_MODIFY_INVALID` error 日志（"schema 自身非法，校验器失能按 allow 放行"）后放行 cur_input。
4. **P2 群**（一次性小改）：
   - `loop.py`：每个工具执行前记 `tool_started = time.monotonic()`，`exec_duration = time.monotonic() - tool_started`；
   - `storage.py`：补 `from typing import Union`；
   - `hooks.py:491-494,503-506`：日志字面量改 `%s` 传 `HOOK_TERMINAL_ACTION_IGNORED` 常量；
   - `remote_adapter.py:190-196`：`decision` 值非 allow/reject/modify → 记 error 日志并按 allow 降级（构造 ToolDecision(decision="allow")）；
   - `tasks.py:72-78`：先改 `task.status` 再 `pop`（或 pop 后仍更新字典外对象——以"状态变更可观测"为准）；
   - `storage_writer.py:203-217`：重试成功路径统一 `task.resolve(result=result)`；
   - `history.py:294,305,335`：读路径与写路径统一 `with self._lock`；
   - `stdio.py`：spawn StreamReader 显式 `limit=FRAME_MAX_BYTES + 65536`（帧长检查恢复可达）；
   - `pipeline.py:323`：`_route_l3(copy.deepcopy(ev))`（L3 旁路事件小，防嵌套共享同源污染；import copy）；
   - `types.py:498`：docstring 措辞收敛为"容器级防污染（元素共享引用，嵌套可变性由 T-5 禁 deepcopy 折衷保留）"。
5. **P1-3 热重载回滚不恢复旧扩展宿主通道**（`supervisor.py`）：
   - `reload` 回滚分支（:215-225）：回滚前先保存 `old_configs = dict(self._ext_configs)`；回滚时恢复 `self._ext_configs = old_configs`，并对 `new_channels` 中**同时存在于旧集合**的名字（同名重载场景）重新调用 `_register_extension(name, self._ext_configs[name])`（恢复 bus 身份 + L3 订阅，supervisor.py:122-127），附 info 日志"回滚后已恢复旧扩展宿主注册"；对仅新增（旧集合没有）的名字维持现状（注销即正确）；
   - 锚定：新增单测 `tests_core/test_supervisor.py` 补 `test_reload_rollback_restores_bus_identity`（同名扩展 reload 失败回滚 → `bus` 能再次受理该扩展 storage_write，且 L3 订阅仍在）。
   **变异验证**：删除重注册调用 → 新单测红（回滚后 storage_write 被拒 ERR_UNAVAILABLE）。

**验收**：pytest 全绿。
**变异验证**：P1-1 还原顺序 → 新增单测红（超限输入不落库断言）；P1-4 删 code → `tests_core/test_error_codes_events.py` 或套件红；P1-5 删 try → 新增单测红（畸形 schema 不炸对话）。

### WP-D（server P1-2/P1-6 + P2 群）

1. **P1-2 shutdown 重排**（`app.py:248-272`，core 零改动，利用 registry.shutdown 传 None 跳过 close）：
   ```
   await registry.shutdown(task_registry=task_registry, message_store=None, storage_provider=None)  # cancel_all + teardown
   storage_writer.close()          # 先排空写队列(此时连接仍开)
   history_store.close(); storage_provider.close()   # 再关存储
   await supervisor.shutdown()     # 关扩展进程
   await l3_sink.close(); llm_client.close()
   ```
   每段保留 try/except 告警；注释与 `CORE.md` §6 同步改。
2. **P1-6**（`routes.py:185` 前）：reload 成功路径补
   `from ..core.snapshot import configure_limits; configure_limits({...三键 from new_core_config...})`。
3. **P2 孤儿进程**（`app.py:239-247` + `host_components.py`）：lifespan 启动段包 try → 失败时逆序执行 `supervisor.shutdown()` 与已 start 组件的 abort/close 再 raise；`host_components.py` ABC 校验移到 `proxy.start()` 之前（先校验后启动）。
4. **P2 小改**：`storage_stdio_proxy.py:25` 改用 core 公开入口（`core/storage.py` 增 `validate_kind()` 公开函数，内部转调 `_validate_kind`）；`routes.py` `/chat` error 返回前显式 `await pipeline_gen.aclose()`（消费完再返回，口径与 SSE 一致）。

**验收**：pytest 全绿（新增 shutdown 顺序单测：`test_app_shutdown_drains_writer_before_store_close`——用可观测 fake store 断言 close 调用序）。
**变异验证**：顺序对调回旧版 → 新单测红；reload 去 configure_limits → 新单测红。

### WP-E（契约文本统一，全部为文档/schema 层）

1. `lifecycle.spec.md` §2：Extension 定义字段名与 schema 对齐（执行时先核对 `lifecycle.schema.json` Extension 实际属性名，spec 改为与其逐字一致；**manifest 的 `language` 字段不动**）；§2 补注"build_injections 对进程内枝干为隐式参与钩子（无需 hooks_implemented 声明）"。
2. `events.spec.md` §1：step_start 行补"step 从 1 起（1-based 轮次）"。
3. `PENDING.md:48`（P-2）：统一为"未上报 → downgrade"口径，删过时括注。
4. `errors.spec.md:16`（§2 括注）：改为"行为套件锚定 13/18 码，其余 5 码由 tests_core 单测锚定（见 §5/R-2）"——具体用例编号清单执行时以 `_extract_codes` 实测为准（不预先硬编码未经复核的清单）。
5. `transport.spec.md:29`（T-2）："schema 级校验" → "宿主消息层校验（kind pattern 仅约束字符集，per-extension 前缀由宿主运行时强制）"。
6. `config.spec.md` §1：注明"未知键严格校验限 core 段；根级允许扩展段键"。
7. `types.spec.md` §7：ResourceLimits 三值注明"协议默认值，宿主可经 config 收紧、不可放宽拒绝语义"。
8. `errors.spec.md` §3：登记 H-5 拦截路径剩余工具的 `tool_result.code = TOOL_REJECTED_BY_POLICY` 为可接受折衷（一句话）。

### WP-F（覆盖缺口补齐）

1. **跨 session 并发**：见 WP-A 测试（同时闭合 P0-1 盲区）。
2. **types T-8 套件用例**：新增 `cases/45-resource-limits.json`（`core_overrides` 传低上限 → 超限 append 被拒、set 类保留；id 前缀走 pipeline 分支）。**前置改造**：runner `:764-775` 构造 ChatPipeline 处补 `resource_limits=overrides.get("resource_limits")`（当前未接线，此键是死输入）——接线后自然消除死输入，无需动 `_check_unknown_extension_inputs`（该守卫只查 `<扩展名>_<后缀>` 形式的键）。
3. **lifecycle L-10**：**不做套件用例**（runner `_run_pipeline_case` 每用例仅单次 `chat_stream`、无多轮驱动，改造 runner 属大改——plan-verifier 核实）；既有 `tests_core/test_session_extra_continuity.py` 已单测锚定，在 `CORE-缺口记录.md` 中把 L-10 登记口径更新为"tests_core 承载（套件不锚定，runner 无多轮驱动）"。
4. **单测补齐**（`tests_core/`）：observe 扩展 `pre_tool_call` 决策忽略（hooks.py:341-355）；H-5 post_tool_call 分支轮中短路 + 剩余工具拦截配对（loop.py:287-314）。
5. **空断言清理**：删 `test_server_host_components.py:101-103`。
6. **tests_branches CI 全 skip**：登记不改（依赖仓库外 `data2/`，处置需另议——见 §3 不做项）。

### WP-G（文档一致性）

1. `teage_liu2/docs/INTERFACES.md:394`：`17/17` → 实际套件数（执行时按最终用例数回写，预计 46/46）；
2. `teage_liu2/docs/CORE.md` §2：`after(self, snapshot, response: Any)` → `after(self, snapshot: Snapshot, response: AfterResponse)`；
3. `teage_liu2/docs/CORE.md` §6：shutdown 编排顺序与 WP-D 新顺序同步；
4. `teage_liu2/docs/plans/CORE-缺口记录.md`：本轮新修项迁入"已修复"（H 系列编号顺延登记）；
5. 根 `CHANGELOG-开发日志.md` 与 `teage_liu2/docs/plans/开发日志.md` 顶部各追加本计划执行条目。

## §2 执行顺序与门禁

1. WP-A → WP-B → WP-C → WP-D → WP-E → WP-F → WP-G；
2. 每工作包完成后立即跑：`D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py` + `pytest tests_core tests_branches -q`；
3. 最终门禁：套件全绿（预计 **45/45**，仅新增用例 45）+ pytest 全绿 + `scripts/audit_liu2.ps1` exit=0 + lints 0 + 全部变异验证记录；
4. 变异验证纪律：每个 P0/P1 修复至少 1 项"退化必红"，验证后**逐字节还原**（`git diff` 核对为空）。

## §3 风险与不做

**不做**：①P-1..P-8 转档与 VERSION 升版；②workflow/custom:*/capability 立项项；③CI workflow 触发路径/PR paths 修正（涉 `.github/`，另批）；④tests_branches CI 全 skip 的根治（需仓库内复刻 guardrails 夹具，涉及 `data2` 约定，另行裁决）；⑤`_ACTIVE_LIMITS` 实例化改造（多实例装配未出现，登记观察）。
**风险**：WP-A 改 pipeline 构造面（`_mode_instance` 外部引用需 grep 兜底，低风险）；WP-B 改 4 个用例 JSON（套件是冻结交付物，断言收紧不改语义，属"修正假绿"非契约变更）；WP-D shutdown 重排需与 CORE.md §6 同步（文档-代码一起改，防再漂移）。

## §4 审查记录

**执行后审查（plan-auditor，2026-09-11）**：**带缺陷通过 → 缺陷已全部补齐**。核对 38 项：落实 33 / 缺陷 5。修复情况：

| 审查发现 | 处置 |
|---|---|
| [必修] host_components ABC 校验未前移（`proxy.start()` 先行） | ✅ 工厂内启动前过 SLOTS 全 ABC 自检（host_components.py） |
| [必修] shutdown 顺序单测缺失 | ✅ 新增 `test_app_shutdown_drains_writer_before_store_close`（类方法 monkeypatch 记录 close 序，变异 S1 顺序对调 → 红） |
| [次修] P1-6 reload 重放无锚定 | ✅ 新增 `test_reload_replays_resource_limits`（变异 S2 删 configure_limits → 红） |
| [次修] 用例 45 断言不承重（merge 使角色序列不变，S3 变异未红） | ✅ 重写：加 watcher 扩展 + invocations 快照断言（messages length=2 + extra set 类保留）——S3 变异实测变红 |
| [次修] L-10 口径未登记 | ✅ CORE-缺口记录.md 登记"tests_core 承载" |
| [次修] CORE.md:243"单快照无共享"表述未动 + INTERFACES 3 处旧 after 签名 | ✅ 全部对齐（P0-1 语义 / AfterResponse） |
| [登记] stdio 超大帧（>4MiB+64KiB）杀通道边界 | ✅ stdio.py 注释登记（降级丢帧不立项，B1 兜底） |

补齐后门禁：套件 **45/45**、pytest **260 passed**、lints 0；变异 S1/S2/S3 全承重、还原零残留。

---

**plan-verifier 审查（2026-09-11，执行前）**：总体判定"修正后执行"，证据核对通过率极高（绝大多数 `文件:行号` 逐条属实）。发现的 7 个问题已全部回写本计划：

| # | 审查发现 | 处置 |
|---|---|---|
| 1（致命） | WP-A 漏 `pipeline.py:422`（`_iter_events` 内）引用，按原计划执行必 AttributeError | §0.9 登记 7 处全量；WP-A 增 `_iter_events` 形参化 |
| 2（致命） | WP-F.3 夸大 runner 能力（无多轮驱动，`session_store` 只建不驱动） | L-10 改由既有 `test_session_extra_continuity.py` 承载，套件不补例；缺口登记口径更新 |
| 3（主要） | WP-F.2 `core_overrides` 未接 `resource_limits`（死输入），"白名单补键"指向错误 | WP-F.2 改为 runner :764-775 显式接线 + 删白名单补键表述 |
| 4（主要） | P1-3 整项遗漏，与"P1 全部"矛盾 | 新增 WP-C.5（回滚后重注册 + 单测 + 变异验证）|
| 5（次要） | `_make_mode_instance` 引用 `self.max_loops` 未存储 | WP-A 补 `self.max_loops = max_loops` |
| 6（次要） | 未知 mode 校验时机两可（构造期 vs 首对话） | WP-A 明确构造期保留校验（Q8 设计意图）|
| 7（次要） | op 白名单漏 `host_port_ops`（与 storage_ops 合并分派） | WP-B.3 改合并白名单 |

审查确认的关键可行性：`registry.shutdown` 传 None 跳过 close 与 `_shutdown_done` 兼容；stdio proxy close 双调幂等有兜底；`host_port.storage_read` 真实存在（transport.py:711）；用例 14/15 精确断言值逐条核对无误；WP-A 的 ReactLoop/BareMode 构造签名一致、StepExecutor 无对话级状态可复用。
执行时注意（审查提示）：stdio 加 `limit=` 后 4MiB+ 区间日志噪音；storage close 早于 supervisor.shutdown 为严格改善但需保留"storage 后拒绝"error 日志预期；WP-E.4 用例编号以实测为准（已回写）。
